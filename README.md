# Athanas Inspires Website

This project has two clear layers:

- `source/` — the clean files you study and edit.
- `website/` — the completed website that opens normally and is recreated on every build.

Start with `guides/START-HERE.md`. On Windows, run `SET UP WEBSITE.bat` once, then use `UPDATE WEBSITE.bat` after source changes.
