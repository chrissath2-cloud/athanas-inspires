# Change Colours, Fonts, and Spacing

Start in `source/styles/settings/design-settings.css`. The values have human-readable names such as `--ai-brand-yellow`.

The first migration preserves the current design exactly, so some older compatibility rules still contain direct colour values. The design settings file is the official control centre for all new or gradually modernised components. Do not perform a global colour replacement without checking every page.
