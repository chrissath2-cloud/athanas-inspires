(function(){
"use strict";
const __mods={"/QR8bitByte.js":function(module,exports,require){
var QRMode = require('./QRMode');

function QR8bitByte(data) {
	this.mode = QRMode.MODE_8BIT_BYTE;
	this.data = data;
}

QR8bitByte.prototype = {

	getLength : function() {
		return this.data.length;
	},
	
	write : function(buffer) {
		for (var i = 0; i < this.data.length; i++) {
			// not JIS ...
			buffer.put(this.data.charCodeAt(i), 8);
		}
	}
};

module.exports = QR8bitByte;

},
"/QRBitBuffer.js":function(module,exports,require){
function QRBitBuffer() {
	this.buffer = [];
	this.length = 0;
}

QRBitBuffer.prototype = {

	get : function(index) {
		var bufIndex = Math.floor(index / 8);
		return ( (this.buffer[bufIndex] >>> (7 - index % 8) ) & 1) == 1;
	},
	
	put : function(num, length) {
		for (var i = 0; i < length; i++) {
			this.putBit( ( (num >>> (length - i - 1) ) & 1) == 1);
		}
	},
	
	getLengthInBits : function() {
		return this.length;
	},
	
	putBit : function(bit) {
	
		var bufIndex = Math.floor(this.length / 8);
		if (this.buffer.length <= bufIndex) {
			this.buffer.push(0);
		}
	
		if (bit) {
			this.buffer[bufIndex] |= (0x80 >>> (this.length % 8) );
		}
	
		this.length++;
	}
};

module.exports = QRBitBuffer;

},
"/QRErrorCorrectLevel.js":function(module,exports,require){
module.exports = {
	L : 1,
	M : 0,
	Q : 3,
	H : 2
};


},
"/QRMaskPattern.js":function(module,exports,require){
module.exports = {
	PATTERN000 : 0,
	PATTERN001 : 1,
	PATTERN010 : 2,
	PATTERN011 : 3,
	PATTERN100 : 4,
	PATTERN101 : 5,
	PATTERN110 : 6,
	PATTERN111 : 7
};

},
"/QRMath.js":function(module,exports,require){
var QRMath = {

	glog : function(n) {
	
		if (n < 1) {
			throw new Error("glog(" + n + ")");
		}
		
		return QRMath.LOG_TABLE[n];
	},
	
	gexp : function(n) {
	
		while (n < 0) {
			n += 255;
		}
	
		while (n >= 256) {
			n -= 255;
		}
	
		return QRMath.EXP_TABLE[n];
	},
	
	EXP_TABLE : new Array(256),
	
	LOG_TABLE : new Array(256)

};
	
for (var i = 0; i < 8; i++) {
	QRMath.EXP_TABLE[i] = 1 << i;
}
for (var i = 8; i < 256; i++) {
	QRMath.EXP_TABLE[i] = QRMath.EXP_TABLE[i - 4]
		^ QRMath.EXP_TABLE[i - 5]
		^ QRMath.EXP_TABLE[i - 6]
		^ QRMath.EXP_TABLE[i - 8];
}
for (var i = 0; i < 255; i++) {
	QRMath.LOG_TABLE[QRMath.EXP_TABLE[i] ] = i;
}

module.exports = QRMath;

},
"/QRMode.js":function(module,exports,require){
module.exports = {
    MODE_NUMBER :       1 << 0,
    MODE_ALPHA_NUM :    1 << 1,
    MODE_8BIT_BYTE :    1 << 2,
    MODE_KANJI :        1 << 3
};

},
"/QRPolynomial.js":function(module,exports,require){
var QRMath = require('./QRMath');

function QRPolynomial(num, shift) {
	if (num.length === undefined) {
		throw new Error(num.length + "/" + shift);
	}

	var offset = 0;

	while (offset < num.length && num[offset] === 0) {
		offset++;
	}

	this.num = new Array(num.length - offset + shift);
	for (var i = 0; i < num.length - offset; i++) {
		this.num[i] = num[i + offset];
	}
}

QRPolynomial.prototype = {

	get : function(index) {
		return this.num[index];
	},
	
	getLength : function() {
		return this.num.length;
	},
	
	multiply : function(e) {
	
		var num = new Array(this.getLength() + e.getLength() - 1);
	
		for (var i = 0; i < this.getLength(); i++) {
			for (var j = 0; j < e.getLength(); j++) {
				num[i + j] ^= QRMath.gexp(QRMath.glog(this.get(i) ) + QRMath.glog(e.get(j) ) );
			}
		}
	
		return new QRPolynomial(num, 0);
	},
	
	mod : function(e) {
	
		if (this.getLength() - e.getLength() < 0) {
			return this;
		}
	
		var ratio = QRMath.glog(this.get(0) ) - QRMath.glog(e.get(0) );
	
		var num = new Array(this.getLength() );
		
		for (var i = 0; i < this.getLength(); i++) {
			num[i] = this.get(i);
		}
		
		for (var x = 0; x < e.getLength(); x++) {
			num[x] ^= QRMath.gexp(QRMath.glog(e.get(x) ) + ratio);
		}
	
		// recursive call
		return new QRPolynomial(num, 0).mod(e);
	}
};

module.exports = QRPolynomial;

},
"/QRRSBlock.js":function(module,exports,require){
var QRErrorCorrectLevel = require('./QRErrorCorrectLevel');

function QRRSBlock(totalCount, dataCount) {
	this.totalCount = totalCount;
	this.dataCount  = dataCount;
}

QRRSBlock.RS_BLOCK_TABLE = [

	// L
	// M
	// Q
	// H

	// 1
	[1, 26, 19],
	[1, 26, 16],
	[1, 26, 13],
	[1, 26, 9],
	
	// 2
	[1, 44, 34],
	[1, 44, 28],
	[1, 44, 22],
	[1, 44, 16],

	// 3
	[1, 70, 55],
	[1, 70, 44],
	[2, 35, 17],
	[2, 35, 13],

	// 4		
	[1, 100, 80],
	[2, 50, 32],
	[2, 50, 24],
	[4, 25, 9],
	
	// 5
	[1, 134, 108],
	[2, 67, 43],
	[2, 33, 15, 2, 34, 16],
	[2, 33, 11, 2, 34, 12],
	
	// 6
	[2, 86, 68],
	[4, 43, 27],
	[4, 43, 19],
	[4, 43, 15],
	
	// 7		
	[2, 98, 78],
	[4, 49, 31],
	[2, 32, 14, 4, 33, 15],
	[4, 39, 13, 1, 40, 14],
	
	// 8
	[2, 121, 97],
	[2, 60, 38, 2, 61, 39],
	[4, 40, 18, 2, 41, 19],
	[4, 40, 14, 2, 41, 15],
	
	// 9
	[2, 146, 116],
	[3, 58, 36, 2, 59, 37],
	[4, 36, 16, 4, 37, 17],
	[4, 36, 12, 4, 37, 13],
	
	// 10		
	[2, 86, 68, 2, 87, 69],
	[4, 69, 43, 1, 70, 44],
	[6, 43, 19, 2, 44, 20],
	[6, 43, 15, 2, 44, 16],

	// 11
	[4, 101, 81],
	[1, 80, 50, 4, 81, 51],
	[4, 50, 22, 4, 51, 23],
	[3, 36, 12, 8, 37, 13],

	// 12
	[2, 116, 92, 2, 117, 93],
	[6, 58, 36, 2, 59, 37],
	[4, 46, 20, 6, 47, 21],
	[7, 42, 14, 4, 43, 15],

	// 13
	[4, 133, 107],
	[8, 59, 37, 1, 60, 38],
	[8, 44, 20, 4, 45, 21],
	[12, 33, 11, 4, 34, 12],

	// 14
	[3, 145, 115, 1, 146, 116],
	[4, 64, 40, 5, 65, 41],
	[11, 36, 16, 5, 37, 17],
	[11, 36, 12, 5, 37, 13],

	// 15
	[5, 109, 87, 1, 110, 88],
	[5, 65, 41, 5, 66, 42],
	[5, 54, 24, 7, 55, 25],
	[11, 36, 12],

	// 16
	[5, 122, 98, 1, 123, 99],
	[7, 73, 45, 3, 74, 46],
	[15, 43, 19, 2, 44, 20],
	[3, 45, 15, 13, 46, 16],

	// 17
	[1, 135, 107, 5, 136, 108],
	[10, 74, 46, 1, 75, 47],
	[1, 50, 22, 15, 51, 23],
	[2, 42, 14, 17, 43, 15],

	// 18
	[5, 150, 120, 1, 151, 121],
	[9, 69, 43, 4, 70, 44],
	[17, 50, 22, 1, 51, 23],
	[2, 42, 14, 19, 43, 15],

	// 19
	[3, 141, 113, 4, 142, 114],
	[3, 70, 44, 11, 71, 45],
	[17, 47, 21, 4, 48, 22],
	[9, 39, 13, 16, 40, 14],

	// 20
	[3, 135, 107, 5, 136, 108],
	[3, 67, 41, 13, 68, 42],
	[15, 54, 24, 5, 55, 25],
	[15, 43, 15, 10, 44, 16],

	// 21
	[4, 144, 116, 4, 145, 117],
	[17, 68, 42],
	[17, 50, 22, 6, 51, 23],
	[19, 46, 16, 6, 47, 17],

	// 22
	[2, 139, 111, 7, 140, 112],
	[17, 74, 46],
	[7, 54, 24, 16, 55, 25],
	[34, 37, 13],

	// 23
	[4, 151, 121, 5, 152, 122],
	[4, 75, 47, 14, 76, 48],
	[11, 54, 24, 14, 55, 25],
	[16, 45, 15, 14, 46, 16],

	// 24
	[6, 147, 117, 4, 148, 118],
	[6, 73, 45, 14, 74, 46],
	[11, 54, 24, 16, 55, 25],
	[30, 46, 16, 2, 47, 17],

	// 25
	[8, 132, 106, 4, 133, 107],
	[8, 75, 47, 13, 76, 48],
	[7, 54, 24, 22, 55, 25],
	[22, 45, 15, 13, 46, 16],

	// 26
	[10, 142, 114, 2, 143, 115],
	[19, 74, 46, 4, 75, 47],
	[28, 50, 22, 6, 51, 23],
	[33, 46, 16, 4, 47, 17],

	// 27
	[8, 152, 122, 4, 153, 123],
	[22, 73, 45, 3, 74, 46],
	[8, 53, 23, 26, 54, 24],
	[12, 45, 15, 28, 46, 16],

	// 28
	[3, 147, 117, 10, 148, 118],
	[3, 73, 45, 23, 74, 46],
	[4, 54, 24, 31, 55, 25],
	[11, 45, 15, 31, 46, 16],

	// 29
	[7, 146, 116, 7, 147, 117],
	[21, 73, 45, 7, 74, 46],
	[1, 53, 23, 37, 54, 24],
	[19, 45, 15, 26, 46, 16],

	// 30
	[5, 145, 115, 10, 146, 116],
	[19, 75, 47, 10, 76, 48],
	[15, 54, 24, 25, 55, 25],
	[23, 45, 15, 25, 46, 16],

	// 31
	[13, 145, 115, 3, 146, 116],
	[2, 74, 46, 29, 75, 47],
	[42, 54, 24, 1, 55, 25],
	[23, 45, 15, 28, 46, 16],

	// 32
	[17, 145, 115],
	[10, 74, 46, 23, 75, 47],
	[10, 54, 24, 35, 55, 25],
	[19, 45, 15, 35, 46, 16],

	// 33
	[17, 145, 115, 1, 146, 116],
	[14, 74, 46, 21, 75, 47],
	[29, 54, 24, 19, 55, 25],
	[11, 45, 15, 46, 46, 16],

	// 34
	[13, 145, 115, 6, 146, 116],
	[14, 74, 46, 23, 75, 47],
	[44, 54, 24, 7, 55, 25],
	[59, 46, 16, 1, 47, 17],

	// 35
	[12, 151, 121, 7, 152, 122],
	[12, 75, 47, 26, 76, 48],
	[39, 54, 24, 14, 55, 25],
	[22, 45, 15, 41, 46, 16],

	// 36
	[6, 151, 121, 14, 152, 122],
	[6, 75, 47, 34, 76, 48],
	[46, 54, 24, 10, 55, 25],
	[2, 45, 15, 64, 46, 16],

	// 37
	[17, 152, 122, 4, 153, 123],
	[29, 74, 46, 14, 75, 47],
	[49, 54, 24, 10, 55, 25],
	[24, 45, 15, 46, 46, 16],

	// 38
	[4, 152, 122, 18, 153, 123],
	[13, 74, 46, 32, 75, 47],
	[48, 54, 24, 14, 55, 25],
	[42, 45, 15, 32, 46, 16],

	// 39
	[20, 147, 117, 4, 148, 118],
	[40, 75, 47, 7, 76, 48],
	[43, 54, 24, 22, 55, 25],
	[10, 45, 15, 67, 46, 16],

	// 40
	[19, 148, 118, 6, 149, 119],
	[18, 75, 47, 31, 76, 48],
	[34, 54, 24, 34, 55, 25],
	[20, 45, 15, 61, 46, 16]
];

QRRSBlock.getRSBlocks = function(typeNumber, errorCorrectLevel) {
	
	var rsBlock = QRRSBlock.getRsBlockTable(typeNumber, errorCorrectLevel);
	
	if (rsBlock === undefined) {
		throw new Error("bad rs block @ typeNumber:" + typeNumber + "/errorCorrectLevel:" + errorCorrectLevel);
	}

	var length = rsBlock.length / 3;
	
	var list = [];
	
	for (var i = 0; i < length; i++) {

		var count = rsBlock[i * 3 + 0];
		var totalCount = rsBlock[i * 3 + 1];
		var dataCount  = rsBlock[i * 3 + 2];

		for (var j = 0; j < count; j++) {
			list.push(new QRRSBlock(totalCount, dataCount) );	
		}
	}
	
	return list;
};

QRRSBlock.getRsBlockTable = function(typeNumber, errorCorrectLevel) {

	switch(errorCorrectLevel) {
	case QRErrorCorrectLevel.L :
		return QRRSBlock.RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 0];
	case QRErrorCorrectLevel.M :
		return QRRSBlock.RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 1];
	case QRErrorCorrectLevel.Q :
		return QRRSBlock.RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 2];
	case QRErrorCorrectLevel.H :
		return QRRSBlock.RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 3];
	default :
		return undefined;
	}
};

module.exports = QRRSBlock;

},
"/QRUtil.js":function(module,exports,require){
var QRMode = require('./QRMode');
var QRPolynomial = require('./QRPolynomial');
var QRMath = require('./QRMath');
var QRMaskPattern = require('./QRMaskPattern');

var QRUtil = {

    PATTERN_POSITION_TABLE : [
        [],
        [6, 18],
        [6, 22],
        [6, 26],
        [6, 30],
        [6, 34],
        [6, 22, 38],
        [6, 24, 42],
        [6, 26, 46],
        [6, 28, 50],
        [6, 30, 54],        
        [6, 32, 58],
        [6, 34, 62],
        [6, 26, 46, 66],
        [6, 26, 48, 70],
        [6, 26, 50, 74],
        [6, 30, 54, 78],
        [6, 30, 56, 82],
        [6, 30, 58, 86],
        [6, 34, 62, 90],
        [6, 28, 50, 72, 94],
        [6, 26, 50, 74, 98],
        [6, 30, 54, 78, 102],
        [6, 28, 54, 80, 106],
        [6, 32, 58, 84, 110],
        [6, 30, 58, 86, 114],
        [6, 34, 62, 90, 118],
        [6, 26, 50, 74, 98, 122],
        [6, 30, 54, 78, 102, 126],
        [6, 26, 52, 78, 104, 130],
        [6, 30, 56, 82, 108, 134],
        [6, 34, 60, 86, 112, 138],
        [6, 30, 58, 86, 114, 142],
        [6, 34, 62, 90, 118, 146],
        [6, 30, 54, 78, 102, 126, 150],
        [6, 24, 50, 76, 102, 128, 154],
        [6, 28, 54, 80, 106, 132, 158],
        [6, 32, 58, 84, 110, 136, 162],
        [6, 26, 54, 82, 110, 138, 166],
        [6, 30, 58, 86, 114, 142, 170]
    ],

    G15 : (1 << 10) | (1 << 8) | (1 << 5) | (1 << 4) | (1 << 2) | (1 << 1) | (1 << 0),
    G18 : (1 << 12) | (1 << 11) | (1 << 10) | (1 << 9) | (1 << 8) | (1 << 5) | (1 << 2) | (1 << 0),
    G15_MASK : (1 << 14) | (1 << 12) | (1 << 10)    | (1 << 4) | (1 << 1),

    getBCHTypeInfo : function(data) {
        var d = data << 10;
        while (QRUtil.getBCHDigit(d) - QRUtil.getBCHDigit(QRUtil.G15) >= 0) {
            d ^= (QRUtil.G15 << (QRUtil.getBCHDigit(d) - QRUtil.getBCHDigit(QRUtil.G15) ) );    
        }
        return ( (data << 10) | d) ^ QRUtil.G15_MASK;
    },

    getBCHTypeNumber : function(data) {
        var d = data << 12;
        while (QRUtil.getBCHDigit(d) - QRUtil.getBCHDigit(QRUtil.G18) >= 0) {
            d ^= (QRUtil.G18 << (QRUtil.getBCHDigit(d) - QRUtil.getBCHDigit(QRUtil.G18) ) );    
        }
        return (data << 12) | d;
    },

    getBCHDigit : function(data) {

        var digit = 0;

        while (data !== 0) {
            digit++;
            data >>>= 1;
        }

        return digit;
    },

    getPatternPosition : function(typeNumber) {
        return QRUtil.PATTERN_POSITION_TABLE[typeNumber - 1];
    },

    getMask : function(maskPattern, i, j) {
        
        switch (maskPattern) {
            
        case QRMaskPattern.PATTERN000 : return (i + j) % 2 === 0;
        case QRMaskPattern.PATTERN001 : return i % 2 === 0;
        case QRMaskPattern.PATTERN010 : return j % 3 === 0;
        case QRMaskPattern.PATTERN011 : return (i + j) % 3 === 0;
        case QRMaskPattern.PATTERN100 : return (Math.floor(i / 2) + Math.floor(j / 3) ) % 2 === 0;
        case QRMaskPattern.PATTERN101 : return (i * j) % 2 + (i * j) % 3 === 0;
        case QRMaskPattern.PATTERN110 : return ( (i * j) % 2 + (i * j) % 3) % 2 === 0;
        case QRMaskPattern.PATTERN111 : return ( (i * j) % 3 + (i + j) % 2) % 2 === 0;

        default :
            throw new Error("bad maskPattern:" + maskPattern);
        }
    },

    getErrorCorrectPolynomial : function(errorCorrectLength) {

        var a = new QRPolynomial([1], 0);

        for (var i = 0; i < errorCorrectLength; i++) {
            a = a.multiply(new QRPolynomial([1, QRMath.gexp(i)], 0) );
        }

        return a;
    },

    getLengthInBits : function(mode, type) {

        if (1 <= type && type < 10) {

            // 1 - 9

            switch(mode) {
            case QRMode.MODE_NUMBER     : return 10;
            case QRMode.MODE_ALPHA_NUM  : return 9;
            case QRMode.MODE_8BIT_BYTE  : return 8;
            case QRMode.MODE_KANJI      : return 8;
            default :
                throw new Error("mode:" + mode);
            }

        } else if (type < 27) {

            // 10 - 26

            switch(mode) {
            case QRMode.MODE_NUMBER     : return 12;
            case QRMode.MODE_ALPHA_NUM  : return 11;
            case QRMode.MODE_8BIT_BYTE  : return 16;
            case QRMode.MODE_KANJI      : return 10;
            default :
                throw new Error("mode:" + mode);
            }

        } else if (type < 41) {

            // 27 - 40

            switch(mode) {
            case QRMode.MODE_NUMBER     : return 14;
            case QRMode.MODE_ALPHA_NUM  : return 13;
            case QRMode.MODE_8BIT_BYTE  : return 16;
            case QRMode.MODE_KANJI      : return 12;
            default :
                throw new Error("mode:" + mode);
            }

        } else {
            throw new Error("type:" + type);
        }
    },

    getLostPoint : function(qrCode) {
        
        var moduleCount = qrCode.getModuleCount();
        var lostPoint = 0;
        var row = 0; 
        var col = 0;

        
        // LEVEL1
        
        for (row = 0; row < moduleCount; row++) {

            for (col = 0; col < moduleCount; col++) {

                var sameCount = 0;
                var dark = qrCode.isDark(row, col);

                for (var r = -1; r <= 1; r++) {

                    if (row + r < 0 || moduleCount <= row + r) {
                        continue;
                    }

                    for (var c = -1; c <= 1; c++) {

                        if (col + c < 0 || moduleCount <= col + c) {
                            continue;
                        }

                        if (r === 0 && c === 0) {
                            continue;
                        }

                        if (dark === qrCode.isDark(row + r, col + c) ) {
                            sameCount++;
                        }
                    }
                }

                if (sameCount > 5) {
                    lostPoint += (3 + sameCount - 5);
                }
            }
        }

        // LEVEL2

        for (row = 0; row < moduleCount - 1; row++) {
            for (col = 0; col < moduleCount - 1; col++) {
                var count = 0;
                if (qrCode.isDark(row,     col    ) ) count++;
                if (qrCode.isDark(row + 1, col    ) ) count++;
                if (qrCode.isDark(row,     col + 1) ) count++;
                if (qrCode.isDark(row + 1, col + 1) ) count++;
                if (count === 0 || count === 4) {
                    lostPoint += 3;
                }
            }
        }

        // LEVEL3

        for (row = 0; row < moduleCount; row++) {
            for (col = 0; col < moduleCount - 6; col++) {
                if (qrCode.isDark(row, col) && 
                        !qrCode.isDark(row, col + 1) && 
                         qrCode.isDark(row, col + 2) && 
                         qrCode.isDark(row, col + 3) && 
                         qrCode.isDark(row, col + 4) && 
                        !qrCode.isDark(row, col + 5) && 
                         qrCode.isDark(row, col + 6) ) {
                    lostPoint += 40;
                }
            }
        }

        for (col = 0; col < moduleCount; col++) {
            for (row = 0; row < moduleCount - 6; row++) {
                if (qrCode.isDark(row, col) &&
                        !qrCode.isDark(row + 1, col) &&
                         qrCode.isDark(row + 2, col) &&
                         qrCode.isDark(row + 3, col) &&
                         qrCode.isDark(row + 4, col) &&
                        !qrCode.isDark(row + 5, col) &&
                         qrCode.isDark(row + 6, col) ) {
                    lostPoint += 40;
                }
            }
        }

        // LEVEL4
        
        var darkCount = 0;

        for (col = 0; col < moduleCount; col++) {
            for (row = 0; row < moduleCount; row++) {
                if (qrCode.isDark(row, col) ) {
                    darkCount++;
                }
            }
        }
        
        var ratio = Math.abs(100 * darkCount / moduleCount / moduleCount - 50) / 5;
        lostPoint += ratio * 10;

        return lostPoint;       
    }

};

module.exports = QRUtil;

},
"/index.js":function(module,exports,require){
//---------------------------------------------------------------------
// QRCode for JavaScript
//
// Copyright (c) 2009 Kazuhiko Arase
//
// URL: http://www.d-project.com/
//
// Licensed under the MIT license:
//   http://www.opensource.org/licenses/mit-license.php
//
// The word "QR Code" is registered trademark of 
// DENSO WAVE INCORPORATED
//   http://www.denso-wave.com/qrcode/faqpatent-e.html
//
//---------------------------------------------------------------------
// Modified to work in node for this project (and some refactoring)
//---------------------------------------------------------------------

var QR8bitByte = require('./QR8bitByte');
var QRUtil = require('./QRUtil');
var QRPolynomial = require('./QRPolynomial');
var QRRSBlock = require('./QRRSBlock');
var QRBitBuffer = require('./QRBitBuffer');

function QRCode(typeNumber, errorCorrectLevel) {
	this.typeNumber = typeNumber;
	this.errorCorrectLevel = errorCorrectLevel;
	this.modules = null;
	this.moduleCount = 0;
	this.dataCache = null;
	this.dataList = [];
}

QRCode.prototype = {
	
	addData : function(data) {
		var newData = new QR8bitByte(data);
		this.dataList.push(newData);
		this.dataCache = null;
	},
	
	isDark : function(row, col) {
		if (row < 0 || this.moduleCount <= row || col < 0 || this.moduleCount <= col) {
			throw new Error(row + "," + col);
		}
		return this.modules[row][col];
	},

	getModuleCount : function() {
		return this.moduleCount;
	},
	
	make : function() {
		// Calculate automatically typeNumber if provided is < 1
		if (this.typeNumber < 1 ){
			var typeNumber = 1;
			for (typeNumber = 1; typeNumber < 40; typeNumber++) {
				var rsBlocks = QRRSBlock.getRSBlocks(typeNumber, this.errorCorrectLevel);

				var buffer = new QRBitBuffer();
				var totalDataCount = 0;
				for (var i = 0; i < rsBlocks.length; i++) {
					totalDataCount += rsBlocks[i].dataCount;
				}

				for (var x = 0; x < this.dataList.length; x++) {
					var data = this.dataList[x];
					buffer.put(data.mode, 4);
					buffer.put(data.getLength(), QRUtil.getLengthInBits(data.mode, typeNumber) );
					data.write(buffer);
				}
				if (buffer.getLengthInBits() <= totalDataCount * 8)
					break;
			}
			this.typeNumber = typeNumber;
		}
		this.makeImpl(false, this.getBestMaskPattern() );
	},
	
	makeImpl : function(test, maskPattern) {
		
		this.moduleCount = this.typeNumber * 4 + 17;
		this.modules = new Array(this.moduleCount);
		
		for (var row = 0; row < this.moduleCount; row++) {
			
			this.modules[row] = new Array(this.moduleCount);
			
			for (var col = 0; col < this.moduleCount; col++) {
				this.modules[row][col] = null;//(col + row) % 3;
			}
		}
	
		this.setupPositionProbePattern(0, 0);
		this.setupPositionProbePattern(this.moduleCount - 7, 0);
		this.setupPositionProbePattern(0, this.moduleCount - 7);
		this.setupPositionAdjustPattern();
		this.setupTimingPattern();
		this.setupTypeInfo(test, maskPattern);
		
		if (this.typeNumber >= 7) {
			this.setupTypeNumber(test);
		}
	
		if (this.dataCache === null) {
			this.dataCache = QRCode.createData(this.typeNumber, this.errorCorrectLevel, this.dataList);
		}
	
		this.mapData(this.dataCache, maskPattern);
	},

	setupPositionProbePattern : function(row, col)  {
		
		for (var r = -1; r <= 7; r++) {
			
			if (row + r <= -1 || this.moduleCount <= row + r) continue;
			
			for (var c = -1; c <= 7; c++) {
				
				if (col + c <= -1 || this.moduleCount <= col + c) continue;
				
				if ( (0 <= r && r <= 6 && (c === 0 || c === 6) ) || 
                     (0 <= c && c <= 6 && (r === 0 || r === 6) ) || 
                     (2 <= r && r <= 4 && 2 <= c && c <= 4) ) {
					this.modules[row + r][col + c] = true;
				} else {
					this.modules[row + r][col + c] = false;
				}
			}		
		}		
	},
	
	getBestMaskPattern : function() {
	
		var minLostPoint = 0;
		var pattern = 0;
	
		for (var i = 0; i < 8; i++) {
			
			this.makeImpl(true, i);
	
			var lostPoint = QRUtil.getLostPoint(this);
	
			if (i === 0 || minLostPoint >  lostPoint) {
				minLostPoint = lostPoint;
				pattern = i;
			}
		}
	
		return pattern;
	},
	
	createMovieClip : function(target_mc, instance_name, depth) {
	
		var qr_mc = target_mc.createEmptyMovieClip(instance_name, depth);
		var cs = 1;
	
		this.make();

		for (var row = 0; row < this.modules.length; row++) {
			
			var y = row * cs;
			
			for (var col = 0; col < this.modules[row].length; col++) {
	
				var x = col * cs;
				var dark = this.modules[row][col];
			
				if (dark) {
					qr_mc.beginFill(0, 100);
					qr_mc.moveTo(x, y);
					qr_mc.lineTo(x + cs, y);
					qr_mc.lineTo(x + cs, y + cs);
					qr_mc.lineTo(x, y + cs);
					qr_mc.endFill();
				}
			}
		}
		
		return qr_mc;
	},

	setupTimingPattern : function() {
		
		for (var r = 8; r < this.moduleCount - 8; r++) {
			if (this.modules[r][6] !== null) {
				continue;
			}
			this.modules[r][6] = (r % 2 === 0);
		}
	
		for (var c = 8; c < this.moduleCount - 8; c++) {
			if (this.modules[6][c] !== null) {
				continue;
			}
			this.modules[6][c] = (c % 2 === 0);
		}
	},
	
	setupPositionAdjustPattern : function() {
	
		var pos = QRUtil.getPatternPosition(this.typeNumber);
		
		for (var i = 0; i < pos.length; i++) {
		
			for (var j = 0; j < pos.length; j++) {
			
				var row = pos[i];
				var col = pos[j];
				
				if (this.modules[row][col] !== null) {
					continue;
				}
				
				for (var r = -2; r <= 2; r++) {
				
					for (var c = -2; c <= 2; c++) {
					
						if (Math.abs(r) === 2 || 
                            Math.abs(c) === 2 ||
                            (r === 0 && c === 0) ) {
							this.modules[row + r][col + c] = true;
						} else {
							this.modules[row + r][col + c] = false;
						}
					}
				}
			}
		}
	},
	
	setupTypeNumber : function(test) {
	
		var bits = QRUtil.getBCHTypeNumber(this.typeNumber);
        var mod;
	
		for (var i = 0; i < 18; i++) {
			mod = (!test && ( (bits >> i) & 1) === 1);
			this.modules[Math.floor(i / 3)][i % 3 + this.moduleCount - 8 - 3] = mod;
		}
	
		for (var x = 0; x < 18; x++) {
			mod = (!test && ( (bits >> x) & 1) === 1);
			this.modules[x % 3 + this.moduleCount - 8 - 3][Math.floor(x / 3)] = mod;
		}
	},
	
	setupTypeInfo : function(test, maskPattern) {
	
		var data = (this.errorCorrectLevel << 3) | maskPattern;
		var bits = QRUtil.getBCHTypeInfo(data);
        var mod;
	
		// vertical		
		for (var v = 0; v < 15; v++) {
	
			mod = (!test && ( (bits >> v) & 1) === 1);
	
			if (v < 6) {
				this.modules[v][8] = mod;
			} else if (v < 8) {
				this.modules[v + 1][8] = mod;
			} else {
				this.modules[this.moduleCount - 15 + v][8] = mod;
			}
		}
	
		// horizontal
		for (var h = 0; h < 15; h++) {
	
			mod = (!test && ( (bits >> h) & 1) === 1);
			
			if (h < 8) {
				this.modules[8][this.moduleCount - h - 1] = mod;
			} else if (h < 9) {
				this.modules[8][15 - h - 1 + 1] = mod;
			} else {
				this.modules[8][15 - h - 1] = mod;
			}
		}
	
		// fixed module
		this.modules[this.moduleCount - 8][8] = (!test);
	
	},
	
	mapData : function(data, maskPattern) {
		
		var inc = -1;
		var row = this.moduleCount - 1;
		var bitIndex = 7;
		var byteIndex = 0;
		
		for (var col = this.moduleCount - 1; col > 0; col -= 2) {
	
			if (col === 6) col--;
	
			while (true) {
	
				for (var c = 0; c < 2; c++) {
					
					if (this.modules[row][col - c] === null) {
						
						var dark = false;
	
						if (byteIndex < data.length) {
							dark = ( ( (data[byteIndex] >>> bitIndex) & 1) === 1);
						}
	
						var mask = QRUtil.getMask(maskPattern, row, col - c);
	
						if (mask) {
							dark = !dark;
						}
						
						this.modules[row][col - c] = dark;
						bitIndex--;
	
						if (bitIndex === -1) {
							byteIndex++;
							bitIndex = 7;
						}
					}
				}
								
				row += inc;
	
				if (row < 0 || this.moduleCount <= row) {
					row -= inc;
					inc = -inc;
					break;
				}
			}
		}
		
	}

};

QRCode.PAD0 = 0xEC;
QRCode.PAD1 = 0x11;

QRCode.createData = function(typeNumber, errorCorrectLevel, dataList) {
	
	var rsBlocks = QRRSBlock.getRSBlocks(typeNumber, errorCorrectLevel);
	
	var buffer = new QRBitBuffer();
	
	for (var i = 0; i < dataList.length; i++) {
		var data = dataList[i];
		buffer.put(data.mode, 4);
		buffer.put(data.getLength(), QRUtil.getLengthInBits(data.mode, typeNumber) );
		data.write(buffer);
	}

	// calc num max data.
	var totalDataCount = 0;
	for (var x = 0; x < rsBlocks.length; x++) {
		totalDataCount += rsBlocks[x].dataCount;
	}

	if (buffer.getLengthInBits() > totalDataCount * 8) {
		throw new Error("code length overflow. (" + 
            buffer.getLengthInBits() + 
            ">" +  
            totalDataCount * 8 + 
            ")");
	}

	// end code
	if (buffer.getLengthInBits() + 4 <= totalDataCount * 8) {
		buffer.put(0, 4);
	}

	// padding
	while (buffer.getLengthInBits() % 8 !== 0) {
		buffer.putBit(false);
	}

	// padding
	while (true) {
		
		if (buffer.getLengthInBits() >= totalDataCount * 8) {
			break;
		}
		buffer.put(QRCode.PAD0, 8);
		
		if (buffer.getLengthInBits() >= totalDataCount * 8) {
			break;
		}
		buffer.put(QRCode.PAD1, 8);
	}

	return QRCode.createBytes(buffer, rsBlocks);
};

QRCode.createBytes = function(buffer, rsBlocks) {

	var offset = 0;
	
	var maxDcCount = 0;
	var maxEcCount = 0;
	
	var dcdata = new Array(rsBlocks.length);
	var ecdata = new Array(rsBlocks.length);
	
	for (var r = 0; r < rsBlocks.length; r++) {

		var dcCount = rsBlocks[r].dataCount;
		var ecCount = rsBlocks[r].totalCount - dcCount;

		maxDcCount = Math.max(maxDcCount, dcCount);
		maxEcCount = Math.max(maxEcCount, ecCount);
		
		dcdata[r] = new Array(dcCount);
		
		for (var i = 0; i < dcdata[r].length; i++) {
			dcdata[r][i] = 0xff & buffer.buffer[i + offset];
		}
		offset += dcCount;
		
		var rsPoly = QRUtil.getErrorCorrectPolynomial(ecCount);
		var rawPoly = new QRPolynomial(dcdata[r], rsPoly.getLength() - 1);

		var modPoly = rawPoly.mod(rsPoly);
		ecdata[r] = new Array(rsPoly.getLength() - 1);
		for (var x = 0; x < ecdata[r].length; x++) {
            var modIndex = x + modPoly.getLength() - ecdata[r].length;
			ecdata[r][x] = (modIndex >= 0)? modPoly.get(modIndex) : 0;
		}

	}
	
	var totalCodeCount = 0;
	for (var y = 0; y < rsBlocks.length; y++) {
		totalCodeCount += rsBlocks[y].totalCount;
	}

	var data = new Array(totalCodeCount);
	var index = 0;

	for (var z = 0; z < maxDcCount; z++) {
		for (var s = 0; s < rsBlocks.length; s++) {
			if (z < dcdata[s].length) {
				data[index++] = dcdata[s][z];
			}
		}
	}

	for (var xx = 0; xx < maxEcCount; xx++) {
		for (var t = 0; t < rsBlocks.length; t++) {
			if (xx < ecdata[t].length) {
				data[index++] = ecdata[t][xx];
			}
		}
	}

	return data;

};

module.exports = QRCode;

}};
const __cache={};
function __resolve(id,parent){if(id.startsWith('.')){const base=parent.slice(0,parent.lastIndexOf('/')+1);const parts=(base+id).split('/');const out=[];for(const p of parts){if(!p||p==='.')continue;if(p==='..')out.pop();else out.push(p);}id='/'+out.join('/');}if(!id.endsWith('.js'))id+='.js';return id;}
function __load(id,parent='/index.js'){id=__resolve(id,parent);if(__cache[id])return __cache[id].exports;if(!__mods[id])throw new Error('QR module not found: '+id);const module={exports:{}};__cache[id]=module;__mods[id](module,module.exports,(child)=>__load(child,id));return module.exports;}
const QRModel=__load('/index.js');
const EC=__load('/QRErrorCorrectLevel.js');
function deepMerge(target,source){if(!source)return target;for(const k of Object.keys(source)){const v=source[k];if(v&&typeof v==='object'&&!Array.isArray(v)&&!(v instanceof Blob)){target[k]=deepMerge(target[k]&&typeof target[k]==='object'?target[k]:{},v);}else target[k]=v;}return target;}
function rounded(ctx,x,y,w,h,r){r=Math.max(0,Math.min(r,w/2,h/2));ctx.beginPath();ctx.moveTo(x+r,y);ctx.arcTo(x+w,y,x+w,y+h,r);ctx.arcTo(x+w,y+h,x,y+h,r);ctx.arcTo(x,y+h,x,y,r);ctx.arcTo(x,y,x+w,y,r);ctx.closePath();}
function isFinder(r,c,n){return (r<7&&c<7)||(r<7&&c>=n-7)||(r>=n-7&&c<7);}
function utf8Binary(str){try{return unescape(encodeURIComponent(String(str)));}catch(e){return String(str);}}
function makeMatrix(data,level){const qr=new QRModel(-1,EC[level]??EC.H);qr.addData(utf8Binary(data));qr.make();return qr;}
function shapeCanvas(ctx,type,x,y,s,color,row,col){ctx.fillStyle=color;const gap=0,xx=x,yy=y,ss=s;if(type==='dots'){ctx.beginPath();ctx.arc(x+s/2,y+s/2,ss*.5,0,Math.PI*2);ctx.fill();return;}if(type==='rounded'||type==='extra-rounded'||type==='classy-rounded'){rounded(ctx,xx,yy,ss,ss,type==='extra-rounded'?ss*.2:ss*.12);ctx.fill();return;}if(type==='classy'){const rad=ss*.15;ctx.beginPath();if((row+col)%2){ctx.moveTo(xx+rad,yy);ctx.lineTo(xx+ss,yy);ctx.lineTo(xx+ss,yy+ss-rad);ctx.quadraticCurveTo(xx+ss,yy+ss,xx+ss-rad,yy+ss);ctx.lineTo(xx,yy+ss);ctx.lineTo(xx,yy+rad);ctx.quadraticCurveTo(xx,yy,xx+rad,yy);}else{ctx.moveTo(xx,yy);ctx.lineTo(xx+ss-rad,yy);ctx.quadraticCurveTo(xx+ss,yy,xx+ss,yy+rad);ctx.lineTo(xx+ss,yy+ss);ctx.lineTo(xx+rad,yy+ss);ctx.quadraticCurveTo(xx,yy+ss,xx,yy+ss-rad);ctx.closePath();}ctx.fill();return;}ctx.fillRect(xx,yy,ss,ss);}
function finderCanvas(ctx,x,y,s,outer,inner,type,bg){ctx.fillStyle=outer;rounded(ctx,x,y,s*7,s*7,type==='square'?0:s*.25);ctx.fill();ctx.fillStyle=bg||'#fff';rounded(ctx,x+s,y+s,s*5,s*5,0);ctx.fill();ctx.fillStyle=inner||outer;if(type==='dot'){ctx.beginPath();ctx.arc(x+s*3.5,y+s*3.5,s*1.5,0,Math.PI*2);ctx.fill();}else{rounded(ctx,x+s*2,y+s*2,s*3,s*3,type==='extra-rounded'?s*.45:type==='rounded'?s*.25:0);ctx.fill();}}
function svgShape(type,x,y,s,color){const gap=0,xx=x,yy=y,ss=s;if(type==='dots')return `<circle cx="${x+s/2}" cy="${y+s/2}" r="${ss*.5}" fill="${color}"/>`;const rx=(type==='rounded'||type==='classy-rounded')?ss*.12:type==='extra-rounded'?ss*.2:0;return `<rect x="${xx}" y="${yy}" width="${ss}" height="${ss}" rx="${rx}" fill="${color}"/>`;}
function svgFinder(x,y,s,outer,inner,type,bg){const rx=type==='square'?0:s*.25;const center=type==='extra-rounded'?s*.45:type==='rounded'?s*.25:0;return `<rect x="${x}" y="${y}" width="${s*7}" height="${s*7}" rx="${rx}" fill="${outer}"/><rect x="${x+s}" y="${y+s}" width="${s*5}" height="${s*5}" fill="${bg||'#fff'}"/>${type==='dot'?`<circle cx="${x+s*3.5}" cy="${y+s*3.5}" r="${s*1.5}" fill="${inner||outer}"/>`:`<rect x="${x+s*2}" y="${y+s*2}" width="${s*3}" height="${s*3}" rx="${center}" fill="${inner||outer}"/>`}`;}
class AthanasQRCodeStyling{
 constructor(options={}){this.options=deepMerge({width:300,height:300,type:'canvas',data:'',margin:10,qrOptions:{errorCorrectionLevel:'H'},dotsOptions:{color:'#000',type:'square'},backgroundOptions:{color:'#fff'},cornersSquareOptions:{color:'#000',type:'square'},cornersDotOptions:{color:'#000',type:'square'},image:'',imageOptions:{imageSize:.3,margin:3,hideBackgroundDots:true}},options);this._el=null;this._node=null;this._matrix=null;}
 append(el){this._el=el;this.render();}
 update(options={}){deepMerge(this.options,options);this.render();}
 _buildMatrix(){this._matrix=makeMatrix(this.options.data||' ',this.options.qrOptions?.errorCorrectionLevel||'H');return this._matrix;}
 render(){if(!this._el)return;this._el.innerHTML='';try{this._node=this.options.type==='svg'?this._makeSvgNode():this._makeCanvas();this._el.appendChild(this._node);}catch(err){const msg=document.createElement('div');msg.style.cssText='padding:12px;color:#9b1c16;background:#fff0ef;border-radius:10px;font:700 13px sans-serif';msg.textContent='QR content is too long. Shorten the information and try again.';this._el.appendChild(msg);}}
 _makeCanvas(){const o=this.options,w=Number(o.width)||300,h=Number(o.height)||w,canvas=document.createElement('canvas');canvas.width=w;canvas.height=h;const ctx=canvas.getContext('2d');ctx.fillStyle=o.backgroundOptions?.color||'#fff';ctx.fillRect(0,0,w,h);const qr=this._buildMatrix(),n=qr.getModuleCount(),m=Number(o.margin)||0,s=Math.min((w-2*m)/n,(h-2*m)/n),ox=(w-s*n)/2,oy=(h-s*n)/2;for(let r=0;r<n;r++)for(let c=0;c<n;c++)if(qr.isDark(r,c)&&!isFinder(r,c,n))shapeCanvas(ctx,o.dotsOptions?.type||'square',ox+c*s,oy+r*s,s,o.dotsOptions?.color||'#000',r,c);const outer=o.cornersSquareOptions?.color||o.dotsOptions?.color||'#000',inner=o.cornersDotOptions?.color||outer,ft=o.cornersSquareOptions?.type||'square';const bg=o.backgroundOptions?.color||'#fff';finderCanvas(ctx,ox,oy,s,outer,inner,ft,bg);finderCanvas(ctx,ox+(n-7)*s,oy,s,outer,inner,ft,bg);finderCanvas(ctx,ox,oy+(n-7)*s,s,outer,inner,ft,bg);if(o.image){const img=new Image();img.onload=()=>{const pct=Math.max(.08,Math.min(.42,Number(o.imageOptions?.imageSize)||.3)),size=Math.min(w,h)*pct,x=(w-size)/2,y=(h-size)/2,pad=Number(o.imageOptions?.margin)||0;ctx.fillStyle=o.backgroundOptions?.color||'#fff';rounded(ctx,x-pad,y-pad,size+2*pad,size+2*pad,Math.max(4,size*.08));ctx.fill();ctx.drawImage(img,x,y,size,size);};img.src=o.image;}return canvas;}
 _svgString(){const o=this.options,w=Number(o.width)||300,h=Number(o.height)||w,qr=this._buildMatrix(),n=qr.getModuleCount(),m=Number(o.margin)||0,s=Math.min((w-2*m)/n,(h-2*m)/n),ox=(w-s*n)/2,oy=(h-s*n)/2;let body=`<rect width="${w}" height="${h}" fill="${o.backgroundOptions?.color||'#fff'}"/>`;for(let r=0;r<n;r++)for(let c=0;c<n;c++)if(qr.isDark(r,c)&&!isFinder(r,c,n))body+=svgShape(o.dotsOptions?.type||'square',ox+c*s,oy+r*s,s,o.dotsOptions?.color||'#000');const outer=o.cornersSquareOptions?.color||o.dotsOptions?.color||'#000',inner=o.cornersDotOptions?.color||outer,ft=o.cornersSquareOptions?.type||'square';const bg=o.backgroundOptions?.color||'#fff';body+=svgFinder(ox,oy,s,outer,inner,ft,bg)+svgFinder(ox+(n-7)*s,oy,s,outer,inner,ft,bg)+svgFinder(ox,oy+(n-7)*s,s,outer,inner,ft,bg);if(o.image){const pct=Math.max(.08,Math.min(.42,Number(o.imageOptions?.imageSize)||.3)),size=Math.min(w,h)*pct,x=(w-size)/2,y=(h-size)/2,pad=Number(o.imageOptions?.margin)||0;body+=`<rect x="${x-pad}" y="${y-pad}" width="${size+2*pad}" height="${size+2*pad}" rx="${Math.max(4,size*.08)}" fill="${o.backgroundOptions?.color||'#fff'}"/><image href="${o.image}" x="${x}" y="${y}" width="${size}" height="${size}" preserveAspectRatio="xMidYMid meet"/>`;}return `<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}">${body}</svg>`;}
 _makeSvgNode(){const wrap=document.createElement('div');wrap.innerHTML=this._svgString();return wrap.firstElementChild;}
 async getRawData(ext='png'){ext=String(ext).toLowerCase();if(ext==='svg')return new Blob([this._svgString()],{type:'image/svg+xml'});const canvas=this._makeCanvas();if(this.options.image)await new Promise(r=>setTimeout(r,100));const mime=ext==='jpeg'||ext==='jpg'?'image/jpeg':ext==='webp'?'image/webp':'image/png';return await new Promise(resolve=>canvas.toBlob(resolve,mime,.94));}
 async download({name='qr-code',extension='png'}={}){const blob=await this.getRawData(extension);const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`${name}.${extension==='jpeg'?'jpg':extension}`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);}
}
window.QRCodeStyling=AthanasQRCodeStyling;
window.ATHANAS_QR_ENGINE_VERSION='1.0.0-local';
})();

;
(()=>{
  "use strict";

  const $=(selector,root=document)=>root.querySelector(selector);
  const $$=(selector,root=document)=>Array.from(root.querySelectorAll(selector));
  const SAMPLE_DATA="https://athanasinspires.com";
  const SAMPLE_LABEL="athanasinspires.com";

  const TYPES={
    website:{label:"Website link",short:"Website",icon:"↗",note:"Open a website, lesson, article or online resource."},
    whatsapp:{label:"WhatsApp message",short:"WhatsApp",icon:"◉",note:"Start a WhatsApp conversation with an optional ready-written message."},
    wifi:{label:"Wi-Fi access",short:"Wi-Fi",icon:"⌁",note:"Help visitors connect to a Wi-Fi network without typing the password."},
    text:{label:"Plain text",short:"Text",icon:"T",note:"Display a short note, instruction, code or other plain text."},
    email:{label:"Email message",short:"Email",icon:"@",note:"Open a new email with an address, subject and optional message."},
    phone:{label:"Phone call",short:"Phone",icon:"☎",note:"Make it easy for someone to call a phone number."},
    sms:{label:"SMS message",short:"SMS",icon:"✉",note:"Open a text message to a selected phone number."},
    contact:{label:"Digital contact card",short:"Contact",icon:"＋",note:"Share a person's name, phone number and email as a contact card."},
    location:{label:"Map location",short:"Location",icon:"⌖",note:"Open a precise location using latitude and longitude."},
    social:{label:"Social media profile",short:"Social",icon:"#",note:"Open a social media page, channel or profile."},
    payment:{label:"Payment information",short:"Payment",icon:"$",note:"Open a trusted payment page or payment request link."}
  };
  const LABEL_TO_TYPE=Object.fromEntries(Object.entries(TYPES).map(([key,value])=>[value.label.toLowerCase(),key]));
  const STEPS=["type","content","poster","design","logo","download"];
  const STEP_COPY={
    type:["STEP 1 OF 6","What should your QR code do?","Type to search or open the professional QR type list."],
    content:["STEP 2 OF 6","Add the necessary details","Only the information required for this QR type is shown."],
    poster:["STEP 3 OF 6","Add optional poster text","Use smart suggestions above and below the QR code, or turn either line off."],
    design:["STEP 4 OF 6","Choose a clean QR design","Use strong colours and a clear pattern that remains easy to scan."],
    logo:["STEP 5 OF 6","Add your logo","Upload a logo, then choose a square, rounded or circular shape."],
    download:["STEP 6 OF 6","Save your finished QR code","Download a premium A4 poster or a clean QR-only PNG."]
  };

  const state={
    step:"type",
    type:"website",
    values:{},
    style:{fg:"#06183a",bg:"#ffffff",dots:"rounded",corners:"extra-rounded"},
    logo:{source:"",processed:"",shape:"rounded",size:26},
    poster:{showTop:true,showBottom:true,showWebsite:true,topText:"Scan to open athanasinspires.com",bottomText:"",topDirty:false,bottomDirty:false},
    touched:new Set(),
    visited:new Set(["type"])
  };

  let qrCode=null;
  let renderTimer=0;
  let toastTimer=0;
  let mobileExpanded=false;

  const dom={};

  function init(){
    cacheDom();
    bindNavigation();
    bindTypePicker();
    bindPosterControls();
    bindDesignControls();
    bindLogoControls();
    bindDownloadControls();
    bindHelp();
    bindPreview();
    renderDynamicFields();
    initializeQr();
    setStep("type",false);
    updateEverything();
  }

  function cacheDom(){
    Object.assign(dom,{
      studio:$("#qrStudio"),
      warning:$("#qrLibraryWarning"),
      typeCombobox:$("#qrTypeCombobox"),
      typeSearch:$("#qrTypeSearch"),
      typeToggle:$("#qrTypeToggle"),
      typeList:$("#qrTypeList"),
      typeError:$("#qrTypeError"),
      typeIcon:$("#qrTypeIcon"),
      typeTitle:$("#qrTypeSummaryTitle"),
      typeNote:$("#qrTypeSummaryNote"),
      dynamicFields:$("#qrDynamicFields"),
      stepEyebrow:$("#qrStepEyebrow"),
      stepTitle:$("#qrStepTitle"),
      stepDescription:$("#qrStepDescription"),
      mount:$("#qrCodeMount"),
      posterSheet:$("#qrPosterSheet"),
      posterTop:$("#qrPosterTop"),
      posterBottom:$("#qrPosterBottom"),
      posterWebsite:$("#qrPosterWebsite"),
      showTop:$("#qrShowTop"),
      showBottom:$("#qrShowBottom"),
      showWebsite:$("#qrShowWebsite"),
      topText:$("#qrTopText"),
      bottomText:$("#qrBottomText"),
      topTextField:$("#qrTopTextField"),
      bottomTextField:$("#qrBottomTextField"),
      previewPanel:$("#qrPreviewPanel"),
      previewToggle:$("#qrPreviewToggle"),
      previewStatus:$("#qrPreviewStatus"),
      previewType:$("#qrPreviewType"),
      previewValue:$("#qrPreviewValue"),
      testResult:$("#qrTestResult"),
      checkContent:$("#qrCheckContent"),
      checkContrast:$("#qrCheckContrast"),
      foreground:$("#qrForeground"),
      foregroundHex:$("#qrForegroundHex"),
      background:$("#qrBackground"),
      backgroundHex:$("#qrBackgroundHex"),
      dots:$("#qrDots"),
      corners:$("#qrCorners"),
      contrastNote:$("#qrContrastNote"),
      logoInput:$("#qrLogoInput"),
      logoControls:$("#qrLogoControls"),
      logoImage:$("#qrLogoImage"),
      logoShape:$("#qrLogoShape"),
      logoSize:$("#qrLogoSize"),
      logoSizeOutput:$("#qrLogoSizeOutput"),
      posterFormat:$("#qrPosterFormat"),
      downloadPoster:$("#qrDownloadPoster"),
      downloadQrOnly:$("#qrDownloadQrOnly"),
      downloadSummary:$("#qrDownloadSummary"),
      toast:$("#qrToast"),
      helpBtn:$("#qrHelpBtn"),
      helpMenu:$("#qrHelpMenu")
    });
  }

  function bindNavigation(){
    $$(".qr-step-button").forEach(button=>button.addEventListener("click",()=>setStep(button.dataset.step,true)));
    const openHashStep=()=>{
      const raw=location.hash.replace(/^#/,"");
      const aliases={"qr-type":"type","qr-content":"content","qr-poster":"poster","poster-text":"poster","qr-design":"design","qr-logo":"logo","qr-download":"download"};
      const step=aliases[raw]||raw;
      if(STEPS.includes(step))setStep(step,false);
    };
    window.setTimeout(openHashStep,0);
    window.addEventListener("hashchange",openHashStep);
    $("#qrResetBtn").addEventListener("click",resetBuilder);
  }

  function setStep(step,scroll=true){
    if(!STEPS.includes(step))return;
    state.step=step;
    state.visited.add(step);
    const copy=STEP_COPY[step];
    dom.stepEyebrow.textContent=copy[0];
    dom.stepTitle.textContent=copy[1];
    dom.stepDescription.textContent=copy[2];
    $$(".qr-step-panel").forEach(panel=>panel.classList.toggle("is-active",panel.dataset.panel===step));
    $$(".qr-step-button").forEach(button=>{
      const active=button.dataset.step===step;
      button.classList.toggle("is-active",active);
      if(active)button.setAttribute("aria-current","step");else button.removeAttribute("aria-current");
    });
    updateStepCompletion();
    if(scroll&&window.innerWidth<=760){
      const editor=$(".qr-editor");
      const offset=(dom.previewPanel?.offsetHeight||0)+58;
      const top=editor.getBoundingClientRect().top+window.scrollY-offset;
      window.scrollTo({top:Math.max(0,top),behavior:"smooth"});
    }
  }

  function bindTypePicker(){
    let activeIndex=-1;

    const setExpanded=expanded=>{
      dom.typeList.hidden=!expanded;
      dom.typeSearch.setAttribute("aria-expanded",String(expanded));
      dom.typeToggle.setAttribute("aria-expanded",String(expanded));
      dom.typeCombobox.classList.toggle("is-open",expanded);
      if(!expanded){activeIndex=-1;dom.typeSearch.removeAttribute("aria-activedescendant");}
    };

    const renderOptions=(query="")=>{
      const clean=query.trim().toLowerCase();
      const entries=Object.entries(TYPES).filter(([,type])=>!clean||`${type.label} ${type.short} ${type.note}`.toLowerCase().includes(clean));
      if(!entries.length){
        dom.typeList.innerHTML='<li class="qr-type-empty">No matching QR type. Try “Website” or “WhatsApp”.</li>';
        activeIndex=-1;
        return;
      }
      dom.typeList.innerHTML=entries.map(([key,type],index)=>`<li id="qr-type-option-${key}" role="option" aria-selected="${state.type===key}" data-type-key="${key}" data-option-index="${index}"><span>${escapeHtml(type.icon)}</span><div><strong>${escapeHtml(type.label)}</strong><small>${escapeHtml(type.note)}</small></div><i aria-hidden="true">${state.type===key?"✓":""}</i></li>`).join("");
      activeIndex=-1;
    };

    const highlight=index=>{
      const options=$$("[data-type-key]",dom.typeList);
      if(!options.length)return;
      activeIndex=Math.max(0,Math.min(index,options.length-1));
      options.forEach((option,i)=>option.classList.toggle("is-active",i===activeIndex));
      const active=options[activeIndex];
      dom.typeSearch.setAttribute("aria-activedescendant",active.id);
      active.scrollIntoView({block:"nearest"});
    };

    const selectType=key=>{
      if(!TYPES[key])return;
      const changed=state.type!==key;
      state.type=key;
      dom.typeSearch.value=TYPES[key].label;
      dom.typeError.hidden=true;
      setExpanded(false);
      if(changed){
        state.touched.clear();
        renderDynamicFields();
        dom.testResult.hidden=true;
        state.poster.topDirty=false;
        updateSmartPosterSuggestion(true);
      }
      updateTypeSummary();
      updateEverything();
    };

    dom.typeSearch.addEventListener("focus",()=>{renderOptions("");setExpanded(true);});
    dom.typeSearch.addEventListener("click",()=>{renderOptions(dom.typeSearch.value===TYPES[state.type].label?"":dom.typeSearch.value);setExpanded(true);});
    dom.typeSearch.addEventListener("input",()=>{
      dom.typeError.hidden=true;
      renderOptions(dom.typeSearch.value);
      setExpanded(true);
      const exact=LABEL_TO_TYPE[dom.typeSearch.value.trim().toLowerCase()];
      if(exact)selectType(exact);
    });
    dom.typeSearch.addEventListener("keydown",event=>{
      const open=!dom.typeList.hidden;
      const options=$$("[data-type-key]",dom.typeList);
      if(event.key==="ArrowDown"){
        event.preventDefault();
        if(!open){renderOptions("");setExpanded(true);}
        highlight(activeIndex+1);
      }else if(event.key==="ArrowUp"){
        event.preventDefault();
        if(!open){renderOptions("");setExpanded(true);}
        highlight(activeIndex<=0?options.length-1:activeIndex-1);
      }else if(event.key==="Enter"){
        if(open&&activeIndex>=0&&options[activeIndex]){event.preventDefault();selectType(options[activeIndex].dataset.typeKey);}
        else{
          const exact=LABEL_TO_TYPE[dom.typeSearch.value.trim().toLowerCase()];
          if(exact){event.preventDefault();selectType(exact);}
        }
      }else if(event.key==="Escape"){
        setExpanded(false);
        dom.typeSearch.value=TYPES[state.type].label;
      }
    });
    dom.typeToggle.addEventListener("click",()=>{
      const open=!dom.typeList.hidden;
      if(open)setExpanded(false);
      else{dom.typeSearch.focus();renderOptions("");setExpanded(true);}
    });
    dom.typeList.addEventListener("mousedown",event=>{
      const option=event.target.closest("[data-type-key]");
      if(option){event.preventDefault();selectType(option.dataset.typeKey);}
    });
    dom.typeSearch.addEventListener("blur",()=>window.setTimeout(()=>{
      if(!dom.typeCombobox.contains(document.activeElement)){
        const exact=LABEL_TO_TYPE[dom.typeSearch.value.trim().toLowerCase()];
        if(exact)selectType(exact);
        else{
          dom.typeError.hidden=false;
          dom.typeSearch.value=TYPES[state.type].label;
          setExpanded(false);
        }
      }
    },120));
    document.addEventListener("click",event=>{if(!event.target.closest("#qrTypeCombobox"))setExpanded(false);});
    renderOptions("");
  }

  function updateTypeSummary(){
    const type=TYPES[state.type];
    dom.typeIcon.textContent=type.icon;
    dom.typeTitle.textContent=type.label;
    dom.typeNote.textContent=type.note;
  }

  function field(name,label,options={}){
    const value=state.values[name]??options.value??"";
    const full=options.full!==false?" full":"";
    const required=options.required?" required":"";
    const autocomplete=options.autocomplete?` autocomplete="${options.autocomplete}"`:"";
    const inputmode=options.inputmode?` inputmode="${options.inputmode}"`:"";
    const placeholder=escapeHtml(options.placeholder||"");
    let control="";
    if(options.type==="textarea"){
      control=`<textarea id="qr-${name}" data-value-key="${name}" placeholder="${placeholder}"${required}>${escapeHtml(value)}</textarea>`;
    }else if(options.type==="select"){
      control=`<select id="qr-${name}" data-value-key="${name}"${required}>${options.options.map(([v,t])=>`<option value="${escapeHtml(v)}"${String(value)===String(v)?" selected":""}>${escapeHtml(t)}</option>`).join("")}</select>`;
    }else{
      control=`<input id="qr-${name}" data-value-key="${name}" type="${options.type||"text"}" value="${escapeHtml(value)}" placeholder="${placeholder}"${required}${autocomplete}${inputmode}>`;
    }
    return `<div class="qr-field${full}"><label for="qr-${name}">${escapeHtml(label)}${options.optional?"<em>Optional</em>":""}</label>${control}${options.help?`<small class="qr-field-help">${escapeHtml(options.help)}</small>`:""}<p class="qr-inline-error" data-error-for="${name}" hidden></p></div>`;
  }

  function renderDynamicFields(){
    let html="";
    switch(state.type){
      case "website":
        html=field("url","Website address",{type:"url",required:true,placeholder:"https://example.com",help:"Include https:// at the beginning.",autocomplete:"url"});
        break;
      case "whatsapp":
        html=field("whatsapp","WhatsApp number",{required:true,placeholder:"+255 695 110 859",help:"Include the country code, such as +255.",autocomplete:"tel",inputmode:"tel"})+
             field("whatsappMessage","Ready-written message",{type:"textarea",optional:true,placeholder:"Hello, I would like to know more..."});
        break;
      case "wifi":
        html=field("ssid","Wi-Fi network name",{required:true,placeholder:"Network name"})+
             field("wifiPassword","Wi-Fi password",{optional:true,placeholder:"Password"})+
             field("wifiSecurity","Security type",{type:"select",required:true,full:false,value:"WPA",options:[["WPA","WPA / WPA2"],["WEP","WEP"],["nopass","No password"]]});
        break;
      case "text":
        html=field("text","Text to display",{type:"textarea",required:true,placeholder:"Enter a short message or instruction."});
        break;
      case "email":
        html=field("email","Email address",{type:"email",required:true,placeholder:"name@example.com",autocomplete:"email"})+
             field("subject","Email subject",{optional:true,placeholder:"How can we help?"})+
             field("emailBody","Email message",{type:"textarea",optional:true,placeholder:"Write an optional message."});
        break;
      case "phone":
        html=field("phone","Phone number",{required:true,placeholder:"+255 695 110 859",help:"Include the country code.",autocomplete:"tel",inputmode:"tel"});
        break;
      case "sms":
        html=field("smsPhone","Phone number",{required:true,placeholder:"+255 695 110 859",autocomplete:"tel",inputmode:"tel"})+
             field("smsMessage","Text message",{type:"textarea",optional:true,placeholder:"Enter an optional message."});
        break;
      case "contact":
        html=field("firstName","First name",{required:true,placeholder:"First name",full:false,autocomplete:"given-name"})+
             field("lastName","Last name",{optional:true,placeholder:"Last name",full:false,autocomplete:"family-name"})+
             field("contactPhone","Phone number",{optional:true,placeholder:"+255...",full:false,autocomplete:"tel",inputmode:"tel"})+
             field("contactEmail","Email address",{type:"email",optional:true,placeholder:"name@example.com",full:false,autocomplete:"email"});
        break;
      case "location":
        html=field("latitude","Latitude",{required:true,placeholder:"-3.365",full:false,inputmode:"decimal"})+
             field("longitude","Longitude",{required:true,placeholder:"36.626",full:false,inputmode:"decimal"})+
             field("locationLabel","Location name",{optional:true,placeholder:"School, office or meeting point"});
        break;
      case "social":
        html=field("socialUrl","Profile or channel link",{type:"url",required:true,placeholder:"https://youtube.com/@yourchannel",help:"Paste the complete public profile link.",autocomplete:"url"});
        break;
      case "payment":
        html=field("paymentLink","Trusted payment link",{type:"url",required:true,placeholder:"https://secure-payment-link.example",help:"Confirm the destination carefully before sharing.",autocomplete:"url"})+
             field("paymentLabel","Payment purpose",{optional:true,placeholder:"School fees, donation or order payment"});
        break;
    }
    dom.dynamicFields.innerHTML=`<div class="qr-dynamic-grid">${html}</div>`;
    $$('[data-value-key]',dom.dynamicFields).forEach(input=>{
      input.addEventListener("input",()=>{
        state.values[input.dataset.valueKey]=input.value;
        if(input.value.trim())state.touched.add(input.dataset.valueKey);
        validateFields(false);
        updateSmartPosterSuggestion(false);
        scheduleUpdate();
      });
      input.addEventListener("change",()=>{
        state.values[input.dataset.valueKey]=input.value;
        state.touched.add(input.dataset.valueKey);
        validateFields(false);
        updateSmartPosterSuggestion(false);
        scheduleUpdate();
      });
      input.addEventListener("blur",()=>{
        state.touched.add(input.dataset.valueKey);
        validateFields(false);
      });
    });
    validateFields(false);
  }

  function bindPosterControls(){
    const toggle=(input,field,key)=>{
      input.addEventListener("change",()=>{
        state.poster[key]=input.checked;
        field.classList.toggle("is-disabled",!input.checked);
        field.querySelector("input").disabled=!input.checked;
        scheduleUpdate();
      });
    };
    toggle(dom.showTop,dom.topTextField,"showTop");
    toggle(dom.showBottom,dom.bottomTextField,"showBottom");
    dom.showWebsite.addEventListener("change",()=>{state.poster.showWebsite=dom.showWebsite.checked;scheduleUpdate();});
    dom.topText.addEventListener("input",()=>{
      state.poster.topText=dom.topText.value;
      state.poster.topDirty=true;
      scheduleUpdate();
    });
    dom.bottomText.addEventListener("input",()=>{
      state.poster.bottomText=dom.bottomText.value;
      state.poster.bottomDirty=true;
      scheduleUpdate();
    });
  }

  function updateSmartPosterSuggestion(force=false){
    if(force||!state.poster.topDirty){
      state.poster.topText=getSuggestedTopText();
      if(dom.topText)dom.topText.value=state.poster.topText;
    }
  }

  function getSuggestedTopText(){
    const v=state.values;
    switch(state.type){
      case "website":return `Scan to open ${friendlyHost(v.url)||"athanasinspires.com"}`;
      case "whatsapp":return "Scan to chat with us on WhatsApp";
      case "wifi":return `Scan to connect${v.ssid?` to ${v.ssid}`:" to Wi-Fi"}`;
      case "text":return "Scan to read this message";
      case "email":return "Scan to send an email";
      case "phone":return "Scan to make a phone call";
      case "sms":return "Scan to send a text message";
      case "contact":return "Scan to save this contact";
      case "location":return `Scan to open${v.locationLabel?` ${v.locationLabel}`:" this location"}`;
      case "social":return "Scan to view this profile";
      case "payment":return "Scan to open secure payment details";
      default:return "Scan this QR code";
    }
  }

  function friendlyHost(value){
    try{return new URL(String(value||"")).hostname.replace(/^www\./,"");}
    catch(error){return "";}
  }

  function bindDesignControls(){
    bindColourPair(dom.foreground,dom.foregroundHex,"fg");
    bindColourPair(dom.background,dom.backgroundHex,"bg");
    dom.dots.addEventListener("change",()=>{state.style.dots=dom.dots.value;scheduleUpdate();});
    dom.corners.addEventListener("change",()=>{state.style.corners=dom.corners.value;scheduleUpdate();});
  }

  function bindColourPair(picker,text,key){
    picker.addEventListener("input",()=>{
      state.style[key]=picker.value.toLowerCase();
      text.value=picker.value.toUpperCase();
      scheduleUpdate();
    });
    text.addEventListener("input",()=>{
      const value=normaliseHex(text.value);
      if(value){
        state.style[key]=value.toLowerCase();
        picker.value=value;
        scheduleUpdate();
      }
    });
    text.addEventListener("blur",()=>{text.value=state.style[key].toUpperCase();});
  }

  function bindLogoControls(){
    dom.logoInput.addEventListener("change",handleLogoUpload);
    dom.logoShape.addEventListener("change",async()=>{
      state.logo.shape=dom.logoShape.value;
      if(state.logo.source)await processLogo();
    });
    dom.logoSize.addEventListener("input",()=>{
      state.logo.size=Number(dom.logoSize.value);
      dom.logoSizeOutput.textContent=`${state.logo.size}%`;
      scheduleUpdate();
    });
    $("#qrRemoveLogo").addEventListener("click",removeLogo);
  }

  async function handleLogoUpload(event){
    const file=event.target.files?.[0];
    if(!file)return;
    if(!file.type.startsWith("image/")){toast("Please choose an image file.","error");return;}
    if(file.size>2.5*1024*1024){toast("Choose a logo smaller than 2.5 MB.","warning");return;}
    try{
      state.logo.source=await readFileAsDataUrl(file);
      await processLogo();
      dom.logoControls.hidden=false;
      toast("Logo added to your QR code.","success");
    }catch(error){toast("The logo could not be processed.","error");}
  }

  async function processLogo(){
    const image=await loadImage(state.logo.source);
    const size=600;
    const canvas=document.createElement("canvas");
    canvas.width=size;canvas.height=size;
    const ctx=canvas.getContext("2d");
    ctx.save();
    if(state.logo.shape==="circle"){
      ctx.beginPath();ctx.arc(size/2,size/2,size/2,0,Math.PI*2);ctx.clip();
    }else if(state.logo.shape==="rounded"){
      roundedPath(ctx,0,0,size,size,105);ctx.clip();
    }
    ctx.fillStyle="#ffffff";ctx.fillRect(0,0,size,size);
    const padding=46;
    const available=size-padding*2;
    const ratio=Math.min(available/image.width,available/image.height);
    const width=image.width*ratio,height=image.height*ratio;
    ctx.drawImage(image,(size-width)/2,(size-height)/2,width,height);
    ctx.restore();
    state.logo.processed=canvas.toDataURL("image/png");
    dom.logoImage.src=state.logo.processed;
    scheduleUpdate();
  }

  function removeLogo(){
    state.logo.source="";state.logo.processed="";
    dom.logoInput.value="";
    dom.logoImage.removeAttribute("src");
    dom.logoControls.hidden=true;
    scheduleUpdate();
    toast("Logo removed.","success");
  }

  function bindDownloadControls(){
    dom.posterFormat.addEventListener("change",updateDownloadText);
    dom.downloadPoster.addEventListener("click",()=>downloadPoster(dom.posterFormat.value));
    dom.downloadQrOnly.addEventListener("click",downloadQrOnly);
    $("#qrQuickDownload").addEventListener("click",()=>downloadPoster("pdf"));
  }

  function updateDownloadText(){
    const format=dom.posterFormat.value.toUpperCase();
    dom.downloadPoster.innerHTML=`<span aria-hidden="true">↓</span> Download A4 Poster ${format}`;
    dom.downloadSummary.textContent=`A4 portrait poster · ${format==="PDF"?"recommended for professional printing":"high-resolution image for digital sharing"}.`;
  }

  function bindHelp(){
    dom.helpBtn.addEventListener("click",event=>{
      event.stopPropagation();
      const open=dom.helpMenu.hidden;
      dom.helpMenu.hidden=!open;
      dom.helpBtn.setAttribute("aria-expanded",String(open));
    });
    $$('[data-help-step]').forEach(button=>button.addEventListener("click",()=>{
      dom.helpMenu.hidden=true;dom.helpBtn.setAttribute("aria-expanded","false");setStep(button.dataset.helpStep,true);
    }));
    document.addEventListener("click",event=>{
      if(!event.target.closest(".qr-help-wrap")){dom.helpMenu.hidden=true;dom.helpBtn.setAttribute("aria-expanded","false");}
    });
  }

  function bindPreview(){
    $("#qrTestBtn").addEventListener("click",testGeneratedCode);
    dom.previewToggle.addEventListener("click",()=>{
      mobileExpanded=!mobileExpanded;
      dom.previewPanel.classList.toggle("is-expanded",mobileExpanded);
      dom.previewPanel.classList.toggle("is-compact",!mobileExpanded);
      dom.previewToggle.setAttribute("aria-expanded",String(mobileExpanded));
    });
    const syncMobilePreview=()=>{
      if(window.innerWidth>760){
        dom.previewPanel.classList.remove("is-compact","is-expanded");
        dom.previewToggle.setAttribute("aria-expanded","true");
        return;
      }
      const threshold=(dom.studio?.offsetTop||0)+100;
      if(window.scrollY>threshold&&!mobileExpanded)dom.previewPanel.classList.add("is-compact");
      else if(!mobileExpanded)dom.previewPanel.classList.remove("is-compact");
    };
    window.addEventListener("scroll",syncMobilePreview,{passive:true});
    window.addEventListener("resize",syncMobilePreview);
    syncMobilePreview();
  }

  function initializeQr(){
    if(typeof window.QRCodeStyling!=="function"){
      dom.warning.textContent="The QR engine could not load. Check your internet connection, then refresh this page.";
      dom.warning.classList.add("is-visible");
      return;
    }
    dom.warning.classList.remove("is-visible");
    qrCode=new window.QRCodeStyling(getQrOptions(SAMPLE_DATA));
    dom.mount.innerHTML="";
    qrCode.append(dom.mount);
  }

  function scheduleUpdate(){
    clearTimeout(renderTimer);
    renderTimer=setTimeout(updateEverything,80);
  }

  function updateEverything(){
    updateTypeSummary();
    validateFields(false);
    updateContrast();
    updatePreview();
    updatePosterPreview();
    updateStepCompletion();
    updateDownloadText();
  }

  function updatePreview(){
    const valid=isContentValid();
    const data=valid?buildData():SAMPLE_DATA;
    const contrastGood=getContrast(state.style.fg,state.style.bg)>=3.5;
    if(qrCode){
      try{qrCode.update(getQrOptions(data));}
      catch(error){toast("The QR preview could not update. Try shorter content.","error");}
    }
    const type=TYPES[state.type];
    dom.previewType.textContent=type.label.toUpperCase();
    dom.previewValue.textContent=valid?getPreviewValue():SAMPLE_LABEL;
    dom.previewStatus.classList.toggle("is-waiting",!valid||!contrastGood);
    dom.previewStatus.innerHTML=`<i aria-hidden="true"></i> ${valid?(contrastGood?"Ready to scan":"Improve contrast"):"Add details"}`;
    dom.checkContent.className=valid?"":"is-waiting";
    dom.checkContent.innerHTML=`<span>${valid?"✓":"○"}</span> ${valid?"Required details complete":"Required details"}`;
    dom.testResult.hidden=true;
  }

  function updatePosterPreview(){
    dom.posterTop.textContent=state.poster.topText||"";
    dom.posterBottom.textContent=state.poster.bottomText||"";
    dom.posterTop.hidden=!state.poster.showTop||!state.poster.topText.trim();
    dom.posterBottom.hidden=!state.poster.showBottom||!state.poster.bottomText.trim();
    dom.posterWebsite.hidden=!state.poster.showWebsite;
    dom.posterSheet.classList.toggle("without-top",dom.posterTop.hidden);
    dom.posterSheet.classList.toggle("without-bottom",dom.posterBottom.hidden);
    dom.posterSheet.classList.toggle("without-website",dom.posterWebsite.hidden);
  }

  function getQrOptions(data){
    const safeCorner=getContrast("#f6c928",state.style.bg)>=3.5?"#f6c928":state.style.fg;
    return {
      width:900,
      height:900,
      type:"canvas",
      data:data||SAMPLE_DATA,
      margin:28,
      qrOptions:{errorCorrectionLevel:state.logo.processed?"H":"Q"},
      dotsOptions:{color:state.style.fg,type:state.style.dots},
      backgroundOptions:{color:state.style.bg},
      cornersSquareOptions:{color:state.style.fg,type:state.style.corners},
      cornersDotOptions:{color:safeCorner,type:state.style.corners==="square"?"square":"dot"},
      image:state.logo.processed||undefined,
      imageOptions:{hideBackgroundDots:true,imageSize:Math.min(.36,state.logo.size/100),margin:6,crossOrigin:"anonymous",saveAsBlob:true}
    };
  }

  function buildData(){
    const v=state.values;
    switch(state.type){
      case "website":return (v.url||"").trim();
      case "whatsapp":{
        const phone=cleanPhone(v.whatsapp||"");
        return phone?`https://wa.me/${phone}${v.whatsappMessage?`?text=${encodeURIComponent(v.whatsappMessage)}`:""}`:"";
      }
      case "wifi":return v.ssid?`WIFI:T:${wifiEscape(v.wifiSecurity||"WPA")};S:${wifiEscape(v.ssid)};P:${wifiEscape(v.wifiPassword||"")};;`:"";
      case "text":return (v.text||"").trim();
      case "email":return v.email?`mailto:${v.email.trim()}?subject=${encodeURIComponent(v.subject||"")}&body=${encodeURIComponent(v.emailBody||"")}`:"";
      case "phone":return v.phone?`tel:${v.phone.replace(/\s+/g,"")}`:"";
      case "sms":return v.smsPhone?`SMSTO:${v.smsPhone.replace(/\s+/g,"")}:${v.smsMessage||""}`:"";
      case "contact":return v.firstName?["BEGIN:VCARD","VERSION:3.0",`N:${v.lastName||""};${v.firstName};;;`,`FN:${[v.firstName,v.lastName].filter(Boolean).join(" ")}`,v.contactPhone?`TEL;TYPE=CELL:${v.contactPhone}`:"",v.contactEmail?`EMAIL:${v.contactEmail}`:"","END:VCARD"].filter(Boolean).join("\n"):"";
      case "location":return v.latitude&&v.longitude?`geo:${v.latitude},${v.longitude}${v.locationLabel?`?q=${v.latitude},${v.longitude}(${encodeURIComponent(v.locationLabel)})`:""}`:"";
      case "social":return (v.socialUrl||"").trim();
      case "payment":return (v.paymentLink||"").trim();
      default:return "";
    }
  }

  function getPreviewValue(){
    const v=state.values;
    const values={
      website:v.url,
      whatsapp:v.whatsapp,
      wifi:v.ssid,
      text:v.text,
      email:v.email,
      phone:v.phone,
      sms:v.smsPhone,
      contact:[v.firstName,v.lastName].filter(Boolean).join(" "),
      location:v.locationLabel||`${v.latitude}, ${v.longitude}`,
      social:v.socialUrl,
      payment:v.paymentLabel||v.paymentLink
    };
    const value=String(values[state.type]||SAMPLE_LABEL).replace(/\s+/g," ").trim();
    return value.length>64?`${value.slice(0,61)}…`:value;
  }

  function getValidationErrors(){
    const v=state.values;
    const errors={};
    const required=(key,message)=>{if(!String(v[key]||"").trim())errors[key]=message;};
    const validUrl=value=>{try{const u=new URL(String(value));return ["http:","https:"].includes(u.protocol)&&Boolean(u.hostname);}catch(e){return false;}};
    const validPhone=value=>/^\+?[0-9][0-9\s()\-]{6,}$/.test(String(value||"").trim());
    switch(state.type){
      case "website":required("url","Enter a website address.");if(v.url&&!validUrl(v.url))errors.url="Enter a complete address beginning with https://";break;
      case "whatsapp":required("whatsapp","Enter a WhatsApp number.");if(v.whatsapp&&!validPhone(v.whatsapp))errors.whatsapp="Enter a valid number with the country code, such as +255.";break;
      case "wifi":required("ssid","Enter the Wi-Fi network name.");break;
      case "text":required("text","Enter the text you want the QR code to display.");break;
      case "email":required("email","Enter an email address.");if(v.email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v.email))errors.email="Enter a valid email address.";break;
      case "phone":required("phone","Enter a phone number.");if(v.phone&&!validPhone(v.phone))errors.phone="Enter a valid phone number with the country code.";break;
      case "sms":required("smsPhone","Enter a phone number.");if(v.smsPhone&&!validPhone(v.smsPhone))errors.smsPhone="Enter a valid phone number with the country code.";break;
      case "contact":required("firstName","Enter the contact's first name.");if(v.contactEmail&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v.contactEmail))errors.contactEmail="Enter a valid email address.";break;
      case "location":required("latitude","Enter the latitude.");required("longitude","Enter the longitude.");if(v.latitude&&(isNaN(Number(v.latitude))||Number(v.latitude)<-90||Number(v.latitude)>90))errors.latitude="Latitude must be between -90 and 90.";if(v.longitude&&(isNaN(Number(v.longitude))||Number(v.longitude)<-180||Number(v.longitude)>180))errors.longitude="Longitude must be between -180 and 180.";break;
      case "social":required("socialUrl","Enter the social profile link.");if(v.socialUrl&&!validUrl(v.socialUrl))errors.socialUrl="Enter a complete link beginning with https://";break;
      case "payment":required("paymentLink","Enter the trusted payment link.");if(v.paymentLink&&!validUrl(v.paymentLink))errors.paymentLink="Enter a complete secure link beginning with https://";break;
    }
    return errors;
  }

  function validateFields(showAll){
    const errors=getValidationErrors();
    $$('[data-error-for]',dom.dynamicFields).forEach(errorNode=>{
      const key=errorNode.dataset.errorFor;
      const message=errors[key];
      const shouldShow=Boolean(message)&&(showAll||state.touched.has(key));
      errorNode.textContent=message||"";
      errorNode.hidden=!shouldShow;
      const input=$(`[data-value-key="${key}"]`,dom.dynamicFields);
      if(input)input.setAttribute("aria-invalid",shouldShow?"true":"false");
    });
    return Object.keys(errors).length===0;
  }

  function isContentValid(){return Object.keys(getValidationErrors()).length===0&&Boolean(buildData());}

  function updateStepCompletion(){
    const completion={type:true,content:isContentValid(),poster:state.visited.has("poster"),design:state.visited.has("design"),logo:state.visited.has("logo"),download:false};
    $$(".qr-step-button").forEach(button=>button.classList.toggle("is-complete",Boolean(completion[button.dataset.step])));
  }

  function updateContrast(){
    const ratio=getContrast(state.style.fg,state.style.bg);
    const good=ratio>=3.5;
    dom.contrastNote.classList.toggle("is-warning",!good);
    dom.contrastNote.innerHTML=good?`<span aria-hidden="true">✓</span><div><strong>Strong scan contrast</strong><small>Your colour combination is clear and scan-friendly.</small></div>`:`<span aria-hidden="true">!</span><div><strong>Contrast is too low</strong><small>Choose a darker QR colour or a lighter background.</small></div>`;
    dom.checkContrast.className=good?"":"is-danger";
    dom.checkContrast.innerHTML=`<span>${good?"✓":"!"}</span> ${good?"Strong colour contrast":"Improve colour contrast"}`;
  }

  async function testGeneratedCode(){
    if(!isContentValid()){
      state.touched=new Set(Object.keys(state.values));
      validateFields(true);
      setStep("content",true);
      toast("Complete the required details before testing.","warning");
      return;
    }
    const data=buildData();
    const canvas=$("canvas",dom.mount);
    let decoded="";
    if(canvas&&typeof window.jsQR==="function"){
      try{
        const ctx=canvas.getContext("2d",{willReadFrequently:true});
        const image=ctx.getImageData(0,0,canvas.width,canvas.height);
        decoded=window.jsQR(image.data,image.width,image.height,{inversionAttempts:"attemptBoth"})?.data||"";
      }catch(error){}
    }
    const result=decoded||data;
    const openable=/^(https?:|mailto:|tel:|sms:|geo:)/i.test(result);
    dom.testResult.innerHTML=`<strong>${decoded?"Scan test passed":"QR content is ready"}</strong>${escapeHtml(result)}${openable?`<br><a href="${escapeHtml(result)}" target="_blank" rel="noopener noreferrer">Open the destination safely ↗</a>`:""}`;
    dom.testResult.hidden=false;
    if(window.innerWidth<=760){mobileExpanded=true;dom.previewPanel.classList.add("is-expanded");dom.previewPanel.classList.remove("is-compact");}
    toast(decoded?"The QR code was decoded successfully.":"The QR content is ready. Test it with a phone before printing.",decoded?"success":"warning");
  }

  async function downloadQrOnly(){
    if(!ensureDownloadReady())return;
    if(typeof window.QRCodeStyling!=="function"){toast("The QR engine is unavailable.","error");return;}
    try{
      const temp=new window.QRCodeStyling({...getQrOptions(buildData()),width:1600,height:1600,type:"canvas"});
      const blob=await temp.getRawData("png");
      blobDownload(blob,`${safeFileName(TYPES[state.type].short)}-qr-code.png`);
      toast("QR-only PNG downloaded successfully.","success");
    }catch(error){
      console.error(error);
      toast("The QR code could not be prepared. Please try again.","error");
    }
  }

  async function downloadPoster(format="pdf"){
    if(!ensureDownloadReady())return;
    try{
      const canvas=await createPosterCanvas();
      const name=`${safeFileName(TYPES[state.type].short)}-qr-poster-a4`;
      if(format==="png"){
        const blob=await canvasToBlob(canvas,"image/png",1);
        blobDownload(blob,`${name}.png`);
      }else{
        blobDownload(buildPdfBlob(canvas,595.28,841.89),`${name}.pdf`);
      }
      toast(`Premium A4 poster ${format.toUpperCase()} downloaded successfully.`,"success");
    }catch(error){
      console.error(error);
      toast("The premium poster could not be prepared. Please try again.","error");
    }
  }

  function ensureDownloadReady(){
    if(!isContentValid()){
      state.touched=new Set(Object.keys(state.values));
      validateFields(true);
      setStep("content",true);
      toast("Complete the required details before downloading.","warning");
      return false;
    }
    return true;
  }

  async function createPosterCanvas(){
    if(typeof window.QRCodeStyling!=="function")throw new Error("QR engine unavailable");
    const canvas=document.createElement("canvas");
    canvas.width=2480;
    canvas.height=3508;
    const ctx=canvas.getContext("2d");
    ctx.fillStyle="#ffffff";
    ctx.fillRect(0,0,canvas.width,canvas.height);

    const panel={x:440,y:604,w:1600,h:2300,r:72};
    ctx.save();
    ctx.shadowColor="rgba(6,24,58,.16)";
    ctx.shadowBlur=72;
    ctx.shadowOffsetY=28;
    roundedPath(ctx,panel.x,panel.y,panel.w,panel.h,panel.r);
    ctx.fillStyle="#ffffff";
    ctx.fill();
    ctx.restore();
    roundedPath(ctx,panel.x,panel.y,panel.w,panel.h,panel.r);
    ctx.strokeStyle="#dbe2ec";
    ctx.lineWidth=4;
    ctx.stroke();

    const accentW=230;
    ctx.fillStyle="#f6c928";
    roundedPath(ctx,panel.x+(panel.w-accentW)/2,panel.y+76,accentW,14,7);
    ctx.fill();

    const qrSize=1160;
    const topSpace=state.poster.showTop&&state.poster.topText.trim()?330:170;
    const qrX=panel.x+(panel.w-qrSize)/2;
    const qrY=panel.y+topSpace;

    if(state.poster.showTop&&state.poster.topText.trim()){
      ctx.fillStyle="#06183a";
      ctx.font="700 72px Arial, Helvetica, sans-serif";
      ctx.textAlign="center";
      ctx.textBaseline="middle";
      drawWrappedText(ctx,state.poster.topText,panel.x+panel.w/2,panel.y+205,panel.w-240,90,2);
    }

    const temp=new window.QRCodeStyling({...getQrOptions(buildData()),width:qrSize,height:qrSize,type:"canvas",margin:48});
    const qrBlob=await temp.getRawData("png");
    const qrImage=await blobToImage(qrBlob);
    ctx.drawImage(qrImage,qrX,qrY,qrSize,qrSize);

    let footerY=qrY+qrSize+150;
    if(state.poster.showBottom&&state.poster.bottomText.trim()){
      ctx.fillStyle="#06183a";
      ctx.font="600 58px Arial, Helvetica, sans-serif";
      ctx.textAlign="center";
      ctx.textBaseline="middle";
      drawWrappedText(ctx,state.poster.bottomText,panel.x+panel.w/2,footerY,panel.w-240,74,2);
      footerY+=175;
    }
    if(state.poster.showWebsite){
      ctx.fillStyle="#68758a";
      ctx.font="700 38px Arial, Helvetica, sans-serif";
      ctx.textAlign="center";
      ctx.fillText("athanasinspires.com",panel.x+panel.w/2,Math.min(panel.y+panel.h-120,footerY));
    }
    return canvas;
  }

  function drawWrappedText(ctx,text,x,y,maxWidth,lineHeight,maxLines=2){
    const words=String(text).trim().split(/\s+/);
    const lines=[];
    let line="";
    for(const word of words){
      const test=line?`${line} ${word}`:word;
      if(ctx.measureText(test).width>maxWidth&&line){lines.push(line);line=word;}
      else line=test;
    }
    if(line)lines.push(line);
    const visible=lines.slice(0,maxLines);
    if(lines.length>maxLines){
      let last=visible[maxLines-1];
      while(last&&ctx.measureText(`${last}…`).width>maxWidth)last=last.slice(0,-1);
      visible[maxLines-1]=`${last}…`;
    }
    const startY=y-((visible.length-1)*lineHeight)/2;
    visible.forEach((value,index)=>ctx.fillText(value,x,startY+index*lineHeight));
  }

  function resetBuilder(){
    state.step="type";
    state.type="website";
    state.values={};
    state.style={fg:"#06183a",bg:"#ffffff",dots:"rounded",corners:"extra-rounded"};
    state.logo={source:"",processed:"",shape:"rounded",size:26};
    state.poster={showTop:true,showBottom:true,showWebsite:true,topText:"Scan to open athanasinspires.com",bottomText:"",topDirty:false,bottomDirty:false};
    state.touched.clear();
    state.visited=new Set(["type"]);
    dom.foreground.value=state.style.fg;dom.foregroundHex.value=state.style.fg.toUpperCase();
    dom.background.value=state.style.bg;dom.backgroundHex.value=state.style.bg.toUpperCase();
    dom.dots.value=state.style.dots;dom.corners.value=state.style.corners;
    dom.logoInput.value="";dom.logoControls.hidden=true;dom.logoImage.removeAttribute("src");
    dom.logoShape.value="rounded";dom.logoSize.value="26";dom.logoSizeOutput.textContent="26%";
    dom.typeSearch.value=TYPES.website.label;
    dom.showTop.checked=true;dom.showBottom.checked=true;dom.showWebsite.checked=true;
    dom.topText.value=state.poster.topText;dom.bottomText.value=state.poster.bottomText;
    dom.topText.disabled=false;dom.bottomText.disabled=false;
    dom.topTextField.classList.remove("is-disabled");dom.bottomTextField.classList.remove("is-disabled");
    dom.posterFormat.value="pdf";
    dom.testResult.hidden=true;
    updateTypeSummary();renderDynamicFields();setStep("type",false);updateEverything();
    toast("The QR builder has been reset.","success");
  }

  function cleanPhone(value){return String(value).replace(/\D/g,"");}
  function wifiEscape(value){return String(value).replace(/([\\;,:"])/g,"\\$1");}
  function normaliseHex(value){const clean=String(value).trim();return /^#[0-9a-f]{6}$/i.test(clean)?clean.toLowerCase():"";}
  function escapeHtml(value=""){return String(value).replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[char]));}
  function safeFileName(value="qr-code"){return String(value).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"qr-code";}
  function readFileAsDataUrl(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.onerror=reject;reader.readAsDataURL(file);});}
  function loadImage(src){return new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=reject;image.src=src;});}
  function roundedPath(ctx,x,y,w,h,r){ctx.beginPath();ctx.moveTo(x+r,y);ctx.arcTo(x+w,y,x+w,y+h,r);ctx.arcTo(x+w,y+h,x,y+h,r);ctx.arcTo(x,y+h,x,y,r);ctx.arcTo(x,y,x+w,y,r);ctx.closePath();}

  function hexToRgb(hex){let value=hex.replace("#","");if(value.length===3)value=value.split("").map(c=>c+c).join("");const n=parseInt(value,16);return {r:(n>>16)&255,g:(n>>8)&255,b:n&255};}
  function luminance(hex){const {r,g,b}=hexToRgb(hex);const values=[r,g,b].map(v=>{v/=255;return v<=.03928?v/12.92:Math.pow((v+.055)/1.055,2.4);});return .2126*values[0]+.7152*values[1]+.0722*values[2];}
  function getContrast(a,b){const l1=luminance(a),l2=luminance(b);return (Math.max(l1,l2)+.05)/(Math.min(l1,l2)+.05);}

  function blobDownload(blob,name){const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download=name;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);}
  async function blobToCanvas(blob){const image=await blobToImage(blob);const canvas=document.createElement("canvas");canvas.width=image.naturalWidth||1200;canvas.height=image.naturalHeight||1200;const ctx=canvas.getContext("2d");ctx.fillStyle="#ffffff";ctx.fillRect(0,0,canvas.width,canvas.height);ctx.drawImage(image,0,0);return canvas;}
  async function blobToImage(blob){const url=URL.createObjectURL(blob);try{return await loadImage(url);}finally{window.setTimeout(()=>URL.revokeObjectURL(url),0);}}
  function canvasToBlob(canvas,type,quality){return new Promise((resolve,reject)=>canvas.toBlob(blob=>blob?resolve(blob):reject(new Error("Canvas export failed")),type,quality));}
  function dataUrlBytes(dataUrl){const binary=atob(dataUrl.split(",")[1]||"");const output=new Uint8Array(binary.length);for(let i=0;i<binary.length;i++)output[i]=binary.charCodeAt(i);return output;}
  function textBytes(value){return new TextEncoder().encode(String(value));}
  function joinBytes(parts){const size=parts.reduce((total,part)=>total+part.length,0);const output=new Uint8Array(size);let offset=0;parts.forEach(part=>{output.set(part,offset);offset+=part.length;});return output;}
  function buildPdfBlob(canvas,pageWidth=595.28,pageHeight=841.89){
    const header=new Uint8Array([37,80,68,70,45,49,46,52,10,37,226,227,207,211,10]);
    const jpeg=dataUrlBytes(canvas.toDataURL("image/jpeg",.95));
    const content=`q
${pageWidth} 0 0 ${pageHeight} 0 0 cm
/Im0 Do
Q
`;
    const objects=[];
    objects[1]=textBytes("1 0 obj\n<< /Type /Catalog /Pages 2 0 R >>\nendobj\n");
    objects[2]=textBytes("2 0 obj\n<< /Type /Pages /Kids [3 0 R] /Count 1 >>\nendobj\n");
    objects[3]=textBytes(`3 0 obj\n<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageWidth} ${pageHeight}] /Resources << /XObject << /Im0 4 0 R >> >> /Contents 5 0 R >>\nendobj\n`);
    objects[4]=joinBytes([textBytes(`4 0 obj\n<< /Type /XObject /Subtype /Image /Width ${canvas.width} /Height ${canvas.height} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${jpeg.length} >>\nstream\n`),jpeg,textBytes("\nendstream\nendobj\n")]);
    objects[5]=textBytes(`5 0 obj\n<< /Length ${textBytes(content).length} >>\nstream\n${content}endstream\nendobj\n`);
    const parts=[header],offsets=[0];let cursor=header.length;
    for(let i=1;i<=5;i++){offsets[i]=cursor;parts.push(objects[i]);cursor+=objects[i].length;}
    const xrefOffset=cursor;
    let xref="xref\n0 6\n0000000000 65535 f \n";
    for(let i=1;i<=5;i++)xref+=`${String(offsets[i]).padStart(10,"0")} 00000 n \n`;
    xref+=`trailer\n<< /Size 6 /Root 1 0 R >>\nstartxref\n${xrefOffset}\n%%EOF`;
    parts.push(textBytes(xref));
    return new Blob([joinBytes(parts)],{type:"application/pdf"});
  }

  function toast(message,type=""){
    clearTimeout(toastTimer);
    dom.toast.textContent=message;
    dom.toast.className=`qr-toast is-visible ${type}`.trim();
    toastTimer=setTimeout(()=>dom.toast.classList.remove("is-visible"),3200);
  }

  document.addEventListener("DOMContentLoaded",init);
})();

;
document.addEventListener("DOMContentLoaded", () => {
    const body = document.body;
    const loader = document.querySelector(".site-loader");
    const navbar = document.querySelector(".navbar");
    const menuToggle = document.getElementById("menu-toggle");
    const transitionCurtain = document.createElement("div");
    transitionCurtain.className = "page-transition-curtain";
    transitionCurtain.setAttribute("aria-hidden", "true");
    document.body.appendChild(transitionCurtain);

    const clearNavigationState = ({ restored = false } = {}) => {
        body.classList.remove("is-leaving");
        transitionCurtain.classList.remove("is-active");
        if (restored) {
            body.classList.add("site-loaded");
            loader?.classList.add("is-hidden");
        }
    };

    window.addEventListener("pageshow", (event) => clearNavigationState({ restored: event.persisted }));
    window.addEventListener("pagehide", (event) => { if (event.persisted) clearNavigationState({ restored: true }); });
    document.addEventListener("visibilitychange", () => { if (!document.hidden) clearNavigationState(); });

    // Homepage welcome audio. Browsers may block sound until the visitor interacts,
    // so autoplay is attempted first and a premium play control appears when needed.
    const welcomeAudio = document.getElementById("welcomeAudio");
    const welcomeAudioWidget = document.getElementById("welcomeAudioWidget");
    const welcomeAudioAction = document.getElementById("welcomeAudioAction");
    const welcomeAudioStatus = document.getElementById("welcomeAudioStatus");
    const welcomeAudioDismiss = document.getElementById("welcomeAudioDismiss");
    welcomeAudioWidget?.setAttribute("data-floating-control", "audio-home");
    const welcomePlayedKey = "athanas-welcome-audio-played";
    const welcomeDismissedKey = "athanas-welcome-audio-dismissed";
    const anyWelcomePlayedKey = "athanas-any-welcome-audio-played";
    let welcomeHideTimer = null;

    const readSessionFlag = (key) => {
        try {
            return window.sessionStorage.getItem(key) === "true";
        } catch (error) {
            return false;
        }
    };

    const saveSessionFlag = (key) => {
        try {
            window.sessionStorage.setItem(key, "true");
        } catch (error) {
            // Storage can be unavailable in strict privacy modes; audio still works.
        }
    };

    const showWelcomeAudioWidget = (state, label) => {
        if (!welcomeAudioWidget || !welcomeAudioAction || !welcomeAudioStatus) return;

        window.clearTimeout(welcomeHideTimer);
        welcomeAudioWidget.hidden = false;
        welcomeAudioWidget.classList.toggle("is-playing", state === "playing");
        welcomeAudioWidget.classList.toggle("is-ready", state === "ready");
        welcomeAudioWidget.classList.toggle("is-ended", state === "ended");
        welcomeAudioStatus.textContent = label;
        document.dispatchEvent(new CustomEvent("athanas:floating-layout"));

        const actionLabel = state === "playing"
            ? "Pause welcome message"
            : state === "ended"
                ? "Replay welcome message"
                : "Play welcome message";
        welcomeAudioAction.setAttribute("aria-label", actionLabel);
    };

    const hideWelcomeAudioWidget = () => {
        if (!welcomeAudioWidget) return;
        welcomeAudioWidget.classList.add("is-hiding");
        document.dispatchEvent(new CustomEvent("athanas:floating-layout"));
        window.setTimeout(() => {
            welcomeAudioWidget.hidden = true;
            welcomeAudioWidget.classList.remove("is-hiding", "is-playing", "is-ready", "is-ended");
        }, 280);
    };

    const playWelcomeAudio = async ({ reset = false } = {}) => {
        if (!welcomeAudio) return false;

        if (reset || welcomeAudio.ended) welcomeAudio.currentTime = 0;
        welcomeAudio.volume = 0.82;

        try {
            await welcomeAudio.play();
            saveSessionFlag(welcomePlayedKey);
            saveSessionFlag(anyWelcomePlayedKey);
            showWelcomeAudioWidget("playing", "Welcome audio playing");
            return true;
        } catch (error) {
            showWelcomeAudioWidget("ready", "Tap to hear our welcome");
            return false;
        }
    };

    const initialiseWelcomeAudio = () => {
        if (!welcomeAudio || readSessionFlag(welcomePlayedKey) || readSessionFlag(welcomeDismissedKey) || readSessionFlag(anyWelcomePlayedKey)) return;

        const dataSaverEnabled = Boolean(navigator.connection && navigator.connection.saveData);
        if (dataSaverEnabled) {
            showWelcomeAudioWidget("ready", "Play our welcome");
            return;
        }

        playWelcomeAudio();
    };

    if (welcomeAudio && welcomeAudioAction && welcomeAudioDismiss) {
        welcomeAudioAction.addEventListener("click", () => {
            if (!welcomeAudio.paused) {
                welcomeAudio.pause();
                showWelcomeAudioWidget("ready", "Continue welcome audio");
                return;
            }

            playWelcomeAudio({ reset: welcomeAudio.ended });
        });

        welcomeAudioDismiss.addEventListener("click", () => {
            welcomeAudio.pause();
            saveSessionFlag(welcomeDismissedKey);
            hideWelcomeAudioWidget();
        });

        welcomeAudio.addEventListener("playing", () => {
            showWelcomeAudioWidget("playing", "Welcome audio playing");
        });

        welcomeAudio.addEventListener("ended", () => {
            showWelcomeAudioWidget("ended", "Replay welcome audio");
            welcomeHideTimer = window.setTimeout(hideWelcomeAudioWidget, 8000);
        });

        welcomeAudio.addEventListener("error", () => {
            hideWelcomeAudioWidget();
        });
    }

    // Top loading state
    window.addEventListener("load", () => {
        body.classList.add("site-loaded");
        if (loader) {
            window.setTimeout(() => loader.classList.add("is-hidden"), 240);
        }
        window.setTimeout(initialiseWelcomeAudio, 900);
    });

    // Page scroll progress indicator
    const progress = document.createElement("div");
    progress.className = "scroll-progress";
    progress.setAttribute("aria-hidden", "true");
    document.body.appendChild(progress);

    const updateScrollUI = () => {
        const scrollTop = window.scrollY || document.documentElement.scrollTop;
        const maxScroll = Math.max(document.documentElement.scrollHeight - window.innerHeight, 1);
        progress.style.width = `${Math.min((scrollTop / maxScroll) * 100, 100)}%`;
        if (navbar) navbar.classList.toggle("nav-scrolled", scrollTop > 16);
    };

    updateScrollUI();
    window.addEventListener("scroll", updateScrollUI, { passive: true });

    // Premium same-page inspirational image viewer.
    // Opening the viewer adds a history entry, so the browser Back button closes it.
    const imageViewerTriggers = Array.from(document.querySelectorAll("[data-image-viewer]"));

    if (imageViewerTriggers.length > 0) {
        const viewer = document.createElement("div");
        viewer.className = "image-viewer";
        viewer.id = "inspiration-image-viewer";
        viewer.setAttribute("aria-hidden", "true");
        viewer.innerHTML = `
            <div class="image-viewer-backdrop" data-image-viewer-close></div>
            <section class="image-viewer-panel" role="dialog" aria-modal="true" aria-labelledby="imageViewerTitle">
                <div class="image-viewer-toolbar">
                    <button class="image-viewer-back" type="button" data-image-viewer-back>
                        <span aria-hidden="true">←</span> Back
                    </button>
                    <span class="image-viewer-kicker">Athanas Inspires · Inspirational Image</span>
                    <button class="image-viewer-close" type="button" data-image-viewer-close aria-label="Close image viewer">×</button>
                </div>
                <figure class="image-viewer-figure">
                    <div class="image-viewer-media">
                        <img src="" alt="">
                    </div>
                    <figcaption class="image-viewer-caption">
                        <div>
                            <span class="image-viewer-type">Visual Inspiration</span>
                            <h2 id="imageViewerTitle"></h2>
                            <p></p>
                        </div>
                        <span class="image-viewer-brand" aria-label="Athanas Inspires">Athanas Inspires</span>
                    </figcaption>
                </figure>
            </section>`;
        document.body.appendChild(viewer);

        const viewerImage = viewer.querySelector(".image-viewer-media img");
        const viewerTitle = viewer.querySelector(".image-viewer-caption h2");
        const viewerCaption = viewer.querySelector(".image-viewer-caption p");
        const viewerBackButton = viewer.querySelector("[data-image-viewer-back]");
        let lastViewerTrigger = null;
        let viewerHistoryWasPushed = false;

        const fillViewer = (trigger) => {
            if (!trigger) return;
            viewerImage.src = trigger.dataset.imageSrc || "";
            viewerImage.alt = trigger.dataset.imageAlt || "Inspirational image";
            viewerTitle.textContent = trigger.dataset.imageTitle || "Inspirational Image";
            viewerCaption.textContent = trigger.dataset.imageCaption || "";
        };

        const openViewer = (trigger, pushHistory = true) => {
            if (trigger) {
                lastViewerTrigger = trigger;
                fillViewer(trigger);
            }
            viewer.classList.add("is-open");
            viewer.setAttribute("aria-hidden", "false");
            document.body.classList.add("image-viewer-open");

            if (pushHistory && window.location.hash !== "#inspiration-image-viewer") {
                history.pushState({ imageViewer: true }, "", "#inspiration-image-viewer");
                viewerHistoryWasPushed = true;
            }

            window.setTimeout(() => viewerBackButton.focus(), 80);
        };

        const closeViewer = (restoreFocus = true) => {
            viewer.classList.remove("is-open");
            viewer.setAttribute("aria-hidden", "true");
            document.body.classList.remove("image-viewer-open");
            if (restoreFocus && lastViewerTrigger) lastViewerTrigger.focus({ preventScroll: true });
        };

        const leaveViewer = () => {
            if (viewerHistoryWasPushed && window.location.hash === "#inspiration-image-viewer") {
                viewerHistoryWasPushed = false;
                history.back();
            } else {
                if (window.location.hash === "#inspiration-image-viewer") {
                    history.replaceState(null, "", `${window.location.pathname}${window.location.search}`);
                }
                closeViewer();
            }
        };

        imageViewerTriggers.forEach((trigger) => {
            trigger.addEventListener("click", (event) => {
                event.preventDefault();
                openViewer(trigger, true);
            });
        });

        viewer.querySelectorAll("[data-image-viewer-close]").forEach((control) => {
            control.addEventListener("click", leaveViewer);
        });
        viewerBackButton.addEventListener("click", leaveViewer);

        window.addEventListener("popstate", () => {
            if (window.location.hash === "#inspiration-image-viewer") {
                openViewer(lastViewerTrigger || imageViewerTriggers[0], false);
            } else {
                viewerHistoryWasPushed = false;
                closeViewer(true);
            }
        });

        document.addEventListener("keydown", (event) => {
            if (!viewer.classList.contains("is-open")) return;

            if (event.key === "Escape") {
                event.preventDefault();
                leaveViewer();
                return;
            }

            if (event.key === "Tab") {
                const focusable = Array.from(viewer.querySelectorAll('button, a[href]')).filter((item) => !item.hasAttribute("disabled"));
                if (focusable.length === 0) return;
                const first = focusable[0];
                const last = focusable[focusable.length - 1];
                if (event.shiftKey && document.activeElement === first) {
                    event.preventDefault();
                    last.focus();
                } else if (!event.shiftKey && document.activeElement === last) {
                    event.preventDefault();
                    first.focus();
                }
            }
        });

        // Support a copied URL containing the viewer hash without opening a new page.
        if (window.location.hash === "#inspiration-image-viewer") {
            openViewer(imageViewerTriggers[0], false);
        }
    }

    // Smooth in-page scrolling and close mobile menu after click
    document.querySelectorAll('a[href]').forEach((link) => {
        link.addEventListener("click", (event) => {
            const href = link.getAttribute("href") || "";

            if (link.hasAttribute("data-image-viewer")) return;

            if (menuToggle && link.closest(".nav-links")) {
                menuToggle.checked = false;
            }

            if (href.startsWith("#") && href.length > 1) {
                const target = document.querySelector(href);
                if (target) {
                    event.preventDefault();
                    target.scrollIntoView({ behavior: "smooth", block: "start" });
                    history.pushState(null, "", href);
                }
            }
        });
    });

    // Gentle page transition for normal internal page links
    document.querySelectorAll('a[href$=".html"], a[href*=".html#"]').forEach((link) => {
        link.addEventListener("click", (event) => {
            const href = link.getAttribute("href") || "";
            const isModifiedClick = event.metaKey || event.ctrlKey || event.shiftKey || event.altKey || event.button !== 0;
            const opensNewTab = link.target && link.target !== "_self";

            if (isModifiedClick || opensNewTab || href.includes("#") || href.startsWith("mailto:") || href.startsWith("tel:")) return;

            event.preventDefault();
            body.classList.add("is-leaving");
            transitionCurtain.classList.add("is-active");
            window.setTimeout(() => {
                window.location.href = href;
            }, 210);
        });
    });

    // Reveal sections/cards gently as visitors scroll
    const revealTargets = document.querySelectorAll([
        ".hero-text",
        ".hero-image",
        ".section-heading",
        ".start-here-card",
        ".course-card",
        ".home-learning-card",
        ".youtube-hub-promo-inner",
        ".mission-pillar",
        ".home-faq-item",
        ".update-card",
        ".platform-card",
        ".assignment-card",
        ".session-card",
        ".download-card",
        ".contact-card",
        ".about-card",
        ".about-image",
        ".video-series",
        ".youtube-hero-card",
        ".community-card",
        ".why-card",
        ".side-card",
        ".purpose-mission-inner",
        ".latest-row",
        ".featured-inspiration-copy",
        ".featured-inspiration-image",
        ".inspiration-link-row",
        ".coming-line",
        ".article-hero-copy",
        ".article-hero-image",
        ".article-reflection",
        ".start-small-challenge",
        ".article-next-step"
    ].join(","));

    if ("IntersectionObserver" in window) {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    entry.target.classList.add("is-visible");
                    observer.unobserve(entry.target);
                }
            });
        }, { threshold: 0.12, rootMargin: "0px 0px -40px 0px" });

        revealTargets.forEach((element, index) => {
            element.classList.add("reveal-on-scroll");
            element.style.transitionDelay = `${Math.min((index % 6) * 55, 275)}ms`;
            observer.observe(element);
        });
    } else {
        revealTargets.forEach((element) => element.classList.add("is-visible"));
    }
});


/* Shared grouped navigation dropdowns and homepage FAQ preview */
document.addEventListener("DOMContentLoaded", () => {
    const dropdowns = Array.from(document.querySelectorAll(".nav-dropdown"));
    const desktopPointer = window.matchMedia("(hover: hover) and (pointer: fine)");
    const closeTimers = new WeakMap();
    const usesDesktopNavigation = () => desktopPointer.matches && window.innerWidth > 860;

    const setDropdown = (dropdown, shouldOpen) => {
        const toggle = dropdown.querySelector(".nav-dropdown-toggle");
        dropdown.classList.toggle("is-open", shouldOpen);
        toggle?.setAttribute("aria-expanded", String(shouldOpen));
    };

    const closeAllDropdowns = (except = null) => {
        dropdowns.forEach((dropdown) => {
            if (dropdown !== except) setDropdown(dropdown, false);
        });
    };

    const focusMenuItem = (dropdown, position = "first") => {
        const links = Array.from(dropdown.querySelectorAll(".nav-dropdown-menu a, .nav-dropdown-menu button"));
        if (!links.length) return;
        (position === "last" ? links.at(-1) : links[0]).focus();
    };

    dropdowns.forEach((dropdown) => {
        const toggle = dropdown.querySelector(".nav-dropdown-toggle");
        const menu = dropdown.querySelector(".nav-dropdown-menu");
        const links = Array.from(dropdown.querySelectorAll(".nav-dropdown-menu a, .nav-dropdown-menu button"));

        toggle?.addEventListener("click", (event) => {
            event.stopPropagation();
            const willOpen = usesDesktopNavigation() ? true : !dropdown.classList.contains("is-open");
            closeAllDropdowns(dropdown);
            setDropdown(dropdown, willOpen);
        });

        toggle?.addEventListener("keydown", (event) => {
            if (event.key === "ArrowDown" || event.key === "ArrowUp") {
                event.preventDefault();
                closeAllDropdowns(dropdown);
                setDropdown(dropdown, true);
                window.setTimeout(() => focusMenuItem(dropdown, event.key === "ArrowUp" ? "last" : "first"), 20);
            }
        });

        menu?.addEventListener("keydown", (event) => {
            const currentIndex = links.indexOf(document.activeElement);
            if (currentIndex < 0) return;

            if (event.key === "ArrowDown" || event.key === "ArrowUp") {
                event.preventDefault();
                const direction = event.key === "ArrowDown" ? 1 : -1;
                links[(currentIndex + direction + links.length) % links.length].focus();
            } else if (event.key === "Home") {
                event.preventDefault();
                links[0]?.focus();
            } else if (event.key === "End") {
                event.preventDefault();
                links.at(-1)?.focus();
            }
        });

        links.forEach((link) => {
            link.addEventListener("click", () => {
                setDropdown(dropdown, false);
                const mobileMenuToggle = document.getElementById("menu-toggle");
                if (mobileMenuToggle) mobileMenuToggle.checked = false;
            });
        });

        dropdown.addEventListener("mouseenter", () => {
            if (!usesDesktopNavigation()) return;
            const timer = closeTimers.get(dropdown);
            if (timer) window.clearTimeout(timer);
            closeAllDropdowns(dropdown);
            setDropdown(dropdown, true);
        });

        dropdown.addEventListener("mouseleave", () => {
            if (!usesDesktopNavigation()) return;
            const timer = window.setTimeout(() => setDropdown(dropdown, false), 140);
            closeTimers.set(dropdown, timer);
        });
    });

    document.addEventListener("click", (event) => {
        if (!event.target.closest(".nav-dropdown")) closeAllDropdowns();
    });

    document.addEventListener("keydown", (event) => {
        if (event.key !== "Escape") return;
        const openDropdown = dropdowns.find((dropdown) => dropdown.classList.contains("is-open"));
        closeAllDropdowns();
        openDropdown?.querySelector(".nav-dropdown-toggle")?.focus();
    });

    document.getElementById("menu-toggle")?.addEventListener("change", (event) => {
        if (!event.target.checked) closeAllDropdowns();
    });

    window.addEventListener("resize", () => closeAllDropdowns(), { passive: true });

    const homeFaqItems = Array.from(document.querySelectorAll(".home-faq-item"));
    homeFaqItems.forEach((item) => {
        item.addEventListener("toggle", () => {
            if (!item.open) return;
            homeFaqItems.forEach((other) => {
                if (other !== item) other.open = false;
            });
        });
    });
});

;
(() => {
  "use strict";
  const selector = '[data-floating-control], .ai-floating-button, .ai-loader-launcher';
  let scheduled = false;

  function visible(element) {
    if (!element || element.hidden) return false;
    const style = getComputedStyle(element);
    if (style.display === "none" || style.visibility === "hidden") return false;
    if (element.classList.contains("ytp-back-top") && !element.classList.contains("is-visible")) return false;
    if (element.classList.contains("ytp-floating-youtube") && !element.classList.contains("is-visible")) return false;
    return true;
  }

  function heightOf(element, fallback) {
    const rect = element.getBoundingClientRect();
    return Math.max(rect.height || 0, fallback);
  }

  function layout() {
    scheduled = false;
    if (!document.body) return;
    const controls = Array.from(document.querySelectorAll(selector)).filter(visible);
    const mobile = matchMedia("(max-width: 760px)").matches;
    const safe = mobile ? 10 : 16;
    const gap = mobile ? 10 : 12;
    const consent = controls.find((el) => el.dataset.floatingControl === "consent");
    const audio = controls.filter((el) => (el.dataset.floatingControl || "").includes("audio"));
    const circles = controls.filter((el) => {
      const role = el.dataset.floatingControl || "";
      return role !== "consent" && !role.includes("audio");
    }).sort((a, b) => {
      const order = { assistant: 0, youtube: 1, "back-top": 2 };
      return (order[a.dataset.floatingControl] ?? 8) - (order[b.dataset.floatingControl] ?? 8);
    });

    let bottom = safe;
    if (consent) {
      consent.style.setProperty("--floating-offset", `${safe}px`);
      consent.style.setProperty("--floating-right", `${safe}px`);
      bottom += heightOf(consent, mobile ? 168 : 110) + gap;
    }

    audio.forEach((el) => {
      el.style.setProperty("--floating-offset", `${bottom}px`);
      el.style.setProperty("--floating-right", `${safe}px`);
      bottom += heightOf(el, 76) + gap;
    });

    if (mobile) {
      circles.forEach((el, index) => {
        el.style.setProperty("--floating-offset", `${bottom}px`);
        el.style.setProperty("--floating-right", `${safe + index * 64}px`);
      });
      if (circles.length) bottom += 66;
    } else {
      circles.forEach((el, index) => {
        el.style.setProperty("--floating-offset", `${bottom + index * 72}px`);
        el.style.setProperty("--floating-right", `${safe}px`);
      });
      if (circles.length) bottom += circles.length * 72;
    }

    document.body.style.setProperty("--floating-reserved-space", `${bottom}px`);
    document.body.classList.toggle("has-visible-audio-control", audio.length > 0);
    document.body.classList.toggle("has-cookie-consent", Boolean(consent));
  }

  function requestLayout() {
    if (scheduled) return;
    scheduled = true;
    requestAnimationFrame(layout);
  }

  document.addEventListener("DOMContentLoaded", () => {
    requestLayout();
    new MutationObserver(requestLayout).observe(document.body, { childList: true, subtree: true, attributes: true, attributeFilter: ["class", "hidden", "style"] });
    if ("ResizeObserver" in window) new ResizeObserver(requestLayout).observe(document.body);
    window.addEventListener("resize", requestLayout, { passive: true });
    window.visualViewport?.addEventListener("resize", requestLayout, { passive: true });
    document.addEventListener("athanas:floating-layout", requestLayout);
    window.setTimeout(requestLayout, 300);
    window.setTimeout(requestLayout, 1200);
  });
})();

;
/* Generated from individual source/content files. */
window.ATHANAS_LEARNING_CONTENT = {
  "version": "2026.07.29.2",
  "updated": "2026-07-29",
  "site": {
    "name": "Athanas Inspires",
    "domain": "https://athanasinspires.com",
    "youtubeChannel": "https://www.youtube.com/@Athanas_Inspires",
    "youtubeSubscribe": "https://www.youtube.com/@Athanas_Inspires?sub_confirmation=1",
    "whatsappCommunity": "https://chat.whatsapp.com/Fd9rDqOyxRrKUctGqfevmt",
    "whatsappSupport": "https://wa.me/255695110859",
    "email": "chriss.ath2@gmail.com"
  },
  "featuredLessonId": "word-3",
  "featuredArticleId": "build-skills-difficult-ignore",
  "latestUpdates": [
    {
      "type": "tool",
      "icon": "▦",
      "title": "QR Code Generator",
      "summary": "Create customised QR codes with a searchable builder, optional poster text, a premium A4 output, and QR-only downloads.",
      "url": "/digital-tools/qr-code-generator.html",
      "date": "2026-07-29",
      "dateLabel": "29 July 2026"
    },
    {
      "type": "article",
      "icon": "✦",
      "title": "Build Skills That Make You Difficult to Ignore",
      "summary": "A bold, practical guide to discovering your strongest potential, building valuable skills, and turning hidden ability into visible value.",
      "url": "/build-skills-that-make-you-difficult-to-ignore.html",
      "date": "2026-07-26",
      "dateLabel": "26 July 2026"
    },
    {
      "type": "lesson",
      "icon": "▶",
      "title": "Word Session 3: Professional Documents with Visuals",
      "summary": "Learn pictures, Wrap Text, shapes, text boxes, WordArt, symbols, and equations step by step.",
      "url": "/youtube.html#featured",
      "date": "2026-07-06",
      "dateLabel": "6 July 2026"
    },
    {
      "type": "assignment",
      "icon": "✓",
      "title": "Word Session 3 Mini Project",
      "summary": "Recreate the Internet Safety Guide and turn the lesson into a complete practical document.",
      "url": "/assignments.html#word-session-3-assignment",
      "date": "2026-07-06",
      "dateLabel": "6 July 2026"
    }
  ],
  "testimonials": [
    {
      "name": "Daniel",
      "country": "Ghana",
      "lesson": "Microsoft Excel learner",
      "quote": "Through Athanas Inspires, I moved from zero to hero. I was selected as an assistant computer-lab technician at my school and as a printing-press management tutor. God bless you more.",
      "featured": true
    },
    {
      "name": "Dorcus Frasisca",
      "country": "Uganda",
      "lesson": "Microsoft Excel learner",
      "quote": "I am now confident in learning this skill because your work shows that the skill is for everybody. Thank you very much, Teacher Athanas."
    },
    {
      "name": "Onyangogo George Indege",
      "country": "Kenya",
      "lesson": "Microsoft Excel learner",
      "quote": "From Nairobi, Kenya: Athanas is the most effective instructor I have ever experienced."
    },
    {
      "name": "Witness Mtonga",
      "country": "Malawi",
      "lesson": "Computer Basics learner",
      "quote": "I understand well. Please continue the way you teach us."
    },
    {
      "name": "BeachmasterX",
      "country": "United States",
      "lesson": "Excel Session 1 learner",
      "quote": "Only a few minutes in, and I already appreciate what you are saying. Dayton, Ohio, USA here—thank you."
    },
    {
      "name": "Humble Jose",
      "country": "Ghana",
      "lesson": "Microsoft Excel learner",
      "quote": "You are an amazing teacher. You make learning easy and understandable—your student from Ghana."
    },
    {
      "name": "Peter Lusenge",
      "country": "Zambia",
      "lesson": "Computer Basics learner",
      "quote": "What an excellent instructor and teacher. Watching from Zambia."
    },
    {
      "name": "Beauty",
      "country": "Nigeria",
      "lesson": "Athanas Inspires ICT learner",
      "quote": "Thank you very much for your great support to us. I am Beauty from Nigeria, watching from Dubai. God bless you, sir."
    }
  ],
  "series": [
    {
      "id": "computer-basics",
      "anchor": "beginner-installments",
      "navLabel": "Computer Basics",
      "eyebrow": "Start Here",
      "title": "Computer Basics",
      "shortDescription": "Build confidence from the very beginning.",
      "description": "Learn the essential computer skills every absolute beginner needs before moving to Microsoft Office.",
      "icon": "💻",
      "art": "assets/images/youtube/computer-basics-series.svg",
      "theme": "basics",
      "lessons": [
        {
          "id": "computer-01",
          "session": "0.1",
          "title": "Introduction to Computers, Mouse, and Keyboard",
          "displayTitle": "Session 0.1: Introduction to Computers, Mouse, and Keyboard",
          "description": "Understand what a computer is and begin using the mouse and keyboard with confidence.",
          "videoId": "i8Qd-g7OWYU",
          "videoUrl": "https://www.youtube.com/watch?v=i8Qd-g7OWYU",
          "status": "published"
        },
        {
          "id": "computer-02",
          "session": "0.2",
          "title": "Understanding Desktop Icons & Basic Computer Navigation",
          "displayTitle": "Session 0.2: Understanding Desktop Icons & Basic Computer Navigation",
          "description": "Learn desktop icons, windows, and the basic navigation skills used across computers.",
          "videoId": "vYGVk9O0EX4",
          "videoUrl": "https://www.youtube.com/watch?v=vYGVk9O0EX4",
          "status": "published"
        },
        {
          "id": "computer-03",
          "session": "0.3",
          "title": "File and Folder Management",
          "displayTitle": "Session 0.3: File and Folder Management — Navigate and Organize Like a Pro",
          "description": "Create, rename, move, copy, and organise files and folders clearly.",
          "videoId": "Bha7ozesRts",
          "videoUrl": "https://www.youtube.com/watch?v=Bha7ozesRts",
          "status": "published"
        },
        {
          "id": "computer-04",
          "session": "0.4",
          "title": "Universal Computer Skills Every User Should Know",
          "displayTitle": "Session 0.4: Computer Skills for Beginners — Universal Concepts Every User Should Know",
          "description": "Practise shutdown, flash drives, window management, and other universal computer skills.",
          "videoId": "8EeUPRaAB2A",
          "videoUrl": "https://www.youtube.com/watch?v=8EeUPRaAB2A",
          "status": "published"
        }
      ]
    },
    {
      "id": "word-series",
      "anchor": "word-series",
      "navLabel": "Word",
      "eyebrow": "Create with Confidence",
      "title": "Microsoft Word",
      "shortDescription": "Move from basic typing to professional documents.",
      "description": "Build clear, well-structured, and visually professional documents step by step.",
      "icon": "📄",
      "art": "assets/images/youtube/word-series.svg",
      "theme": "word",
      "lessons": [
        {
          "id": "word-1",
          "session": "1",
          "title": "Microsoft Word for Absolute Beginners",
          "displayTitle": "Word Session 1: Microsoft Word for Absolute Beginners",
          "description": "Open Word, type, format text, save documents, and use undo and redo.",
          "videoId": "vG-tM8XaDjw",
          "videoUrl": "https://www.youtube.com/watch?v=vG-tM8XaDjw",
          "status": "published"
        },
        {
          "id": "word-2",
          "session": "2",
          "title": "From Typing to a Proper Document",
          "displayTitle": "Word Session 2: From Typing to a Proper Document",
          "description": "Use paragraphs, spacing, alignment, lists, headings, and document structure.",
          "videoId": "mT-q8ZIe_fE",
          "videoUrl": "https://www.youtube.com/watch?v=mT-q8ZIe_fE",
          "status": "published"
        },
        {
          "id": "word-3",
          "session": "3",
          "title": "Create Professional Documents with Visuals",
          "displayTitle": "Word Session 3: Create Professional Documents with Visuals",
          "description": "Use pictures, Wrap Text, shapes, text boxes, WordArt, symbols, and equations.",
          "videoId": "Fr0np3nukWg",
          "videoUrl": "https://www.youtube.com/watch?v=Fr0np3nukWg",
          "status": "published",
          "featured": true,
          "assignmentId": "word-3-assignment",
          "resources": [
            {
              "id": "word-3-image-zip",
              "label": "Download Image Resource",
              "type": "ZIP",
              "url": "assets/downloads/word-session-3-mini-project-image.zip",
              "filename": "word-session-3-mini-project-image.zip"
            }
          ]
        },
        {
          "id": "word-4",
          "session": "4",
          "title": "Word Session 4",
          "displayTitle": "Word Session 4",
          "description": "The next Microsoft Word lesson is being prepared.",
          "status": "coming-soon"
        }
      ]
    },
    {
      "id": "excel-series",
      "anchor": "excel-series",
      "navLabel": "Excel",
      "eyebrow": "Think with Spreadsheets",
      "title": "Microsoft Excel",
      "shortDescription": "Learn formulas, tables, and smart decision systems.",
      "description": "Learn formulas, tables, IF statements, and practical spreadsheet thinking for real work.",
      "icon": "📊",
      "art": "assets/images/youtube/excel-series.svg",
      "theme": "excel",
      "lessons": [
        {
          "id": "excel-1",
          "session": "1",
          "title": "Excel for Absolute Beginners — Start Here",
          "displayTitle": "Excel Session 1: Excel for Absolute Beginners — Start Here",
          "description": "Open Excel, enter and organise data, format cells, use SUM, and save your workbook.",
          "videoId": "LQuil7Itb-4",
          "videoUrl": "https://www.youtube.com/watch?v=LQuil7Itb-4",
          "status": "published"
        },
        {
          "id": "excel-2",
          "session": "2",
          "title": "Start Using Formulas the Right Way",
          "displayTitle": "Excel Session 2: Start Using Formulas the Right Way",
          "description": "Learn formula structure, SUM, AVERAGE, RANK, borders, and organised worksheet design.",
          "videoId": "fDepU9fUpSw",
          "videoUrl": "https://www.youtube.com/watch?v=fDepU9fUpSw",
          "status": "published",
          "assignmentId": "excel-2-assignment"
        },
        {
          "id": "excel-3",
          "session": "3",
          "title": "Building Real Tables with Formulas",
          "displayTitle": "Excel Session 3: Building Real Tables with Formulas",
          "description": "Build a business table using formulas, percentages, summaries, sorting, MAX, and MIN.",
          "videoId": "jobqJzV9gGk",
          "videoUrl": "https://www.youtube.com/watch?v=jobqJzV9gGk",
          "status": "published"
        },
        {
          "id": "excel-4",
          "session": "4",
          "title": "IF Statements for Beginners",
          "displayTitle": "Excel Session 4: IF Statements for Beginners",
          "description": "Use IF logic for pass or fail, remarks, profit or loss, and automatic decisions.",
          "videoId": "QqfmHzystTc",
          "videoUrl": "https://www.youtube.com/watch?v=QqfmHzystTc",
          "status": "published",
          "assignmentId": "excel-4-assignment"
        },
        {
          "id": "excel-5",
          "session": "5",
          "title": "Building Smart Decision Systems with Nested IF",
          "displayTitle": "Excel Session 5: Building Smart Decision Systems with Nested IF",
          "description": "Move beyond a single condition and build smarter automatic decisions with Nested IF.",
          "status": "coming-soon",
          "upcoming": true
        }
      ]
    }
  ],
  "futureSeries": [
    {
      "id": "powerpoint",
      "title": "Microsoft PowerPoint",
      "icon": "📽️",
      "description": "Create powerful presentations for teaching, business, school, and public speaking.",
      "status": "coming-soon"
    },
    {
      "id": "python",
      "title": "Python for Beginners",
      "icon": "🐍",
      "description": "Learn programming step by step and begin building useful beginner projects.",
      "status": "coming-soon"
    }
  ],
  "assignments": [
    {
      "id": "word-3-assignment",
      "lessonId": "word-3",
      "series": "Microsoft Word",
      "title": "Word Session 3 Mini Project",
      "description": "Recreate the Internet Safety Guide and practise pictures, Wrap Text, shapes, text boxes, WordArt, symbols, equations, and clean layout.",
      "icon": "📄",
      "status": "available",
      "anchor": "word-session-3-assignment",
      "watchUrl": "https://www.youtube.com/watch?v=Fr0np3nukWg",
      "downloadUrl": "assets/downloads/word-session-3-mini-project-image.zip",
      "downloadLabel": "Download Image Resource",
      "preview": "assets/images/word-session-3-mini-project-preview.png",
      "submitUrl": "https://wa.me/255695110859?text=Hello%20Athanas%20Inspires%2C%20I%20want%20to%20submit%20my%20Word%20Session%203%20assignment.%20My%20name%20is%20_____.%20I%20recreated%20the%20Internet%20Safety%20Guide%20mini%20project."
    },
    {
      "id": "excel-2-assignment",
      "lessonId": "excel-2",
      "series": "Microsoft Excel",
      "title": "Excel Session 2 Assignment",
      "description": "Practise formulas, basic calculations, SUM, AVERAGE, RANK, and Excel cell references.",
      "icon": "📊",
      "status": "available",
      "anchor": "excel-session-2",
      "watchUrl": "https://www.youtube.com/watch?v=fDepU9fUpSw",
      "downloadUrl": "assets/downloads/excel-session-2-assignment.xlsx",
      "downloadLabel": "Download Assignment",
      "submitUrl": "https://wa.me/255695110859?text=Hello%20Athanas%20Inspires%2C%20I%20want%20to%20submit%20my%20Excel%20Session%202%20assignment.%20My%20name%20is%20_____."
    },
    {
      "id": "excel-4-assignment",
      "lessonId": "excel-4",
      "series": "Microsoft Excel",
      "title": "Excel Session 4 Assignment",
      "description": "Practise IF logic and simple automatic decision-making in Excel.",
      "icon": "📈",
      "status": "available",
      "anchor": "excel-session-4",
      "watchUrl": "https://www.youtube.com/watch?v=QqfmHzystTc",
      "downloadUrl": "assets/downloads/excel-session-4-assignment.xlsx",
      "downloadLabel": "Download Assignment",
      "submitUrl": "https://wa.me/255695110859?text=Hello%20Athanas%20Inspires%2C%20I%20want%20to%20submit%20my%20Excel%20Session%204%20assignment.%20My%20name%20is%20_____."
    }
  ],
  "downloads": [
    {
      "id": "word-3-image-zip",
      "category": "Microsoft Word",
      "title": "Word Session 3 Image Resource",
      "description": "The image pack used for the Internet Safety Guide mini project.",
      "icon": "📄",
      "type": "ZIP",
      "url": "assets/downloads/word-session-3-mini-project-image.zip",
      "relatedUrl": "assignments.html#word-session-3-assignment"
    },
    {
      "id": "excel-2-file",
      "category": "Microsoft Excel",
      "title": "Excel Session 2 Assignment File",
      "description": "Practice formulas, calculations, and cell references from Excel Session 2.",
      "icon": "📊",
      "type": "XLSX",
      "url": "assets/downloads/excel-session-2-assignment.xlsx",
      "relatedUrl": "assignments.html#excel-session-2"
    },
    {
      "id": "excel-4-file",
      "category": "Microsoft Excel",
      "title": "Excel Session 4 Assignment File",
      "description": "Practice IF logic and automatic decision-making from Excel Session 4.",
      "icon": "📈",
      "type": "XLSX",
      "url": "assets/downloads/excel-session-4-assignment.xlsx",
      "relatedUrl": "assignments.html#excel-session-4"
    }
  ],
  "tools": [
    {
      "id": "quiz",
      "title": "General ICT Quiz Game",
      "description": "Practise computer basics, Word, Excel, internet safety, shortcuts, and beginner ICT skills with instant feedback.",
      "icon": "🎮",
      "url": "/digital-tools/ict-quiz.html",
      "category": "learn-practise",
      "status": "available",
      "level": "Beginner",
      "purpose": "Knowledge practice"
    },
    {
      "id": "shortcuts",
      "title": "Computer Shortcut Keys Trainer",
      "description": "Train Windows, Word, Excel, browser, and common keyboard shortcuts with instant correction.",
      "icon": "⌨️",
      "url": "/digital-tools/shortcut-keys-trainer.html",
      "category": "learn-practise",
      "status": "available",
      "level": "Beginner",
      "purpose": "Speed and productivity"
    },
    {
      "id": "typing",
      "title": "Typing Speed Trainer",
      "description": "Improve typing speed, accuracy, focus, and ICT vocabulary through timed practice.",
      "icon": "⚡",
      "url": "/digital-tools/typing-trainer.html",
      "category": "learn-practise",
      "status": "available",
      "level": "All levels",
      "purpose": "Typing confidence"
    },
    {
      "id": "calculator",
      "title": "Scientific Calculator",
      "description": "Use a practical calculator for school mathematics, science, percentages, powers, roots, and trigonometry.",
      "icon": "🧮",
      "url": "/digital-tools/scientific-calculator.html",
      "category": "calculate",
      "status": "available",
      "level": "All levels",
      "purpose": "Calculations"
    },
    {
      "id": "qr",
      "title": "QR Code Generator",
      "description": "Create customised QR codes with a live portrait poster preview, optional top and bottom text, A4 PDF or PNG export, and a QR-only PNG.",
      "icon": "▦",
      "url": "/digital-tools/qr-code-generator.html",
      "category": "create",
      "status": "available",
      "level": "All levels",
      "purpose": "Create and share"
    },
    {
      "id": "results-templates",
      "title": "Results Analysis Templates",
      "description": "Excel templates for marks, totals, averages, ranking, grades, and performance review.",
      "icon": "📊",
      "status": "coming-soon",
      "level": "Teachers",
      "purpose": "School analysis"
    },
    {
      "id": "school-records",
      "title": "School Record Templates",
      "description": "Templates for attendance, reports, lists, summaries, and school organisation.",
      "icon": "📋",
      "status": "coming-soon",
      "level": "Teachers",
      "purpose": "School records"
    },
    {
      "id": "simple-bots",
      "title": "Simple Bots",
      "description": "Beginner-friendly bots for learning support, reminders, quizzes, and simple automation.",
      "icon": "🤖",
      "status": "coming-soon",
      "level": "Future",
      "purpose": "Automation"
    }
  ],
  "pages": [
    {
      "id": "home",
      "category": "Page",
      "title": "Home",
      "description": "Explore Athanas Inspires learning, inspiration, tools, and resources.",
      "url": "index.html",
      "keywords": "home platform learn believe grow build"
    },
    {
      "id": "about",
      "category": "Page",
      "title": "About Athanas Inspires",
      "description": "Learn about the mission, story, and purpose behind Athanas Inspires.",
      "url": "about.html",
      "keywords": "about mission purpose teacher mentor Tanzania"
    },
    {
      "id": "youtube",
      "category": "Page",
      "title": "YouTube Learning Hub",
      "description": "Watch Computer Basics, Microsoft Word, and Microsoft Excel lessons in one guided flow.",
      "url": "youtube.html",
      "keywords": "youtube videos channel subscribe learning hub"
    },
    {
      "id": "courses",
      "category": "Page",
      "title": "ICT Lessons",
      "description": "Follow the complete beginner-friendly computer, Word, and Excel learning roadmap.",
      "url": "courses.html",
      "keywords": "courses lessons roadmap computer word excel"
    },
    {
      "id": "assignments",
      "category": "Page",
      "title": "Assignments",
      "description": "Watch, download, complete, and submit available practice assignments.",
      "url": "assignments.html",
      "keywords": "assignments practice submit whatsapp"
    },
    {
      "id": "downloads",
      "category": "Page",
      "title": "Downloads",
      "description": "Download lesson files, templates, and practice resources.",
      "url": "downloads.html",
      "keywords": "downloads xlsx zip files resources"
    },
    {
      "id": "tools",
      "category": "Page",
      "title": "Digital Tools",
      "description": "Open the QR Code Generator, ICT quiz, shortcut trainer, typing trainer, and scientific calculator.",
      "url": "/digital-tools/index.html",
      "keywords": "tools qr code generator quiz shortcut typing calculator"
    },
    {
      "id": "quiz-page",
      "category": "Digital Tool",
      "title": "General ICT Quiz Game",
      "description": "Test computer basics, Microsoft Word, Excel, internet safety, and general ICT knowledge.",
      "url": "/digital-tools/ict-quiz.html",
      "keywords": "quiz ICT game questions test"
    },
    {
      "id": "shortcuts-page",
      "category": "Digital Tool",
      "title": "Computer Shortcut Keys Trainer",
      "description": "Practise useful Windows, Word, Excel, browser, and general keyboard shortcuts.",
      "url": "/digital-tools/shortcut-keys-trainer.html",
      "keywords": "keyboard shortcuts ctrl trainer"
    },
    {
      "id": "typing-page",
      "category": "Digital Tool",
      "title": "Typing Speed Trainer",
      "description": "Improve typing speed and accuracy through timed beginner-friendly practice.",
      "url": "/digital-tools/typing-trainer.html",
      "keywords": "typing speed accuracy keyboard practice"
    },
    {
      "id": "calculator-page",
      "category": "Digital Tool",
      "title": "Scientific Calculator",
      "description": "Complete everyday and scientific calculations directly in the browser.",
      "url": "/digital-tools/scientific-calculator.html",
      "keywords": "calculator mathematics science percentages roots"
    },
    {
      "id": "qr-generator-page",
      "category": "Digital Tool",
      "title": "QR Code Generator",
      "description": "Create, customise, test, and download QR codes with optional poster text, premium A4 PDF or PNG output, and a clean QR-only PNG.",
      "url": "/digital-tools/qr-code-generator.html",
      "keywords": "qr code generator website whatsapp wifi contact payment poster text a4 pdf png qr only print"
    },
    {
      "id": "education",
      "category": "Page",
      "title": "Tanzanian Education Resources",
      "description": "Explore resources supporting the Tanzanian primary education system.",
      "url": "education.html",
      "keywords": "Tanzania primary education exams notes books"
    },
    {
      "id": "faith",
      "category": "Page",
      "title": "Faith & Personal Growth",
      "description": "Read faith-rooted encouragement, courage-building articles, and personal-growth messages.",
      "url": "faith-inspiration.html",
      "keywords": "faith inspiration courage personal growth encouragement"
    },
    {
      "id": "technology",
      "category": "Page",
      "title": "Technology & Digital Growth",
      "description": "Read simple technology articles that build digital confidence and help beginners move forward.",
      "url": "technology-insights.html",
      "keywords": "technology articles digital growth confidence beginner future skills"
    },
    {
      "id": "faq",
      "category": "Page",
      "title": "Frequently Asked Questions",
      "description": "Find answers about lessons, assignments, downloads, tools, and the community.",
      "url": "faq.html",
      "keywords": "faq questions help support"
    },
    {
      "id": "contact",
      "category": "Page",
      "title": "Contact Athanas Inspires",
      "description": "Reach Athanas Inspires through phone, WhatsApp, or email.",
      "url": "contact.html",
      "keywords": "contact phone whatsapp email"
    },
    {
      "id": "privacy",
      "category": "Page",
      "title": "Privacy Policy",
      "description": "Learn how website data, analytics, embedded media, and local storage are handled.",
      "url": "privacy-policy.html",
      "keywords": "privacy analytics data cookies"
    },
    {
      "id": "cookies",
      "category": "Page",
      "title": "Cookie Information",
      "description": "Understand essential storage and optional analytics technologies.",
      "url": "cookie-policy.html",
      "keywords": "cookies consent local storage analytics"
    },
    {
      "id": "terms",
      "category": "Page",
      "title": "Terms of Use",
      "description": "Read the website, educational-content, and download terms.",
      "url": "terms.html",
      "keywords": "terms disclaimer downloads education"
    },
    {
      "id": "accessibility",
      "category": "Page",
      "title": "Accessibility Statement",
      "description": "Read the accessibility commitment and how to request assistance.",
      "url": "accessibility.html",
      "keywords": "accessibility keyboard screen reader contrast"
    }
  ],
  "articles": [
    {
      "id": "build-skills-difficult-ignore",
      "title": "Build Skills That Make You Difficult to Ignore",
      "subtitle": "Turn your hidden potential into visible value",
      "description": "A bold, deeply practical guide to discovering your strongest potential, building complementary skills, using technology wisely, creating visible evidence, and becoming someone people can trust.",
      "url": "build-skills-that-make-you-difficult-to-ignore.html",
      "date": "2026-07-26",
      "dateLabel": "26 July 2026",
      "readingLabel": "Personal Growth · Skills · Technology",
      "categories": [
        "faith",
        "technology"
      ],
      "primaryCategory": "faith",
      "featured": true,
      "image": "assets/images/articles/build-skills-difficult-to-ignore-premium.jpg",
      "socialImage": "assets/images/social/social-build-skills.jpg",
      "quote": "Do not aim to impress people once. Become the person who repeatedly brings value, solves problems, and finishes what they start.",
      "keywords": "skills potential strengths skill stacking technology problem solving consistency character visibility career growth personal development 30 day challenge",
      "assistantSummary": "The article teaches that people become difficult to ignore by developing valuable and complementary skills, solving real problems, using technology as a multiplier, building trust through consistency and character, and making their work visible without becoming noisy.",
      "keyIdeas": [
        "Potential is only a starting point; developed skill and results are the evidence.",
        "Discover strengths through repeated interests, feedback, problems you solve well, and areas where you learn quickly.",
        "Build a small stack of complementary skills instead of collecting unrelated abilities.",
        "Use technology to amplify an existing strength, not to replace thinking or character.",
        "Build visible evidence through completed work, solved problems, a portfolio, teaching, and consistent results.",
        "Confidence grows stronger when it is supported by evidence, feedback, and repeated contribution.",
        "Manage weaknesses that restrict progress while mastering strengths that distinguish you.",
        "Use mistakes as feedback and measure progress against your previous level rather than copying another person's path.",
        "Follow a simple four-week challenge: discover, practise, add technology, and create evidence."
      ],
      "assistantQuestions": [
        {
          "id": "article-skills-main",
          "title": "How can I become difficult to ignore?",
          "keywords": [
            "difficult to ignore",
            "stand out",
            "be valuable",
            "build valuable skills"
          ],
          "answer": "Become useful, skilled, trustworthy, adaptable, and consistent. Discover what you do well, develop one valuable core skill, add complementary skills, use technology to multiply your effectiveness, solve real problems, and create visible evidence of your work.",
          "details": "The goal is not fame or noise. It is dependable value: the kind of contribution that saves time, improves results, explains ideas clearly, or helps people move forward."
        },
        {
          "id": "article-skills-potential",
          "title": "How do I discover my strongest potential?",
          "keywords": [
            "discover potential",
            "find strengths",
            "what am I good at",
            "natural ability"
          ],
          "answer": "Look for repeated interests, tasks you learn unusually fast, problems people ask you to solve, positive feedback you receive, and work that gives you energy even when it requires effort.",
          "details": "Do not rely on one personality test. Collect evidence from your experiences, results, feedback, and patterns over time, then choose one strength to develop deliberately."
        },
        {
          "id": "article-skills-stack",
          "title": "What is skill stacking?",
          "keywords": [
            "skill stacking",
            "combine skills",
            "complementary skills"
          ],
          "answer": "Skill stacking means combining a few abilities that strengthen one another. You may not be the world's best at each one, but the combination can make your contribution unusually useful.",
          "details": "For example, teaching knowledge plus communication, presentation design, technology, leadership, and content creation can create far more value than any one skill alone."
        },
        {
          "id": "article-skills-technology",
          "title": "How can technology multiply my strengths?",
          "keywords": [
            "technology multiplier",
            "technology skills",
            "use technology for growth"
          ],
          "answer": "Choose technology that improves what you already do well. A teacher can use digital presentations and video; a businessperson can use spreadsheets and online communication; a creator can use editing, design, and publishing tools.",
          "details": "Technology should make your work clearer, faster, more organised, or easier to share. It should strengthen thinking and service—not replace them."
        },
        {
          "id": "article-skills-visible",
          "title": "How do I make my skills visible without arrogance?",
          "keywords": [
            "show skills",
            "visibility",
            "portfolio",
            "self promotion arrogance"
          ],
          "answer": "Let completed work provide the evidence. Build a small portfolio, share useful lessons, document results, solve problems, and communicate your contribution clearly without exaggeration.",
          "details": "Do not become noisy without substance, but do not remain so hidden that nobody knows what you can contribute."
        },
        {
          "id": "article-skills-challenge",
          "title": "What is the article's 30-day challenge?",
          "keywords": [
            "30 day skill challenge",
            "four week challenge",
            "skill building plan"
          ],
          "answer": "Week 1: identify one strong potential. Week 2: practise one valuable skill. Week 3: add one useful technology skill. Week 4: create one small piece of visible evidence.",
          "details": "Keep it simple. The goal is not perfection in thirty days; it is to replace vague ambition with focused, visible progress."
        }
      ],
      "continue": [
        {
          "title": "Start a Practical ICT Skill",
          "url": "courses.html"
        },
        {
          "title": "Practise with a Free Tool",
          "url": "/digital-tools/index.html"
        },
        {
          "title": "Read More Personal Growth",
          "url": "faith-inspiration.html"
        }
      ]
    },
    {
      "id": "digital-skills-beginner",
      "title": "Digital Skills Every Beginner Should Learn",
      "subtitle": "Start small and build your future",
      "description": "A simple guide to the practical digital skills every beginner can learn step by step.",
      "url": "digital-skills-every-beginner-should-learn.html",
      "date": "2026-07-15",
      "dateLabel": "15 July 2026",
      "readingLabel": "Digital Skills · Personal Growth",
      "categories": [
        "technology",
        "faith"
      ],
      "primaryCategory": "technology",
      "featured": false,
      "image": "assets/images/social/social-digital-skills-beginner.jpg",
      "socialImage": "assets/images/social/social-digital-skills-beginner.jpg",
      "quote": "A confident digital learner is someone who is willing to keep learning.",
      "keywords": "digital skills beginner computer typing folders internet email word excel powerpoint smartphone cloud troubleshooting pdf online learning",
      "assistantSummary": "This article gives beginners a clear map of practical computer, internet, communication, Microsoft Office, smartphone, cloud, PDF, troubleshooting, and online-learning skills.",
      "keyIdeas": [
        "Start with basic computer confidence.",
        "Build typing, file-management, internet, email, Word, Excel, and PowerPoint skills step by step."
      ],
      "assistantQuestions": [],
      "continue": [
        {
          "title": "Start Computer Basics",
          "url": "courses.html#beginner-installments"
        },
        {
          "title": "Practise Typing",
          "url": "/digital-tools/typing-trainer.html"
        },
        {
          "title": "Explore ICT Lessons",
          "url": "courses.html"
        }
      ]
    },
    {
      "id": "why-technology",
      "title": "Why Technology Is Necessary in Today’s World",
      "subtitle": "Learn, adapt, and move forward",
      "description": "An inspiring guide to learning, adapting, and using technology to move forward.",
      "url": "why-technology-is-necessary.html",
      "date": "2026-07-12",
      "dateLabel": "12 July 2026",
      "readingLabel": "Technology · Personal Growth",
      "categories": [
        "technology"
      ],
      "primaryCategory": "technology",
      "featured": false,
      "image": "assets/images/social/social-why-technology.jpg",
      "socialImage": "assets/images/social/social-why-technology.jpg",
      "quote": "You do not need to become an expert today. You only need the courage to begin.",
      "keywords": "technology digital skills learning work business beginner future",
      "assistantSummary": "This article explains why technology matters in learning, work, communication, business, problem-solving, and adapting to a changing world.",
      "keyIdeas": [
        "Technology is part of everyday opportunity.",
        "Beginners can start small and learn progressively."
      ],
      "assistantQuestions": [],
      "continue": [
        {
          "title": "Choose a Digital Skill",
          "url": "digital-skills-every-beginner-should-learn.html"
        },
        {
          "title": "Open Digital Tools",
          "url": "/digital-tools/index.html"
        },
        {
          "title": "Join the ICT Community",
          "url": "https://chat.whatsapp.com/Fd9rDqOyxRrKUctGqfevmt"
        }
      ]
    },
    {
      "id": "small-beginnings",
      "title": "The Power of Small Beginnings",
      "subtitle": "Start where you are and use what you have",
      "description": "An encouragement to begin faithfully, use what you have, and grow one step at a time.",
      "url": "the-power-of-small-beginnings.html",
      "date": "2026-07-10",
      "dateLabel": "10 July 2026",
      "readingLabel": "Faith · Personal Growth",
      "categories": [
        "faith"
      ],
      "primaryCategory": "faith",
      "featured": false,
      "image": "assets/images/start-where-you-are.webp",
      "socialImage": "assets/images/social/social-small-beginnings.jpg",
      "quote": "A seed does not need to look like a harvest before it is planted.",
      "keywords": "small beginnings start purpose faith courage Zechariah",
      "assistantSummary": "This article encourages readers not to despise small beginnings, but to start with what they have, remain faithful, and allow steady action to grow into something meaningful.",
      "keyIdeas": [
        "Small beginnings can carry great potential.",
        "Faithful action matters more than waiting for perfect conditions."
      ],
      "assistantQuestions": [],
      "continue": [
        {
          "title": "Begin Computer Basics",
          "url": "courses.html#beginner-installments"
        },
        {
          "title": "Complete One Assignment",
          "url": "assignments.html"
        },
        {
          "title": "Read More Inspiration",
          "url": "faith-inspiration.html"
        }
      ]
    }
  ]
};

;
(() => {
  "use strict";
  const data = window.ATHANAS_LEARNING_CONTENT;
  if (!data) return;
  const normalize = (value = "") => value.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^a-z0-9]+/g, " ").trim();
  const track = (name, detail = {}) => document.dispatchEvent(new CustomEvent("athanas:track", { detail: { name, ...detail } }));
  const resolveUrl = (url = "") => /^(?:https?:|mailto:|tel:|#|\/)/i.test(url) ? url : `/${url.replace(/^\.\//, "")}`;
  const groupFor = (entry) => {
    if (/Digital Tool/i.test(entry.category)) return "Digital Tools";
    if (/Article/i.test(entry.category)) return "Articles";
    if (/Video Lesson/i.test(entry.category)) return "Videos";
    if (/Assignment|Upcoming Lesson/i.test(entry.category)) return "Lessons";
    if (/Download|Resource/i.test(entry.category) || /education|download/i.test(entry.title)) return "Resources";
    if (/YouTube/i.test(entry.title)) return "Videos";
    if (/ICT Lessons|Assignments/i.test(entry.title)) return "Lessons";
    return "Pages";
  };

  const rawEntries = [
    ...(data.pages || []),
    ...(data.articles || []).map((article) => ({ category: "Article", title: article.title, description: article.description, url: article.url, keywords: `${article.subtitle || ""} ${article.keywords || ""} ${(article.keyIdeas || []).join(" ")}` })),
    ...data.series.flatMap((series) => series.lessons.map((lesson) => ({
      category: lesson.status === "published" ? "Video Lesson" : "Upcoming Lesson",
      title: lesson.displayTitle,
      description: lesson.description,
      url: lesson.status === "published" ? lesson.videoUrl : `/youtube.html#${series.id}`,
      external: lesson.status === "published",
      keywords: `${series.title} ${lesson.title} ${lesson.session} ${lesson.status}`
    }))),
    ...data.assignments.map((item) => ({ category: "Assignment", title: item.title, description: item.description, url: `/assignments.html#${item.anchor}`, keywords: `${item.series} practice download submit` })),
    ...data.downloads.map((item) => ({ category: "Download", title: item.title, description: item.description, url: item.url, keywords: `${item.category} ${item.type} file resource` })),
    ...data.tools.map((item) => ({ category: item.status === "available" ? "Digital Tool" : "Upcoming Digital Tool", title: item.title, description: item.description, url: item.url || "/digital-tools/index.html", keywords: `${item.level} ${item.purpose} ${item.status}` }))
  ];
  const entries = Array.from(new Map(rawEntries.map((entry) => [`${entry.url}|${entry.title}`, entry])).values()).map((entry) => ({ ...entry, url: resolveUrl(entry.url), group: groupFor(entry), search: normalize(`${entry.title} ${entry.description} ${entry.keywords || ""} ${entry.category}`) }));

  function createUi() {
    if (document.getElementById("siteSearchDialog")) return;
    const wrap = document.createElement("div");
    wrap.className = "site-search-shell";
    wrap.innerHTML = `
      <div class="site-search-overlay" id="siteSearchDialog" aria-hidden="true">
        <button class="site-search-backdrop" type="button" aria-label="Close website search"></button>
        <section class="site-search-panel" role="dialog" aria-modal="true" aria-labelledby="siteSearchTitle">
          <header class="site-search-header">
            <div><p class="eyebrow dark">Search Athanas Inspires</p><h2 id="siteSearchTitle">Find anything across the website</h2></div>
            <button class="site-search-close" type="button" aria-label="Close website search">×</button>
          </header>
          <label class="site-search-field"><span class="sr-only">Search lessons, assignments, tools, articles, and pages</span><svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/></svg><input id="siteSearchInput" type="search" autocomplete="off" placeholder="Try ‘Excel Session 4’, ‘typing’, or ‘assignment’"></label>
          <div class="site-search-suggestions" aria-label="Popular searches"><button type="button" data-search-suggestion="computer basics">Computer Basics</button><button type="button" data-search-suggestion="microsoft word">Microsoft Word</button><button type="button" data-search-suggestion="excel assignments">Excel Assignments</button><button type="button" data-search-suggestion="qr code generator">QR Code Generator</button><button type="button" data-search-suggestion="build valuable skills">Build Valuable Skills</button></div>
          <div class="site-search-summary" id="siteSearchSummary">Start typing to search the whole platform.</div>
          <div class="site-search-results" id="siteSearchResults" role="listbox" aria-live="polite"></div>
        </section>
      </div>`;
    document.body.appendChild(wrap);
  }

  function addNavButton() {
    const nav = document.querySelector(".nav-links");
    if (!nav || nav.querySelector(".nav-site-search-item")) return;
    const item = document.createElement("li");
    item.className = "nav-site-search-item";
    item.innerHTML = '<button class="nav-site-search-btn" type="button" data-site-search-open aria-label="Search Athanas Inspires"><svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/></svg><span>Search</span></button>';
    const support = nav.querySelector(".nav-dropdown-support");
    if (support?.parentElement === nav) nav.insertBefore(item, support);
    else nav.appendChild(item);
  }

  let lastFocus = null;
  function openSearch(seed = "") {
    createUi();
    const overlay = document.getElementById("siteSearchDialog");
    const input = document.getElementById("siteSearchInput");
    lastFocus = document.activeElement;
    overlay.classList.add("is-open");
    overlay.setAttribute("aria-hidden", "false");
    document.body.classList.add("site-search-open");
    input.value = seed;
    render(seed);
    setTimeout(() => input.focus(), 50);
    track("search_open", { label: seed ? "Homepage search" : "Navigation search" });
  }

  function closeSearch() {
    const overlay = document.getElementById("siteSearchDialog");
    if (!overlay) return;
    overlay.classList.remove("is-open");
    overlay.setAttribute("aria-hidden", "true");
    document.body.classList.remove("site-search-open");
    lastFocus?.focus?.({ preventScroll: true });
  }

  function score(entry, query) {
    const terms = normalize(query).split(" ").filter(Boolean);
    if (!terms.length) return 0;
    let total = 0;
    terms.forEach((term) => {
      if (normalize(entry.title).includes(term)) total += 8;
      if (normalize(entry.category).includes(term)) total += 4;
      if (entry.search.includes(term)) total += 2;
    });
    if (entry.search.includes(normalize(query))) total += 12;
    return total;
  }

  function render(query) {
    const results = document.getElementById("siteSearchResults");
    const summary = document.getElementById("siteSearchSummary");
    if (!results || !summary) return;
    const clean = query.trim();
    if (!clean) {
      results.innerHTML = "";
      summary.textContent = "Start typing to search the whole platform.";
      return;
    }
    const matches = entries.map((entry) => ({ entry, score: score(entry, clean) })).filter((item) => item.score > 0).sort((a, b) => b.score - a.score || a.entry.title.localeCompare(b.entry.title)).slice(0, 18);
    summary.textContent = matches.length ? `${matches.length} result${matches.length === 1 ? "" : "s"} found for “${clean}”.` : `No results found for “${clean}”. Try a shorter phrase.`;
    const order = ["Lessons", "Digital Tools", "Articles", "Videos", "Resources", "Pages"];
    const grouped = matches.reduce((map, item) => { (map[item.entry.group] ||= []).push(item.entry); return map; }, {});
    results.innerHTML = order.filter((group) => grouped[group]?.length).map((group) => `<section class="site-search-group" aria-labelledby="search-group-${group.toLowerCase().replace(/[^a-z]+/g, "-")}"><h3 class="site-search-group-title" id="search-group-${group.toLowerCase().replace(/[^a-z]+/g, "-")}">${group}<span>${grouped[group].length}</span></h3><div class="site-search-group-list">${grouped[group].map((entry) => `<a class="site-search-result" role="option" href="${window.AthanasPaths?.resolve(entry.url) || entry.url}"${entry.external || /^https?:/.test(entry.url) ? ' target="_blank" rel="noopener noreferrer"' : ""} data-search-result data-track="search_result_open" data-track-label="${entry.title.replace(/"/g, "&quot;")}"><span class="site-search-category">${entry.category}</span><strong>${entry.title}</strong><p>${entry.description}</p><span class="site-search-arrow" aria-hidden="true">→</span></a>`).join("")}</div></section>`).join("");
    track("search_performed", { label: clean, value: matches.length });
  }

  document.addEventListener("DOMContentLoaded", () => {
    createUi();
    addNavButton();
    const overlay = document.getElementById("siteSearchDialog");
    const input = document.getElementById("siteSearchInput");
    overlay.querySelector(".site-search-close")?.addEventListener("click", closeSearch);
    overlay.querySelector(".site-search-backdrop")?.addEventListener("click", closeSearch);
    input?.addEventListener("input", () => render(input.value));
    overlay.querySelectorAll("[data-search-suggestion]").forEach((button) => button.addEventListener("click", () => { input.value = button.dataset.searchSuggestion; render(input.value); input.focus(); }));

    const params = new URLSearchParams(window.location.search);
    const requestedSearch = params.get("search");
    if (requestedSearch) setTimeout(() => openSearch(requestedSearch), 120);

    const homeInput = document.getElementById("homeSearch");
    if (homeInput) {
      homeInput.dataset.searchMode = "global";
      homeInput.placeholder = "Search the whole website...";
      homeInput.addEventListener("focus", () => openSearch(homeInput.value));
      homeInput.addEventListener("click", () => openSearch(homeInput.value));
      document.getElementById("clearSearch")?.addEventListener("click", () => { homeInput.value = ""; openSearch(); });
    }
  });

  document.addEventListener("click", (event) => {
    if (event.target.closest("[data-site-search-open]")) openSearch();
    if (event.target.closest("[data-search-result]")) closeSearch();
  });
  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape" && document.body.classList.contains("site-search-open")) closeSearch();
    if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "k") { event.preventDefault(); openSearch(); }
  });
})();

;
(() => {
  "use strict";
  const send = (name, params = {}) => {
    if (typeof window.gtag !== "function") return;
    window.gtag("event", name, {
      event_category: params.category || "engagement",
      event_label: params.label || "",
      value: Number.isFinite(Number(params.value)) ? Number(params.value) : undefined,
      page_path: window.location.pathname,
      transport_type: "beacon"
    });
  };

  document.addEventListener("athanas:track", (event) => {
    const detail = event.detail || {};
    if (detail.name) send(detail.name, detail);
  });

  document.addEventListener("click", (event) => {
    const target = event.target.closest("a,button");
    if (!target) return;
    const href = target.getAttribute("href") || "";
    const label = target.dataset.trackLabel || target.textContent.trim().replace(/\s+/g, " ").slice(0, 120);
    if (target.dataset.track) send(target.dataset.track, { label });
    else if (/youtube\.com|youtu\.be/.test(href)) send(href.includes("sub_confirmation") ? "youtube_subscribe_click" : "youtube_video_click", { label });
    else if (/wa\.me|chat\.whatsapp\.com/.test(href)) send(href.includes("chat.whatsapp") ? "whatsapp_community_click" : "whatsapp_support_click", { label });
    else if (target.hasAttribute("download") || /assets\/downloads\//.test(href)) send("file_download", { label: label || href.split("/").pop() });
    else if (target.closest(".ytp-slider-controls")) send("testimonial_control", { label });
  });

  document.addEventListener("DOMContentLoaded", () => {
    document.querySelectorAll("audio").forEach((audio) => {
      const name = audio.id || "website_audio";
      let started = false;
      audio.addEventListener("play", () => { if (!started) { started = true; send("welcome_audio_play", { label: name }); } });
      audio.addEventListener("ended", () => send("welcome_audio_complete", { label: name }));
    });
  });
})();

;
(() => {
  "use strict";
  const KEY = "athanasCookieChoiceV1";
  let choice = null;
  try { choice = localStorage.getItem(KEY); } catch (_) {}

  function update(value) {
    try { localStorage.setItem(KEY, value); } catch (_) {}
    if (typeof window.gtag === "function") {
      window.gtag("consent", "update", {
        analytics_storage: value === "granted" ? "granted" : "denied",
        ad_storage: "denied",
        ad_user_data: "denied",
        ad_personalization: "denied"
      });
    }
    document.querySelector(".cookie-consent")?.remove();
  }

  function show() {
    if (choice) return;
    const banner = document.createElement("aside");
    banner.className = "cookie-consent";
    banner.setAttribute("data-floating-control", "consent");
    banner.setAttribute("aria-label", "Cookie and analytics choices");
    banner.innerHTML = `<div><strong>Your privacy choices</strong><p>Athanas Inspires uses essential browser storage for features such as audio preferences and the Assistant. Optional analytics help us understand which lessons and tools are useful.</p><a href="cookie-policy.html">Read cookie information</a></div><div class="cookie-consent-actions"><button type="button" data-consent="denied">Essential Only</button><button type="button" class="is-primary" data-consent="granted">Accept Analytics</button></div>`;
    banner.addEventListener("click", (event) => {
      const button = event.target.closest("[data-consent]");
      if (button) update(button.dataset.consent);
    });
    document.body.appendChild(banner);
  }

  document.addEventListener("DOMContentLoaded", show);
  window.ATHANAS_CONSENT = { update };
})();

;
(() => {
  "use strict";
  const VERSION = "20260729-assistant-scroll-2";
  let loadingPromise = null;

  const track = (name, detail = {}) => document.dispatchEvent(new CustomEvent("athanas:track", { detail: { name, ...detail } }));

  function addLauncher() {
    if (document.querySelector(".ai-loader-launcher, .ai-floating-button")) return;
    const button = document.createElement("button");
    button.type = "button";
    button.className = "ai-loader-launcher";
    button.setAttribute("aria-label", "Open Athanas Inspires AI Assistant");
    button.setAttribute("data-floating-control", "assistant");
    button.innerHTML = '<span aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M12 3a6 6 0 0 0-6 6v1.2A3.5 3.5 0 0 0 4 13.4V17a2 2 0 0 0 2 2h2l1.6 2h4.8L16 19h2a2 2 0 0 0 2-2v-3.6a3.5 3.5 0 0 0-2-3.2V9a6 6 0 0 0-6-6Z"/><path d="M9 12h.01M15 12h.01M9.5 16h5"/></svg></span><span class="ai-loader-label">AI Assistant</span>';
    button.addEventListener("click", () => loadAssistant(true));
    document.body.appendChild(button);
  }

  function loadStylesheet(href) {
    return new Promise((resolve, reject) => {
      const existing = document.querySelector(`link[href^="${href}"]`);
      if (existing) {
        if (existing.sheet) resolve();
        else { existing.addEventListener("load", resolve, { once: true }); existing.addEventListener("error", reject, { once: true }); }
        return;
      }
      const link = document.createElement("link");
      link.rel = "stylesheet";
      link.href = `${href}?v=${VERSION}`;
      link.onload = resolve;
      link.onerror = reject;
      document.head.appendChild(link);
    });
  }

  function loadScript(src) {
    return new Promise((resolve, reject) => {
      if (document.querySelector(`script[src^="${src}"]`)) return resolve();
      const script = document.createElement("script");
      script.src = `${src}?v=${VERSION}`;
      script.defer = true;
      script.onload = resolve;
      script.onerror = reject;
      document.body.appendChild(script);
    });
  }

  async function loadAssistant(openAfterLoad = false) {
    if (window.ATHANAS_ASSISTANT_DATA && document.querySelector(".ai-assistant-shell")) {
      document.querySelector(".ai-floating-button")?.click();
      return;
    }
    window.__ATHANAS_ASSISTANT_AUTO_OPEN = Boolean(openAfterLoad);
    document.querySelector(".ai-loader-launcher")?.classList.add("is-loading");
    if (!loadingPromise) {
      loadingPromise = (async () => {
        await loadStylesheet(window.AthanasPaths?.resolve("/css/bot.min.css") || "/css/bot.min.css");
        await loadScript(window.AthanasPaths?.resolve("/js/bot-data.min.js") || "/js/bot-data.min.js");
        await loadScript(window.AthanasPaths?.resolve("/js/assistant-learning-sync.js") || "/js/assistant-learning-sync.js");
        await loadScript(window.AthanasPaths?.resolve("/js/assistant-knowledge-base.js") || "/js/assistant-knowledge-base.js");
        await loadScript(window.AthanasPaths?.resolve("/js/bot.min.js") || "/js/bot.min.js");
        document.querySelector(".ai-loader-launcher")?.remove();
        track("assistant_loaded", { label: "AI Assistant" });
      })().catch((error) => {
        console.error("Assistant could not load", error);
        const launcher = document.querySelector(".ai-loader-launcher");
        launcher?.classList.remove("is-loading");
        launcher?.setAttribute("aria-label", "Try loading the AI Assistant again");
        loadingPromise = null;
      });
    }
    return loadingPromise;
  }

  document.addEventListener("click", (event) => {
    const trigger = event.target.closest("[data-ai-open]");
    if (!trigger || document.querySelector(".ai-assistant-shell")) return;
    event.preventDefault();
    event.stopImmediatePropagation();
    track("assistant_open", { label: "Navigation support link" });
    loadAssistant(true);
  }, true);

  document.addEventListener("DOMContentLoaded", addLauncher);
})();

;
(() => {
  "use strict";

  const normalisePath = (value) => {
    const path = String(value || "/").replace(/\/index\.html$/i, "/");
    return path.length > 1 ? path.replace(/\/$/, "") : path;
  };

  const refreshActiveNavigation = () => {
    const currentPath = normalisePath(window.location.pathname);
    const currentHash = window.location.hash || "";
    const cards = Array.from(document.querySelectorAll(".nav-card-menu a.nav-menu-card"));
    const primaryLinks = Array.from(document.querySelectorAll(".nav-links > li > a"));

    primaryLinks.forEach((link) => {
      let url;
      try { url = new URL(link.href, window.location.href); } catch (error) { return; }
      const active = normalisePath(url.pathname) === currentPath && (url.hash || "") === currentHash;
      link.classList.toggle("active", active);
      if (active) link.setAttribute("aria-current", "page");
      else link.removeAttribute("aria-current");
    });

    document.querySelectorAll(".nav-dropdown-toggle.active").forEach((toggle) => toggle.classList.remove("active"));
    cards.forEach((link) => {
      let url;
      try { url = new URL(link.href, window.location.href); } catch (error) { return; }
      const samePath = normalisePath(url.pathname) === currentPath;
      const exactHash = (url.hash || "") === currentHash;
      const active = samePath && exactHash;
      link.classList.toggle("active", active);
      if (active) link.setAttribute("aria-current", "page");
      else link.removeAttribute("aria-current");

      if (samePath) {
        link.closest(".nav-dropdown")?.querySelector(":scope > .nav-dropdown-toggle")?.classList.add("active");
      }
    });
  };

  const preparePremiumReveals = () => {
    const targets = Array.from(document.querySelectorAll([
      ".start-here-mini-card",
      ".home-editorial-card",
      ".latest-update-row",
      ".digital-tool-row",
      ".premium-discovery-panel",
      ".premium-site-footer .footer-main > *",
      ".premium-site-footer .footer-community-strip"
    ].join(",")));

    const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reducedMotion || !("IntersectionObserver" in window)) {
      targets.forEach((target) => target.classList.add("premium-reveal", "is-visible"));
      return;
    }

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (!entry.isIntersecting) return;
        entry.target.classList.add("is-visible");
        observer.unobserve(entry.target);
      });
    }, { threshold: 0.1, rootMargin: "0px 0px -34px" });

    targets.forEach((target, index) => {
      target.classList.add("premium-reveal");
      target.style.setProperty("--premium-reveal-delay", `${Math.min((index % 4) * 70, 210)}ms`);
      observer.observe(target);
    });
  };

  document.addEventListener("DOMContentLoaded", () => {
    refreshActiveNavigation();
    preparePremiumReveals();
  });
  window.addEventListener("hashchange", refreshActiveNavigation);
})();

;
(() => {
  "use strict";

  const NEW_TARGETS = [
    ".eyebrow",
    ".home-editorial-meta > span",
    ".home-editorial-image > span",
    ".latest-update-meta b",
    ".latest-new-badge",
    ".inspiration-link-row small",
    ".new-label",
    ".question-tag"
  ].join(",");

  const wrapNewWord = (root = document) => {
    const targets = [];
    if (root.nodeType === Node.ELEMENT_NODE && root.matches?.(NEW_TARGETS)) targets.push(root);
    root.querySelectorAll?.(NEW_TARGETS).forEach((element) => targets.push(element));

    targets.forEach((element) => {
      if (element.querySelector(".new-blink")) return;
      const walker = document.createTreeWalker(element, NodeFilter.SHOW_TEXT);
      let textNode;
      while ((textNode = walker.nextNode())) {
        const match = textNode.nodeValue.match(/\bNew\b/i);
        if (!match) continue;
        const value = textNode.nodeValue;
        const index = match.index;
        const fragment = document.createDocumentFragment();
        fragment.append(value.slice(0, index));
        const blink = document.createElement("span");
        blink.className = "new-blink";
        blink.textContent = value.slice(index, index + match[0].length);
        fragment.append(blink, value.slice(index + match[0].length));
        textNode.replaceWith(fragment);
        break;
      }
    });
  };

  const prepareNewPanels = () => {
    const panels = Array.from(document.querySelectorAll(".premium-discovery-panel"));
    if (!panels.length) return;

    const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reduced || !("IntersectionObserver" in window)) {
      panels.forEach((panel) => panel.classList.add("premium-reveal", "is-visible"));
      return;
    }

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (!entry.isIntersecting) return;
        entry.target.classList.add("is-visible");
        observer.unobserve(entry.target);
      });
    }, { threshold: 0.12, rootMargin: "0px 0px -28px" });

    panels.forEach((panel, index) => {
      panel.classList.add("premium-reveal");
      panel.style.setProperty("--premium-reveal-delay", `${index * 90}ms`);
      observer.observe(panel);
    });
  };

  document.addEventListener("DOMContentLoaded", () => {
    wrapNewWord();
    prepareNewPanels();

    const observer = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => mutation.addedNodes.forEach((node) => {
        if (node.nodeType === Node.ELEMENT_NODE) wrapNewWord(node);
      }));
    });
    observer.observe(document.body, { childList: true, subtree: true });
  });
})();

;
(() => {
  "use strict";

  const SELECTORS = {
    practiceMain: ".typing-page, .shortcut-page, .quiz-page",
    practiceApp: ".typing-app-card, .shortcut-app-card, .quiz-app-card"
  };

  function enhanceNewArticleBadge(root = document) {
    const badges = [];
    if (root.nodeType === Node.ELEMENT_NODE && root.matches?.(".home-editorial-image > span")) badges.push(root);
    root.querySelectorAll?.(".home-editorial-image > span").forEach((item) => badges.push(item));
    badges.forEach((badge) => {
      if (badge.classList.contains("home-editorial-badge")) return;
      badge.classList.add("home-editorial-badge");
      badge.setAttribute("aria-label", "New article");
      badge.innerHTML = '<span class="new-blink">NEW</span><span>ARTICLE</span>';
    });
  }

  function structureDigitalToolsMenu() {
    document.querySelectorAll("#digital-tools-menu").forEach((menu) => {
      const items = Array.from(menu.children).filter((item) => item.tagName === "LI" && !item.classList.contains("nav-tools-group-label"));
      if (!items.length) return;

      items[0].classList.add("nav-tools-overview");
      if (!menu.querySelector(".nav-tools-group-label")) {
        const label = document.createElement("li");
        label.className = "nav-tools-group-label";
        label.setAttribute("aria-hidden", "true");
        label.textContent = "Tool Categories";
        items[0].insertAdjacentElement("afterend", label);
      }

      items.slice(1).forEach((item) => item.classList.add("nav-tools-category"));
      menu.setAttribute("data-hierarchy-ready", "true");
    });
  }

  function focusIcon() {
    return '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 3H4a1 1 0 0 0-1 1v4M16 3h4a1 1 0 0 1 1 1v4M8 21H4a1 1 0 0 1-1-1v-4M16 21h4a1 1 0 0 0 1-1v-4"/></svg>';
  }

  function initPracticeFocusMode() {
    const main = document.querySelector(SELECTORS.practiceMain);
    if (!main || main.querySelector(".tool-focus-toolbar")) return;
    const app = main.querySelector(SELECTORS.practiceApp);
    if (!app) return;

    document.body.classList.add("tool-practice-page");

    const toolbar = document.createElement("div");
    toolbar.className = "tool-focus-toolbar";
    toolbar.innerHTML = `
      <div class="tool-focus-intro">
        <span aria-hidden="true">◎</span>
        <div><strong>Practice Workspace</strong><small>Use Focus Mode to remove distractions and fit more practice on screen.</small></div>
      </div>
      <div class="tool-focus-actions">
        <button class="tool-focus-button" type="button" aria-pressed="false"><span>Focus Mode</span><b aria-hidden="true">→</b></button>
        <button class="tool-fullscreen-button" type="button" aria-label="Open browser full screen" title="Open browser full screen">${focusIcon()}</button>
      </div>
      <span class="tool-focus-status" role="status" aria-live="polite"></span>`;
    main.insertBefore(toolbar, app);

    const focusButton = toolbar.querySelector(".tool-focus-button");
    const focusLabel = focusButton.querySelector("span");
    const focusArrow = focusButton.querySelector("b");
    const fullscreenButton = toolbar.querySelector(".tool-fullscreen-button");
    const status = toolbar.querySelector(".tool-focus-status");
    let previousScroll = 0;

    const setFocusMode = (enabled, { announce = true } = {}) => {
      if (enabled === document.body.classList.contains("tool-focus-mode")) return;
      if (enabled) previousScroll = window.scrollY;
      document.body.classList.toggle("tool-focus-mode", enabled);
      document.documentElement.classList.toggle("tool-focus-mode", enabled);
      focusButton.setAttribute("aria-pressed", String(enabled));
      focusLabel.textContent = enabled ? "Exit Focus Mode" : "Focus Mode";
      focusArrow.textContent = enabled ? "×" : "→";
      if (announce) status.textContent = enabled ? "Focus Mode opened. Navigation and extra panels are hidden." : "Focus Mode closed. Full page navigation restored.";
      if (enabled) {
        requestAnimationFrame(() => {
          main.scrollTop = 0;
          focusButton.focus({ preventScroll: true });
        });
      } else {
        requestAnimationFrame(() => window.scrollTo({ top: previousScroll, behavior: "smooth" }));
      }
    };

    focusButton.addEventListener("click", () => setFocusMode(!document.body.classList.contains("tool-focus-mode")));

    fullscreenButton.addEventListener("click", async () => {
      try {
        if (!document.fullscreenElement) {
          await document.documentElement.requestFullscreen();
          status.textContent = "Browser full screen opened. Press Escape to leave full screen.";
        } else {
          await document.exitFullscreen();
        }
      } catch (error) {
        status.textContent = "Browser full screen is not available here. Focus Mode still works normally.";
      }
    });

    document.addEventListener("fullscreenchange", () => {
      const active = Boolean(document.fullscreenElement);
      fullscreenButton.setAttribute("aria-label", active ? "Exit browser full screen" : "Open browser full screen");
      fullscreenButton.title = active ? "Exit browser full screen" : "Open browser full screen";
      fullscreenButton.classList.toggle("is-active", active);
    });

    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && document.body.classList.contains("tool-focus-mode") && !document.fullscreenElement) {
        setFocusMode(false);
      }
    });
  }

  function boot() {
    enhanceNewArticleBadge();
    structureDigitalToolsMenu();
    initPracticeFocusMode();

    const observer = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => {
        mutation.addedNodes.forEach((node) => {
          if (node.nodeType !== Node.ELEMENT_NODE) return;
          enhanceNewArticleBadge(node);
          if (node.matches?.("#digital-tools-menu") || node.querySelector?.("#digital-tools-menu")) structureDigitalToolsMenu();
        });
      });
    });
    observer.observe(document.body, { childList: true, subtree: true });
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot, { once: true });
  else boot();
})();

;
(() => {
  "use strict";

  const STORAGE = {
    assignment: "athanasAssignmentProgressV2",
    recentTools: "athanasRecentToolsV2"
  };

  const toolConfig = {
    typing: {
      selector: ".typing-page",
      app: ".typing-app-card",
      side: ".typing-side-panel",
      title: "Typing Speed Trainer",
      icon: "⚡",
      help: [
        ["1", "Choose a practice type", "Begin with a short passage and focus on accuracy before speed."],
        ["2", "Choose a time", "Use 30 seconds for a quick warm-up or 60 seconds for normal practice."],
        ["3", "Start calmly", "Keep your eyes on the passage and type without rushing."],
        ["4", "Review the result", "Use WPM, accuracy and mistakes to choose your next goal."]
      ]
    },
    shortcuts: {
      selector: ".shortcut-page",
      app: ".shortcut-app-card",
      side: ".shortcut-side-panel",
      title: "Computer Shortcut Keys Trainer",
      icon: "⌨",
      help: [
        ["1", "Choose a category", "Practise common, Windows, Word, Excel or browser shortcuts."],
        ["2", "Answer the challenge", "Click an option or press the real shortcut on a physical keyboard."],
        ["3", "Open the explanation", "Use the feedback to understand where the shortcut is useful."],
        ["4", "Build a streak", "Repeat difficult shortcuts until the correct keys feel natural."]
      ]
    },
    quiz: {
      selector: ".quiz-page",
      app: ".quiz-app-card",
      side: ".quiz-side-panel",
      title: "General ICT Quiz Game",
      icon: "🎮",
      help: [
        ["1", "Choose a category", "Select All Topics or focus on one ICT area."],
        ["2", "Answer one question", "Read carefully and select the best answer."],
        ["3", "Learn from feedback", "Correct answers and explanations appear immediately."],
        ["4", "Review your result", "Use your final score and mistakes to decide what to practise next."]
      ]
    },
    calculator: {
      selector: ".calculator-page",
      app: ".calculator-shell",
      side: ".calculator-side-panel",
      title: "Scientific Calculator",
      icon: "🧮",
      help: [
        ["B", "Basic mode", "Use numbers, brackets, percentages and everyday operations."],
        ["SCI", "Scientific mode", "Reveal trigonometry, logarithms, powers, roots and constants."],
        ["DEG", "Angle mode", "DEG is suitable for most school trigonometry questions. Switch to RAD when required."],
        ["↺", "Calculation history", "Select a previous answer to load it back into the calculator."]
      ]
    }
  };

  const qs = (selector, root = document) => root.querySelector(selector);
  const qsa = (selector, root = document) => Array.from(root.querySelectorAll(selector));
  const escapeHtml = (value = "") => String(value).replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" }[char]));

  function readJSON(key, fallback) {
    try { return JSON.parse(localStorage.getItem(key) || "") || fallback; }
    catch (_) { return fallback; }
  }
  function writeJSON(key, value) {
    try { localStorage.setItem(key, JSON.stringify(value)); } catch (_) { /* storage can be blocked */ }
  }

  function createModal({ id, title, kicker = "Athanas Inspires Help", body, wide = false }) {
    let modal = document.getElementById(id);
    if (modal) return modal;
    modal = document.createElement("div");
    modal.className = "plw-modal";
    modal.id = id;
    modal.hidden = true;
    modal.innerHTML = `
      <button class="plw-modal-backdrop" type="button" aria-label="Close help"></button>
      <section class="plw-modal-panel${wide ? " plw-modal-panel--wide" : ""}" role="dialog" aria-modal="true" aria-labelledby="${id}Title">
        <div class="plw-modal-head">
          <div><span class="plw-modal-kicker">${escapeHtml(kicker)}</span><h2 id="${id}Title">${escapeHtml(title)}</h2></div>
          <button class="plw-modal-close" type="button" aria-label="Close help">×</button>
        </div>
        <div class="plw-modal-body">${body}</div>
      </section>`;
    document.body.appendChild(modal);

    const close = () => {
      modal.hidden = true;
      document.body.classList.remove("plw-modal-open");
      const returnId = modal.dataset.returnFocus;
      if (returnId) document.getElementById(returnId)?.focus({ preventScroll: true });
    };
    modal.querySelector(".plw-modal-backdrop").addEventListener("click", close);
    modal.querySelector(".plw-modal-close").addEventListener("click", close);
    modal.addEventListener("keydown", (event) => {
      if (event.key === "Escape") close();
    });
    modal.openFrom = (button) => {
      if (button && !button.id) button.id = `${id}Trigger`;
      modal.dataset.returnFocus = button?.id || "";
      modal.hidden = false;
      document.body.classList.add("plw-modal-open");
      requestAnimationFrame(() => modal.querySelector(".plw-modal-close")?.focus({ preventScroll: true }));
    };
    modal.closeModal = close;
    return modal;
  }

  function focusSvg() {
    return '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 3H4a1 1 0 0 0-1 1v4M16 3h4a1 1 0 0 1 1 1v4M8 21H4a1 1 0 0 1-1-1v-4M16 21h4a1 1 0 0 0 1-1v-4"/></svg>';
  }
  function helpSvg() {
    return '<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M9.8 9a2.4 2.4 0 1 1 3.3 2.2c-.8.4-1.1.9-1.1 1.8M12 17h.01"/></svg>';
  }
  function toolsSvg() {
    return '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 4h6v6H4zM14 4h6v6h-6zM4 14h6v6H4zM14 14h6v6h-6z"/></svg>';
  }

  function detectTool() {
    return Object.entries(toolConfig).find(([, config]) => qs(config.selector)) || null;
  }

  function recordRecentTool(key, config) {
    const recent = readJSON(STORAGE.recentTools, []);
    const currentUrl = location.pathname.split("/").pop() || "index.html";
    const item = {
      id: key,
      title: config.title,
      icon: config.icon,
      url: currentUrl,
      path: location.pathname,
      lastVisited: Date.now(),
      detail: "Continue from your latest practice session"
    };
    const updated = [item, ...recent.filter((entry) => entry.id !== key)].slice(0, 5);
    writeJSON(STORAGE.recentTools, updated);

    const scoreTargets = qsa("#typingBestPill,#shortcutBestPill,#bestScorePill,#resultDisplay");
    if (scoreTargets.length) {
      const update = () => {
        const saved = readJSON(STORAGE.recentTools, []);
        const entry = saved.find((value) => value.id === key);
        if (!entry) return;
        const detail = scoreTargets.map((el) => el.textContent.trim()).find(Boolean);
        if (detail) entry.detail = detail;
        writeJSON(STORAGE.recentTools, saved);
      };
      const observer = new MutationObserver(update);
      scoreTargets.forEach((el) => observer.observe(el, { childList: true, characterData: true, subtree: true }));
      update();
    }
  }

  function buildToolHelp(config) {
    const body = `<p>Use this compact guide whenever you need a reminder. The activity remains open behind the help window.</p><div class="plw-help-list">${config.help.map(([step, title, copy]) => `
      <div class="plw-help-item"><span>${escapeHtml(step)}</span><div><strong>${escapeHtml(title)}</strong><small>${escapeHtml(copy)}</small></div></div>`).join("")}</div>`;
    return createModal({ id: "plwToolHelpModal", title: `${config.title} Help`, body });
  }

  function moveSecondaryCards(main, side) {
    if (!side || main.querySelector(".plw-tool-more")) return;
    const extras = Array.from(side.children).slice(1);
    if (!extras.length) return;
    const details = document.createElement("details");
    details.className = "plw-tool-more";
    details.innerHTML = '<summary>Tips, guidance and extra learning options</summary><div class="plw-tool-more-body"></div>';
    const body = details.querySelector(".plw-tool-more-body");
    extras.forEach((card) => body.appendChild(card));
    main.appendChild(details);
  }

  function initToolPage() {
    const detected = detectTool();
    if (!detected) return;
    const [key, config] = detected;
    const main = qs(config.selector);
    const app = qs(config.app, main);
    const side = qs(config.side, main);
    if (!main || !app || main.querySelector(".plw-tool-commandbar")) return;

    document.body.classList.add("plw-tool-page", `plw-tool-${key}`);
    if (key === "calculator") document.body.classList.add("plw-calculator-basic");

    const oldToolbar = qs(".tool-focus-toolbar", main);
    const oldFocus = oldToolbar?.querySelector(".tool-focus-button");
    const oldFullscreen = oldToolbar?.querySelector(".tool-fullscreen-button");

    const commandbar = document.createElement("div");
    commandbar.className = "plw-tool-commandbar";
    commandbar.innerHTML = `
      <div class="plw-tool-identity">
        <span class="plw-tool-icon" aria-hidden="true">${config.icon}</span>
        <div><span class="plw-tool-breadcrumb"><a href="index.html">Digital Tools</a><b aria-hidden="true">›</b>${escapeHtml(config.title)}</span><strong class="plw-tool-title">${escapeHtml(config.title)}</strong></div>
      </div>
      <div class="plw-tool-actions">
        <a class="plw-tool-action" href="index.html" title="Explore more tools">${toolsSvg()}<span>More Tools</span></a>
        <button class="plw-tool-action plw-tool-help" type="button" title="Open help">${helpSvg()}<span>Help</span></button>
        <button class="plw-tool-action plw-tool-action--focus" type="button" aria-pressed="false" title="Focus Mode">${focusSvg()}<span>Focus Mode</span></button>
        <button class="plw-tool-action plw-tool-action--icon plw-tool-fullscreen" type="button" title="Browser full screen" aria-label="Browser full screen">${focusSvg()}</button>
      </div>`;
    main.insertBefore(commandbar, oldToolbar || app);

    const helpModal = buildToolHelp(config);
    const helpButton = commandbar.querySelector(".plw-tool-help");
    helpButton.addEventListener("click", () => helpModal.openFrom(helpButton));

    const focusButton = commandbar.querySelector(".plw-tool-action--focus");
    const focusText = focusButton.querySelector("span");
    const updateFocus = () => {
      const active = document.body.classList.contains("tool-focus-mode");
      focusButton.setAttribute("aria-pressed", String(active));
      focusText.textContent = active ? "Exit Focus" : "Focus Mode";
      focusButton.title = active ? "Exit Focus Mode" : "Focus Mode";
    };
    focusButton.addEventListener("click", () => {
      if (oldFocus) oldFocus.click();
      else {
        document.body.classList.toggle("tool-focus-mode");
        document.documentElement.classList.toggle("tool-focus-mode", document.body.classList.contains("tool-focus-mode"));
      }
      setTimeout(updateFocus, 0);
    });

    const fullscreenButton = commandbar.querySelector(".plw-tool-fullscreen");
    fullscreenButton.addEventListener("click", async () => {
      if (oldFullscreen) { oldFullscreen.click(); return; }
      try {
        if (!document.fullscreenElement) await document.documentElement.requestFullscreen();
        else await document.exitFullscreen();
      } catch (_) { /* unsupported */ }
    });
    document.addEventListener("fullscreenchange", () => {
      fullscreenButton.classList.toggle("is-active", Boolean(document.fullscreenElement));
    });
    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && document.body.classList.contains("tool-focus-mode") && !document.fullscreenElement && !document.body.classList.contains("plw-modal-open")) {
        if (oldFocus) oldFocus.click();
        else {
          document.body.classList.remove("tool-focus-mode");
          document.documentElement.classList.remove("tool-focus-mode");
        }
        setTimeout(updateFocus, 0);
      }
    });

    moveSecondaryCards(main, side);
    recordRecentTool(key, config);

    const startSelectors = key === "typing" ? "#startTypingBtn" : key === "shortcuts" ? "#nextShortcut,#restartShortcuts" : key === "quiz" ? "#nextQuestion,#restartQuiz" : ".calc-btn";
    qsa(startSelectors, main).forEach((button) => button.addEventListener("click", () => document.body.classList.add("plw-tool-session-active"), { once: true }));

    if (key === "calculator") { initCalculatorModes(main); enhanceCalculatorHistory(main); }
    if (key === "shortcuts") { initShortcutKeyboardMode(main); enhanceCompactFeedback(main, "shortcut"); }
    if (key === "quiz") { enhanceQuizResults(main); enhanceCompactFeedback(main, "quiz"); }
    updateFocus();
  }


  function normaliseShortcutText(value) {
    return String(value || "")
      .replace(/Windows/gi, "Meta")
      .replace(/Control/gi, "Ctrl")
      .replace(/\s+/g, "")
      .toLowerCase();
  }

  function shortcutFromEvent(event) {
    const parts = [];
    if (event.ctrlKey) parts.push("Ctrl");
    if (event.altKey) parts.push("Alt");
    if (event.shiftKey) parts.push("Shift");
    if (event.metaKey) parts.push("Meta");
    const modifierKeys = ["Control", "Alt", "Shift", "Meta"];
    if (!modifierKeys.includes(event.key)) {
      const keyMap = { " ": "Space", "Escape": "Esc", "ArrowUp": "↑", "ArrowDown": "↓", "ArrowLeft": "←", "ArrowRight": "→" };
      const key = keyMap[event.key] || (event.key.length === 1 ? event.key.toUpperCase() : event.key);
      parts.push(key);
    }
    return parts.join(" + ");
  }

  function enhanceCompactFeedback(main, type) {
    const feedback = qs(type === "shortcut" ? "#shortcutFeedback" : "#feedbackBox", main);
    if (!feedback) return;
    let processing = false;
    const compact = () => {
      if (processing || feedback.querySelector(".plw-feedback-details") || !feedback.textContent.trim()) return;
      const first = feedback.querySelector(":scope > strong");
      if (!first) return;
      const rest = [];
      let node = first.nextSibling;
      while (node) { const next = node.nextSibling; rest.push(node); node = next; }
      if (!rest.length) return;
      processing = true;
      const details = document.createElement("details");
      details.className = "plw-feedback-details";
      details.innerHTML = `<summary>${type === "shortcut" ? "See shortcut and explanation" : "See explanation"}</summary><div></div>`;
      rest.forEach((item) => details.lastElementChild.appendChild(item));
      feedback.appendChild(details);
      processing = false;
    };
    const observer = new MutationObserver(compact);
    observer.observe(feedback, { childList: true, subtree: false });
    compact();
  }

  function enhanceQuizResults(main) {
    const resultCard = qs("#resultCard", main);
    const answerGrid = qs("#answerGrid", main);
    const restart = qs("#restartQuiz", main);
    const tabs = qs("#categoryTabs", main);
    const resultCircle = qs("#resultCircle", main);
    const resultSummary = qs("#resultSummary", main);
    if (!resultCard || resultCard.querySelector(".plw-quiz-result-actions")) return;
    const mistakes = [];
    const actions = document.createElement("div");
    actions.className = "plw-quiz-result-actions";
    actions.innerHTML = '<button type="button" data-result-action="retry">Try Again</button><button type="button" data-result-action="review">Review Mistakes</button><button type="button" data-result-action="category">Another Category</button>';
    resultCard.appendChild(actions);
    const insights = document.createElement("div");
    insights.className = "plw-result-insights";
    insights.innerHTML = '<span><b>Strength:</b> Keep building your ICT confidence.</span><span><b>Next focus:</b> Complete the quiz to receive guidance.</span>';
    resultCard.insertBefore(insights, actions);
    const updateReadyState = () => {
      const text = (resultSummary?.textContent || "").trim();
      const ready = Boolean(text) && !/final result will appear|answer the questions/i.test(text);
      resultCard.classList.toggle("plw-result-ready", ready);
    };
    if (resultSummary) new MutationObserver(updateReadyState).observe(resultSummary, { childList: true, characterData: true, subtree: true });
    updateReadyState();

    answerGrid?.addEventListener("click", (event) => {
      const button = event.target.closest("button");
      if (!button) return;
      setTimeout(() => {
        try {
          const current = state.questions[state.currentIndex];
          if (current && state.selectedAnswer && state.selectedAnswer !== current.answer) {
            mistakes.push({ question: current.question, selected: state.selectedAnswer, answer: current.answer, explanation: current.explanation, category: current.category });
          }
        } catch (_) { /* older quiz build */ }
      }, 0);
    });

    const reviewBody = () => mistakes.length ? `<div class="plw-help-list">${mistakes.map((item, index) => `<div class="plw-help-item"><span>${index + 1}</span><div><strong>${escapeHtml(item.question)}</strong><small>Your answer: ${escapeHtml(item.selected)}<br>Correct answer: ${escapeHtml(item.answer)}<br>${escapeHtml(item.explanation)}</small></div></div>`).join("")}</div>` : '<p>No recorded mistakes yet. Complete a quiz round, then return here to review.</p>';
    let reviewModal = null;
    actions.addEventListener("click", (event) => {
      const button = event.target.closest("button");
      if (!button) return;
      if (button.dataset.resultAction === "retry") restart?.click();
      if (button.dataset.resultAction === "category") tabs?.scrollIntoView({ behavior: "smooth", block: "center" });
      if (button.dataset.resultAction === "review") {
        reviewModal?.remove();
        reviewModal = createModal({ id: "plwQuizReviewModal", title: "Review Your Mistakes", body: reviewBody(), wide: true });
        reviewModal.openFrom(button);
      }
    });

    if (resultCircle) {
      const updateInsights = () => {
        const percent = Number.parseInt(resultCircle.textContent, 10) || 0;
        const strength = percent >= 80 ? "Strong ICT understanding." : percent >= 60 ? "Good foundation with room to grow." : "You started — that is the first win.";
        const categories = [...new Set(mistakes.map((item) => item.category))];
        const focus = categories.length ? `Review ${categories.slice(0,2).join(" and ")}.` : "Complete the quiz and review any missed questions.";
        insights.innerHTML = `<span><b>Strength:</b> ${escapeHtml(strength)}</span><span><b>Next focus:</b> ${escapeHtml(focus)}</span>`;
      };
      new MutationObserver(updateInsights).observe(resultCircle, { childList: true, characterData: true, subtree: true });
      updateInsights();
    }
  }

  function enhanceCalculatorHistory(main) {
    const list = qs("#historyList", main);
    if (!list) return;
    let busy = false;
    const enhance = () => {
      if (busy) return;
      busy = true;
      qsa(".history-item", list).forEach((item) => {
        if (item.parentElement?.classList.contains("plw-history-row")) return;
        const row = document.createElement("div");
        row.className = "plw-history-row";
        item.parentNode.insertBefore(row, item);
        row.appendChild(item);
        const tools = document.createElement("div");
        tools.className = "plw-history-actions";
        tools.innerHTML = '<button type="button" data-history-copy title="Copy result">Copy</button><button type="button" data-history-delete title="Remove from view">×</button>';
        row.appendChild(tools);
        tools.querySelector("[data-history-copy]").addEventListener("click", async () => {
          const value = item.querySelector("strong")?.textContent.trim() || item.textContent.trim();
          try { await navigator.clipboard.writeText(value); } catch (_) { /* clipboard permission */ }
        });
        tools.querySelector("[data-history-delete]").addEventListener("click", () => row.remove());
      });
      busy = false;
    };
    new MutationObserver(enhance).observe(list, { childList: true });
    enhance();
  }

  function initShortcutKeyboardMode(main) {
    const questionCard = qs(".shortcut-question-card", main);
    if (!questionCard || questionCard.querySelector(".plw-shortcut-mode")) return;
    const mode = document.createElement("div");
    mode.className = "plw-shortcut-mode";
    mode.innerHTML = '<span>Answer mode:</span><button type="button" class="is-active" data-mode="choice">Multiple choice</button><button type="button" data-mode="keys">Press real keys</button>';
    questionCard.insertBefore(mode, qs(".shortcut-keyboard-preview", questionCard));
    mode.querySelectorAll("button").forEach((button) => button.addEventListener("click", () => {
      mode.querySelectorAll("button").forEach((item) => item.classList.toggle("is-active", item === button));
      document.body.classList.toggle("plw-shortcut-key-mode", button.dataset.mode === "keys");
      if (button.dataset.mode === "keys") qs("#shortcutQuestion", main)?.focus?.();
    }));
    document.addEventListener("keydown", (event) => {
      if (!document.body.classList.contains("plw-shortcut-key-mode") || event.repeat || event.target.matches("input,textarea,select")) return;
      try {
        if (shortcutState.answered) return;
        const current = shortcutState.questions[shortcutState.currentIndex];
        const pressed = shortcutFromEvent(event);
        if (!pressed || pressed.split("+").length < 2) return;
        event.preventDefault();
        const choice = qsa("#shortcutAnswerGrid button", main).find((button) => normaliseShortcutText(button.textContent) === normaliseShortcutText(pressed));
        if (choice) selectShortcutAnswer(choice, choice.textContent.trim());
        else {
          const feedback = qs("#shortcutFeedback", main);
          feedback.className = "feedback-box wrong";
          feedback.textContent = `You pressed ${pressed}. Try the shortcut shown in the choices.`;
        }
      } catch (_) { /* trainer state unavailable */ }
    });
  }

  function initCalculatorModes(main) {
    const shell = qs(".calculator-shell", main);
    const topbar = qs(".calculator-topbar", shell);
    const screen = qs(".calculator-screen", shell);
    if (!shell || !topbar || shell.querySelector(".plw-calculator-modebar")) return;

    const indicators = document.createElement("div");
    indicators.className = "plw-calc-indicators";
    indicators.innerHTML = '<span id="plwAngleIndicator">DEG</span><span>MEM</span><span id="plwSciIndicator">BASIC</span>';
    screen.insertBefore(indicators, screen.firstChild);

    const modebar = document.createElement("div");
    modebar.className = "plw-calculator-modebar";
    modebar.innerHTML = `
      <span class="plw-calculator-mode-copy">Choose a clean everyday keypad or unlock the full scientific functions.</span>
      <div class="plw-calculator-switch" role="group" aria-label="Calculator mode">
        <button type="button" class="is-active" data-calc-mode="basic">Basic</button>
        <button type="button" data-calc-mode="scientific">✦ Scientific</button>
      </div>`;
    qs(".calculator-tools-row", shell).insertAdjacentElement("afterend", modebar);

    modebar.querySelectorAll("button").forEach((button) => button.addEventListener("click", () => {
      const scientific = button.dataset.calcMode === "scientific";
      document.body.classList.toggle("plw-calculator-basic", !scientific);
      document.body.classList.toggle("plw-calculator-scientific", scientific);
      modebar.querySelectorAll("button").forEach((item) => item.classList.toggle("is-active", item === button));
      qs("#plwSciIndicator").textContent = scientific ? "SCI" : "BASIC";
    }));

    const angle = qs("#angleMode", shell);
    angle?.addEventListener("click", () => { qs("#plwAngleIndicator").textContent = angle.textContent.trim(); });
  }

  function initAssignments() {
    const root = qs("#assignmentsRoot");
    if (!root) return;
    document.body.classList.add("assignments-dashboard-page");

    const waitForCards = () => {
      const cards = qsa(".premium-assignment-card", root);
      if (!cards.length) { requestAnimationFrame(waitForCards); return; }
      enhanceAssignments(root, cards);
    };
    waitForCards();
  }

  function enhanceAssignments(root, cards) {
    if (root.dataset.dashboardReady === "true") return;
    root.dataset.dashboardReady = "true";
    const progress = readJSON(STORAGE.assignment, {});

    const section = root.closest(".premium-assignment-section");
    const dashboard = document.createElement("div");
    dashboard.className = "assignment-dashboard";
    dashboard.innerHTML = `
      <div class="assignment-summary-strip">
        <div class="assignment-summary-intro"><small>Practice Dashboard</small><strong>Choose, practise and keep moving.</strong><span><b data-count="completed">0</b> completed on this device</span></div>
        <div class="assignment-summary-stat"><strong data-count="total">0</strong><span>Total</span></div>
        <div class="assignment-summary-stat"><strong data-count="word">0</strong><span>Word</span></div>
        <div class="assignment-summary-stat"><strong data-count="excel">0</strong><span>Excel</span></div>
        <div class="assignment-summary-stat"><strong data-count="computer-basics">0</strong><span>Computer Basics</span></div>
      </div>
      <div class="assignment-filterbar">
        <div class="assignment-filter-tabs" role="tablist" aria-label="Assignment subjects">
          <button class="assignment-filter-tab is-active" type="button" data-assignment-filter="all">All</button>
          <button class="assignment-filter-tab" type="button" data-assignment-filter="word">Word</button>
          <button class="assignment-filter-tab" type="button" data-assignment-filter="excel">Excel</button>
          <button class="assignment-filter-tab" type="button" data-assignment-filter="computer-basics">Computer Basics</button>
        </div>
        <label class="assignment-search-field"><span aria-hidden="true">⌕</span><input type="search" id="assignmentSearch" placeholder="Search assignments" autocomplete="off"></label>
        <span class="assignment-filter-result" id="assignmentFilterResult"></span>
      </div>`;
    section.insertBefore(dashboard, root);

    const times = ["30–40 min", "20–30 min", "20–30 min"];
    cards.forEach((card, index) => {
      const id = card.id || `assignment-${index + 1}`;
      const series = qs(".premium-assignment-series", card)?.textContent.trim() || "ICT";
      const title = qs("h2", card)?.textContent.trim() || "Practice Assignment";
      const description = Array.from(card.children).find((child) => child.tagName === "P" && !child.classList.contains("premium-assignment-series"))?.textContent.trim() || "";
      const icon = qs(".premium-assignment-icon", card)?.textContent.trim() || "✓";
      const preview = qs(".premium-assignment-preview", card)?.getAttribute("src") || "";
      const actionLinks = qsa(".premium-assignment-actions a", card).map((link) => ({
        href: link.getAttribute("href"), text: link.textContent.trim(), className: link.className,
        target: link.getAttribute("target"), rel: link.getAttribute("rel"), download: link.hasAttribute("download")
      }));
      const subject = /word/i.test(series) ? "word" : /excel/i.test(series) ? "excel" : "computer-basics";
      const sessionMatch = title.match(/Session\s+([\d.]+)/i);
      const session = sessionMatch ? `Session ${sessionMatch[1]}` : "Practice";
      const state = progress[id]?.status || "not-started";

      card.dataset.subject = subject;
      card.dataset.search = `${series} ${title} ${description}`.toLowerCase();
      card.innerHTML = `
        <button class="assignment-card-toggle" type="button" aria-expanded="false" aria-controls="${id}-details">
          <span class="assignment-card-icon" aria-hidden="true">${escapeHtml(icon)}</span>
          <span class="assignment-card-copy"><span class="assignment-card-series">${escapeHtml(series)}</span><span class="assignment-card-title">${escapeHtml(title)}</span><span class="assignment-card-meta"><span>${escapeHtml(session)}</span><span>${times[index] || "20–30 min"}</span></span></span>
          <span class="assignment-expand-arrow" aria-hidden="true">⌄</span>
        </button>
        <div class="assignment-progress-row">
          <div class="assignment-progress-head"><span class="assignment-status-badge" data-status="${state}">${statusText(state)}</span><span class="assignment-saved-mark">✓ Saved on this device</span></div>
          <div class="assignment-progress-track"><span></span></div>
        </div>
        <div class="assignment-card-details" id="${id}-details" hidden>
          <div class="assignment-detail-main">${preview ? `<img src="${escapeHtml(preview)}" alt="Preview for ${escapeHtml(title)}" loading="lazy">` : ""}<p>${escapeHtml(description)}</p></div>
          <div class="assignment-detail-steps"><span>1 · Watch</span><span>2 · Download</span><span>3 · Complete</span><span>4 · Submit</span></div>
          <div class="assignment-card-actions">${actionLinks.map((link, actionIndex) => `<a href="${escapeHtml(link.href || "#")}"${link.target ? ` target="${escapeHtml(link.target)}"` : ""}${link.rel ? ` rel="${escapeHtml(link.rel)}"` : ""}${link.download ? " download" : ""} data-assignment-action="${actionIndex}">${escapeHtml(link.text)}</a>`).join("")}</div>
          <div class="assignment-progress-actions"><button class="assignment-progress-action" type="button" data-set-progress="in-progress">Mark In Progress</button><button class="assignment-progress-action" type="button" data-set-progress="completed">Mark Completed</button></div>
        </div>`;
      applyAssignmentState(card, state);

      const toggle = qs(".assignment-card-toggle", card);
      const details = qs(".assignment-card-details", card);
      toggle.addEventListener("click", () => {
        const expanded = toggle.getAttribute("aria-expanded") === "true";
        toggle.setAttribute("aria-expanded", String(!expanded));
        details.hidden = expanded;
        card.classList.toggle("is-expanded", !expanded);
      });
      qsa("[data-set-progress]", card).forEach((button) => button.addEventListener("click", () => saveAssignmentState(card, button.dataset.setProgress)));
      qsa("[data-assignment-action]", card).forEach((link) => link.addEventListener("click", () => {
        const action = Number(link.dataset.assignmentAction);
        saveAssignmentState(card, action >= 2 ? "completed" : "in-progress", false);
      }));
    });

    const filterTabs = qsa("[data-assignment-filter]", dashboard);
    const search = qs("#assignmentSearch", dashboard);
    let activeFilter = "all";
    const applyFilter = () => {
      const term = search.value.trim().toLowerCase();
      let visible = 0;
      cards.forEach((card) => {
        const matchesFilter = activeFilter === "all" || card.dataset.subject === activeFilter;
        const matchesSearch = !term || card.dataset.search.includes(term);
        card.hidden = !(matchesFilter && matchesSearch);
        if (!card.hidden) visible += 1;
      });
      qs("#assignmentFilterResult", dashboard).textContent = `${visible} assignment${visible === 1 ? "" : "s"} shown`;
      updateAssignmentCounts(cards, dashboard);
    };
    filterTabs.forEach((tab) => tab.addEventListener("click", () => {
      activeFilter = tab.dataset.assignmentFilter;
      filterTabs.forEach((item) => item.classList.toggle("is-active", item === tab));
      applyFilter();
    }));
    search.addEventListener("input", applyFilter);
    applyFilter();

    const requested = location.hash ? document.getElementById(location.hash.slice(1)) : null;
    if (requested?.classList.contains("premium-assignment-card")) {
      setTimeout(() => {
        qs(".assignment-card-toggle", requested)?.click();
        requested.scrollIntoView({ behavior: "smooth", block: "center" });
      }, 160);
    }
  }

  function statusText(status) {
    return status === "completed" ? "Completed" : status === "in-progress" ? "In Progress" : "Not Started";
  }
  function statusPercent(status) { return status === "completed" ? 100 : status === "in-progress" ? 50 : 0; }
  function applyAssignmentState(card, status) {
    const badge = qs(".assignment-status-badge", card);
    badge.dataset.status = status;
    badge.textContent = statusText(status);
    qs(".assignment-progress-track span", card).style.width = `${statusPercent(status)}%`;
    card.dataset.progressStatus = status;
  }
  function saveAssignmentState(card, status, showToast = true) {
    const progress = readJSON(STORAGE.assignment, {});
    progress[card.id] = { status, updatedAt: Date.now() };
    writeJSON(STORAGE.assignment, progress);
    applyAssignmentState(card, status);
    updateAssignmentCounts(qsa(".premium-assignment-card", qs("#assignmentsRoot")), qs(".assignment-dashboard"));
    if (showToast) showAssignmentToast("Progress saved on this device ✓");
  }
  function updateAssignmentCounts(cards, dashboard) {
    if (!dashboard) return;
    const set = (key, value) => { const el = dashboard.querySelector(`[data-count="${key}"]`); if (el) el.textContent = value; };
    set("total", cards.length);
    set("word", cards.filter((card) => card.dataset.subject === "word").length);
    set("excel", cards.filter((card) => card.dataset.subject === "excel").length);
    set("computer-basics", cards.filter((card) => card.dataset.subject === "computer-basics").length);
    set("completed", cards.filter((card) => card.dataset.progressStatus === "completed").length);
  }
  function showAssignmentToast(message) {
    let toast = qs(".assignment-save-toast");
    if (!toast) {
      toast = document.createElement("div");
      toast.className = "assignment-save-toast";
      toast.setAttribute("role", "status");
      document.body.appendChild(toast);
    }
    toast.textContent = message;
    toast.classList.add("is-visible");
    clearTimeout(showAssignmentToast.timer);
    showAssignmentToast.timer = setTimeout(() => toast.classList.remove("is-visible"), 1900);
  }

  function initQrHelpModal() {
    const helpBtn = qs("#qrHelpBtn");
    const menu = qs("#qrHelpMenu");
    if (!helpBtn || !menu || qs("#plwQrHelpModal")) return;

    const existingButtons = qsa("[data-help-step]", menu).map((button) => ({
      step: button.dataset.helpStep,
      number: button.querySelector("span")?.textContent.trim() || "•",
      text: button.querySelector("i")?.textContent.trim() || button.textContent.trim()
    }));
    const contact = menu.querySelector("a")?.getAttribute("href") || "../contact.html";
    const body = `<p>Choose a step to jump directly to that part of the QR Builder.</p><div class="plw-help-list">${existingButtons.map((item) => `
      <button class="plw-help-item" type="button" data-qr-help-step="${escapeHtml(item.step)}"><span>${escapeHtml(item.number)}</span><div><strong>${escapeHtml(item.text)}</strong><small>Open this QR Builder step</small></div></button>`).join("")}</div><a class="plw-tool-action" href="${escapeHtml(contact)}" style="width:fit-content;color:#fff;background:linear-gradient(135deg,#176ed6,#0a438e)">Contact Athanas Inspires</a>`;
    const modal = createModal({ id: "plwQrHelpModal", title: "QR Builder Help", body, wide: true });
    helpBtn.setAttribute("aria-controls", modal.id);
    helpBtn.setAttribute("aria-haspopup", "dialog");

    helpBtn.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopImmediatePropagation();
      helpBtn.setAttribute("aria-expanded", "true");
      modal.openFrom(helpBtn);
    }, true);
    modal.querySelectorAll("[data-qr-help-step]").forEach((button) => button.addEventListener("click", () => {
      const original = menu.querySelector(`[data-help-step="${CSS.escape(button.dataset.qrHelpStep)}"]`);
      modal.closeModal();
      helpBtn.setAttribute("aria-expanded", "false");
      original?.click();
    }));
    const originalClose = modal.closeModal;
    modal.closeModal = () => {
      originalClose();
      helpBtn.setAttribute("aria-expanded", "false");
    };
    modal.querySelector(".plw-modal-backdrop")?.addEventListener("click", () => helpBtn.setAttribute("aria-expanded", "false"));
    modal.querySelector(".plw-modal-close")?.addEventListener("click", () => helpBtn.setAttribute("aria-expanded", "false"));
    modal.addEventListener("keydown", (event) => { if (event.key === "Escape") helpBtn.setAttribute("aria-expanded", "false"); });
    menu.hidden = true;
    menu.setAttribute("aria-hidden", "true");
    menu.remove();
  }

  function initContinuePractising() {
    const library = qs("#digital-tools-library");
    if (!library || qs(".plw-continue-section")) return;
    const recent = readJSON(STORAGE.recentTools, []);
    if (!recent.length) return;
    const item = recent[0];
    const section = document.createElement("section");
    section.className = "plw-continue-section";
    section.setAttribute("aria-label", "Continue practising");
    section.innerHTML = `<div class="plw-continue-card"><span class="plw-continue-icon" aria-hidden="true">${escapeHtml(item.icon || "↻")}</span><div class="plw-continue-copy"><small>Continue Practising</small><strong>${escapeHtml(item.title || "Your recent tool")}</strong><span>${escapeHtml(item.detail || "Return to your latest practice session")}</span></div><a class="plw-continue-action" href="${escapeHtml(item.url || item.path || "index.html")}">Continue <span aria-hidden="true">→</span></a></div>`;
    library.parentNode.insertBefore(section, library);
  }

  function boot() {
    initAssignments();
    initQrHelpModal();
    initToolPage();
    initContinuePractising();
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot, { once: true });
  else boot();
})();

;
(() => {
  "use strict";

  const qs = (selector, root = document) => root.querySelector(selector);
  const qsa = (selector, root = document) => Array.from(root.querySelectorAll(selector));

  function initQrJourney() {
    const nav = qs(".qr-step-nav");
    const buttons = qsa(".qr-step-button", nav || document);
    if (!nav || !buttons.length || nav.querySelector(".qr-step-progress-summary")) return;

    const title = qs(".qr-step-nav-title", nav);
    const summary = document.createElement("div");
    summary.className = "qr-step-progress-summary";
    summary.setAttribute("aria-live", "polite");
    summary.innerHTML = `
      <div class="qr-step-progress-copy"><span data-qr-progress-copy>Step 1 of ${buttons.length}</span><strong data-qr-progress-percent>${Math.round(100 / buttons.length)}% complete</strong></div>
      <div class="qr-step-progress-track" aria-hidden="true"><span data-qr-progress-bar></span></div>`;
    title?.insertAdjacentElement("afterend", summary);

    const mobile = document.createElement("div");
    mobile.className = "qr-mobile-journey";
    mobile.innerHTML = `
      <button type="button" data-qr-mobile-prev aria-label="Previous QR Builder step">‹</button>
      <div class="qr-mobile-current">
        <div class="qr-mobile-current-line"><strong data-qr-mobile-title>QR Type</strong><span data-qr-mobile-copy>Step 1 of ${buttons.length}</span></div>
        <div class="qr-mobile-progress" aria-hidden="true"><span data-qr-mobile-bar></span></div>
      </div>
      <button type="button" data-qr-mobile-next aria-label="Next QR Builder step">›</button>`;
    nav.appendChild(mobile);

    const update = () => {
      let index = buttons.findIndex((button) => button.classList.contains("is-active"));
      if (index < 0) index = 0;
      const current = buttons[index];
      const percent = Math.round(((index + 1) / buttons.length) * 100);
      const label = `Step ${index + 1} of ${buttons.length}`;
      const currentTitle = qs("strong", current)?.textContent.trim() || `Step ${index + 1}`;

      const copy = qs("[data-qr-progress-copy]", summary);
      const pct = qs("[data-qr-progress-percent]", summary);
      const bar = qs("[data-qr-progress-bar]", summary);
      if (copy) copy.textContent = label;
      if (pct) pct.textContent = `${percent}% complete`;
      if (bar) bar.style.width = `${percent}%`;

      const mobileTitle = qs("[data-qr-mobile-title]", mobile);
      const mobileCopy = qs("[data-qr-mobile-copy]", mobile);
      const mobileBar = qs("[data-qr-mobile-bar]", mobile);
      const prev = qs("[data-qr-mobile-prev]", mobile);
      const next = qs("[data-qr-mobile-next]", mobile);
      if (mobileTitle) mobileTitle.textContent = currentTitle;
      if (mobileCopy) mobileCopy.textContent = label;
      if (mobileBar) mobileBar.style.width = `${percent}%`;
      if (prev) prev.disabled = index === 0;
      if (next) next.disabled = index === buttons.length - 1;

      buttons.forEach((button, buttonIndex) => {
        button.setAttribute("aria-label", `${buttonIndex + 1}. ${qs("strong", button)?.textContent.trim() || "QR Builder step"}${button.classList.contains("is-complete") ? ", completed" : ""}${buttonIndex === index ? ", current step" : ""}`);
      });
    };

    qs("[data-qr-mobile-prev]", mobile)?.addEventListener("click", () => {
      const index = buttons.findIndex((button) => button.classList.contains("is-active"));
      buttons[Math.max(0, index - 1)]?.click();
    });
    qs("[data-qr-mobile-next]", mobile)?.addEventListener("click", () => {
      const index = buttons.findIndex((button) => button.classList.contains("is-active"));
      buttons[Math.min(buttons.length - 1, index + 1)]?.click();
    });

    const observer = new MutationObserver(update);
    buttons.forEach((button) => observer.observe(button, { attributes: true, attributeFilter: ["class", "aria-current"] }));
    update();
  }

  function initQrValidation() {
    const errors = qsa(".qr-inline-error");
    if (!errors.length) return;

    const sync = (error) => {
      const visible = !error.hidden && Boolean(error.textContent.trim());
      const field = error.closest(".qr-field");
      field?.classList.toggle("has-error", visible);
      const input = field?.querySelector("input,select,textarea,[role='combobox']");
      if (visible) {
        error.setAttribute("role", "alert");
        input?.setAttribute("aria-invalid", "true");
      } else if (input?.getAttribute("aria-invalid") === "true") {
        input.setAttribute("aria-invalid", "false");
      }
    };

    errors.forEach((error) => {
      new MutationObserver(() => sync(error)).observe(error, {
        attributes: true,
        attributeFilter: ["hidden"],
        childList: true,
        characterData: true,
        subtree: true
      });
      sync(error);
    });
  }

  function initShortcutFeedback() {
    const feedback = qs("#shortcutFeedback");
    if (!feedback) return;
    feedback.setAttribute("role", "status");
    feedback.setAttribute("aria-live", "polite");

    const sync = () => {
      feedback.classList.toggle("is-visible", Boolean(feedback.textContent.trim()));
      qsa(".shortcut-choice-btn", feedback.closest(".shortcut-app-card") || document).forEach((button) => {
        if (button.classList.contains("correct-answer")) button.setAttribute("aria-label", `${button.textContent.trim()}, correct answer`);
        else if (button.classList.contains("wrong-answer")) button.setAttribute("aria-label", `${button.textContent.trim()}, your incorrect answer`);
      });
    };
    new MutationObserver(sync).observe(feedback.closest(".shortcut-question-card") || feedback, {
      attributes: true,
      childList: true,
      subtree: true,
      attributeFilter: ["class"]
    });
    sync();
  }

  function auditInvalidFields() {
    qsa("[aria-invalid='true']").forEach((field) => field.closest(".qr-field,.form-group,.field-group")?.classList.add("has-error"));
  }

  function boot() {
    initQrJourney();
    initQrValidation();
    initShortcutFeedback();
    auditInvalidFields();
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot, { once: true });
  else boot();
})();

;
(function () {
  "use strict";

  const currentPath = (window.location.pathname || "").toLowerCase();
  const compactToolPattern = /(?:^|\/)(?:typing-trainer|shortcut-trainer|quiz|calculator|qr-code-generator)\.html$/;
  const focusedDigitalToolPattern = /\/digital-tools\/(?:typing-trainer|shortcut-keys-trainer|ict-quiz|scientific-calculator|qr-code-generator)\.html$/;

  if (compactToolPattern.test(currentPath) || focusedDigitalToolPattern.test(currentPath)) return;

  const navbar = document.querySelector(".navbar");
  if (!navbar || navbar.querySelector(".ai-live-strip")) return;

  const messages = [
    "One skill today can open a door tomorrow.",
    "Keep learning—your progress is building something powerful.",
    "Learn. Believe. Grow. Build."
  ];

  const strip = document.createElement("div");
  strip.className = "ai-live-strip";
  strip.setAttribute("role", "group");
  strip.setAttribute("aria-live", "off");
  strip.setAttribute("aria-label", "Local date, time, and encouragement");
  strip.innerHTML = `
    <div class="ai-live-primary">
      <span class="ai-live-icon" aria-hidden="true">🌙</span>
      <strong class="ai-live-greeting">Good evening</strong>
      <span class="ai-live-separator" aria-hidden="true">·</span>
      <span class="ai-live-date-full"></span>
      <span class="ai-live-date-short"></span>
      <span class="ai-live-separator" aria-hidden="true">·</span>
      <time class="ai-live-clock"></time>
    </div>
    <div class="ai-live-encouragement" aria-hidden="true"><span></span></div>
  `;

  navbar.appendChild(strip);
  navbar.classList.add("has-live-strip");

  const icon = strip.querySelector(".ai-live-icon");
  const greeting = strip.querySelector(".ai-live-greeting");
  const fullDate = strip.querySelector(".ai-live-date-full");
  const shortDate = strip.querySelector(".ai-live-date-short");
  const clock = strip.querySelector(".ai-live-clock");
  const encouragement = strip.querySelector(".ai-live-encouragement");
  const encouragementText = encouragement.querySelector("span");

  const fullDateFormatter = new Intl.DateTimeFormat("en-GB", {
    weekday: "long",
    day: "numeric",
    month: "long",
    year: "numeric"
  });

  const shortDateFormatter = new Intl.DateTimeFormat("en-GB", {
    day: "numeric",
    month: "short"
  });

  const timeFormatter = new Intl.DateTimeFormat("en-US", {
    hour: "numeric",
    minute: "2-digit",
    second: "2-digit",
    hour12: true
  });

  function timeIdentity(hour) {
    if (hour >= 5 && hour < 12) return { icon: "🌅", greeting: "Good morning" };
    if (hour >= 12 && hour < 18) return { icon: "☀️", greeting: "Good afternoon" };
    if (hour >= 18 && hour < 21) return { icon: "🌇", greeting: "Good evening" };
    return { icon: "🌙", greeting: "Good evening" };
  }

  function updateClock() {
    const now = new Date();
    const identity = timeIdentity(now.getHours());

    icon.textContent = identity.icon;
    greeting.textContent = identity.greeting;
    fullDate.textContent = fullDateFormatter.format(now);
    shortDate.textContent = shortDateFormatter.format(now);
    clock.dateTime = now.toISOString();
    clock.textContent = timeFormatter.format(now);
  }

  let messageIndex = 0;
  encouragementText.textContent = messages[messageIndex];

  function rotateMessage() {
    encouragement.classList.add("is-changing");
    window.setTimeout(function () {
      messageIndex = (messageIndex + 1) % messages.length;
      encouragementText.textContent = messages[messageIndex];
      encouragement.classList.remove("is-changing");
    }, 330);
  }

  function updateScrolledState() {
    navbar.classList.toggle("nav-scrolled", window.scrollY > 42);
  }

  const menuToggle = navbar.querySelector(".menu-toggle");
  if (menuToggle) {
    const syncMenuState = function () {
      navbar.classList.toggle("live-menu-open", menuToggle.checked);
    };
    menuToggle.addEventListener("change", syncMenuState);
    syncMenuState();
  }

  updateClock();
  updateScrolledState();
  window.setInterval(updateClock, 1000);
  window.setInterval(rotateMessage, 13000);
  window.addEventListener("scroll", updateScrolledState, { passive: true });
})();
